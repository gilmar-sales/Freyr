{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Freyr","text":"<p>Freyr is a high-performance, multithreaded Entity-Component-System (ECS) library for C++23, designed for real-time simulations and games. Named after the Norse god of prosperity, Freyr brings abundance of performance through data-oriented design and lock-free parallelism.</p> <p>Its core idea is simple: organize entities into archetype chunks \u2014 contiguous memory blocks grouped by component signature \u2014 and distribute those chunks across a thread pool as independent tasks. The result is predictable cache behaviour and straightforward parallelism without manual synchronization.</p>"},{"location":"#the-big-picture","title":"The big picture","text":"<pre><code>graph TB\n    subgraph Runtime[\"Runtime Scene\"]\n        SC[\"Scene\"]\n        CM[\"ComponentManager\"]\n        EM[\"EntityManager\"]\n        EVM[\"EventManager\"]\n        SM[\"SystemManager\"]\n        TP[\"ThreadPool\"]\n    end\n\n    subgraph Storage[\"Archetype Storage\"]\n        A1[\"Archetype [Pos, Vel]\"]\n        A2[\"Archetype [Pos, Health]\"]\n        A3[\"Archetype [Pos, Vel, Mesh]\"]\n    end\n\n    subgraph Workers[\"Worker Threads\"]\n        W1[\"Worker 0\"]\n        W2[\"Worker 1\"]\n        W3[\"Worker 2\"]\n        W4[\"Worker 3\"]\n    end\n\n    SC --&gt; CM\n    SC --&gt; EM\n    SC --&gt; EVM\n    SC --&gt; SM\n    SC --&gt; TP\n\n    CM --&gt; A1\n    CM --&gt; A2\n    CM --&gt; A3\n\n    A1 --&gt;|chunk tasks| TP\n    A2 --&gt;|chunk tasks| TP\n    A3 --&gt;|chunk tasks| TP\n\n    TP --&gt;|distributes| W1\n    TP --&gt;|distributes| W2\n    TP --&gt;|distributes| W3\n    TP --&gt;|distributes| W4</code></pre>"},{"location":"#highlights","title":"Highlights","text":"<ul> <li> <p> High performance by design</p> <p>Entities are stored in contiguous archetype chunks, minimising cache misses. The work-stealing thread pool distributes chunk processing across all available cores. Component arrays are plain vectors \u2014 no pointer chasing.</p> </li> <li> <p> Simple, composable API</p> <p>Fluent builder pattern throughout \u2014 configure components, systems, and options with a single, readable call chain. No complex registration macros, no code generation.</p> </li> <li> <p> Built-in multithreading</p> <p><code>Query::EachAsync</code> distributes chunk processing across a lock-free thread pool with zero boilerplate. Each chunk becomes an independent task \u2014 perfect load balancing with work stealing.</p> </li> <li> <p> Decoupled event system</p> <p>Thread-safe publish/subscribe bus lets systems communicate without direct dependencies. Events are delivered synchronously with safe concurrent subscription.</p> </li> <li> <p> Archetype-based storage</p> <p>Entities sharing the same component types are grouped into archetypes. Component addition/removal triggers automatic archetype migration \u2014 no manual management needed.</p> </li> <li> <p> Built-in profiling</p> <p>Optional Perfetto integration provides detailed execution traces. Visualize system timings, chunk iteration duration, and thread utilisation in the Perfetto UI.</p> </li> </ul>"},{"location":"#at-a-glance","title":"At a glance","text":"<pre><code>#include &lt;Freyr/Freyr.hpp&gt;\n\nstruct Position : fr::Component { float x, y, z; };\nstruct Velocity : fr::Component { float dx, dy, dz; };\n\nclass MovementSystem : public fr::System {\npublic:\n    explicit MovementSystem(const Ref&lt;fr::Scene&gt;&amp; scene) : System(scene) {}\n\n    void Update(float dt) override {\n        // EachAsync dispatches one task per chunk \u2014 all 8 threads share the work\n        mScene-&gt;CreateQuery()-&gt;EachAsync&lt;Position, Velocity&gt;(\n            [dt](fr::Entity, Position&amp; pos, Velocity&amp; vel) {\n                pos.x += vel.dx * dt;\n                pos.y += vel.dy * dt;\n                pos.z += vel.dz * dt;\n            });\n    }\n};\n\nint main() {\n    auto app = skr::ApplicationBuilder()\n        .AddExtension&lt;fr::FreyrExtension&gt;([](fr::FreyrExtension&amp; freyr) {\n            freyr\n                .WithOptions([](fr::FreyrOptionsBuilder&amp; opts) {\n                    opts.WithMaxEntities(1'000'000).WithThreadCount(8);\n                })\n                .WithComponent&lt;Position&gt;()\n                .WithComponent&lt;Velocity&gt;()\n                .WithPipeline([](fr::PipelineBuilder&amp; pipeline) {\n                    pipeline.WithName(\"Physics\")\n                        .WithRate(60.0f)\n                        .WithSystem&lt;MovementSystem&gt;();\n                });\n        })\n        .Build&lt;MyApp&gt;();\n\n    app-&gt;Run();\n}\n</code></pre>"},{"location":"#requirements","title":"Requirements","text":"Requirement Minimum version Notes C++ standard C++23 Requires <code>std::print</code>, <code>std::format</code> CMake 3.29 FetchContent support GCC 13 Fully tested Clang 16 Fully tested MSVC 19.37 Visual Studio 2022 17.7+"},{"location":"#dependencies","title":"Dependencies","text":"Library Version Purpose Skirnir \u22650.15.3 DI container, application framework Perfetto latest Profiling (optional, via submodule) Google Test \u22651.17.0 Testing (dev only) <p>All dependencies except Perfetto are fetched automatically via CMake <code>FetchContent</code>.</p>"},{"location":"#next-steps","title":"Next steps","text":"<ul> <li>Installation \u2014 Add Freyr to your project with CMake FetchContent</li> <li>Quick Start \u2014 Build a full example in minutes</li> <li>ECS Overview \u2014 Understand the concepts behind the library</li> <li>Architecture \u2014 Deep dive into the internal design</li> </ul>"},{"location":"api/archetype-builder/","title":"ArchetypeBuilder","text":"<p><code>ArchetypeBuilder</code> efficiently creates large numbers of entities with the same component layout. It pre-allocates all chunks at once and populates them in parallel, making it significantly faster than calling <code>Scene::CreateEntity</code> in a loop for bulk spawning.</p> <p>Obtain a builder from the scene:</p> <pre><code>auto builder = scene-&gt;CreateArchetypeBuilder();\n</code></pre>"},{"location":"api/archetype-builder/#bulk-creation-flow","title":"Bulk creation flow","text":"<pre><code>graph TB\n    subgraph Setup[\"Builder Methods\"]\n        WC[\"WithComponent&amp;lt;T&amp;gt;(default)\"]\n        WV[\"WithComponent&amp;lt;T&amp;gt;(default)\"]\n        WE[\"WithEntities(count)\"]\n        FE[\"ForEach&amp;lt;T&amp;gt;(callback)\"]\n    end\n\n    subgraph Process[\"Build Process\"]\n        ALLOC[\"Pre-allocate chunks\"]\n        REG[\"Register components on archetype\"]\n        SPAWN[\"Create entities in bulk\"]\n        CALL[\"Run ForEach callbacks\"]\n        DONE[\"Return Ref&amp;lt;Archetype&amp;gt;\"]\n    end\n\n    WC --&gt; WV --&gt; WE --&gt; FE\n    FE --&gt; ALLOC\n    ALLOC --&gt; REG --&gt; SPAWN --&gt; CALL --&gt; DONE</code></pre>"},{"location":"api/archetype-builder/#basic-usage","title":"Basic usage","text":"<pre><code>scene-&gt;CreateArchetypeBuilder()\n    .WithComponent(Position { .x = 0.f, .y = 0.f })\n    .WithComponent(Velocity { .dx = 1.f })\n    .WithEntities(100'000)\n    .Build();\n</code></pre> <p>This creates 100,000 entities, each with a <code>Position</code> initialised to <code>{0, 0}</code> and a <code>Velocity</code> initialised to <code>{dx: 1}</code>. All entities share a single archetype.</p>"},{"location":"api/archetype-builder/#methods","title":"Methods","text":""},{"location":"api/archetype-builder/#withcomponenttvalue","title":"<code>WithComponent&lt;T&gt;(value)</code>","text":"<p>Declares a component type and sets the default value applied to every entity created by this builder.</p> <p>Signature: <pre><code>template &lt;typename T&gt;\n    requires IsComponent&lt;T&gt;\nArchetypeBuilder&amp; WithComponent(T component);\n</code></pre></p> <p>Complexity: $O(1)$ \u2014 registers the component type on the archetype and stores the default value function.</p> <p>Thread safety: Not thread-safe.</p> <pre><code>builder.WithComponent(Health { .max = 100, .current = 100 });\n</code></pre> <p>Template parameter <code>T</code> is inferred from the argument type \u2014 must satisfy <code>fr::IsComponent</code>.</p> <p>Calling <code>WithComponent</code> with the same type more than once is ignored (the first registration wins).</p>"},{"location":"api/archetype-builder/#withentitiescount","title":"<code>WithEntities(count)</code>","text":"<p>Sets the number of entities to create.</p> <p>Signature: <code>ArchetypeBuilder&amp; WithEntities(Entity entityCount)</code></p> <p>Complexity: $O(1)$ \u2014 stores the count.</p> <p>Thread safety: Not thread-safe.</p> <pre><code>builder.WithEntities(50'000);\n</code></pre> <p>Calling <code>Build()</code> with <code>count = 0</code> returns <code>nullptr</code> without creating any entities.</p>"},{"location":"api/archetype-builder/#foreachtsfn","title":"<code>ForEach&lt;Ts...&gt;(fn)</code>","text":"<p>Registers a post-creation callback to customise individual entities. The callback receives <code>(Entity, Ts&amp;...)</code> for every entity after they are created.</p> <p>Signature: <pre><code>template &lt;typename... Components&gt;\nArchetypeBuilder&amp; ForEach(auto&amp;&amp; f);\n</code></pre></p> <p>Complexity: $O(N)$ where N is the entity count \u2014 called once per entity during <code>Build()</code>.</p> <p>Thread safety: Not thread-safe \u2014 callbacks run during <code>Build()</code>.</p> <pre><code>builder\n    .WithComponent(Position {})\n    .WithEntities(1000)\n    .ForEach&lt;Position&gt;([](fr::Entity e, Position&amp; pos) {\n        pos.x = static_cast&lt;float&gt;(e) * 2.f; // spread entities along X\n    });\n</code></pre> <p>Multiple <code>ForEach</code> calls are executed in registration order.</p>"},{"location":"api/archetype-builder/#build","title":"<code>Build()</code>","text":"<p>Commits the builder, creates the entities, and returns a <code>Ref&lt;Archetype&gt;</code>.</p> <p>Signature: <code>Ref&lt;Archetype&gt; Build()</code></p> <p>Complexity: $O(N + C)$ where N is entity count and C is chunk count. Pre-allocates all chunks, then populates entities and runs callbacks.</p> <p>Thread safety: Not thread-safe.</p> <pre><code>auto archetype = builder.Build(); // nullptr if WithEntities(0)\n</code></pre>"},{"location":"api/archetype-builder/#archetype-reuse","title":"Archetype reuse","text":"<p>If an archetype with the same component signature already exists (from a previous <code>Build()</code> call or from <code>CreateEntity</code>), the new entities are appended to the existing archetype rather than creating a new one.</p> <pre><code>auto a1 = scene-&gt;CreateArchetypeBuilder()\n    .WithComponent(Position { .x = 0.f })\n    .WithEntities(100)\n    .Build();\n\nauto a2 = scene-&gt;CreateArchetypeBuilder()\n    .WithComponent(Position { .x = 999.f })\n    .WithEntities(50)\n    .Build();\n\nassert(a1 == a2);          // same archetype object\nassert(a1-&gt;Count() == 150); // 150 total entities across both builds\n</code></pre>"},{"location":"api/archetype-builder/#memory-layout-during-bulk-creation","title":"Memory layout during bulk creation","text":"<pre><code>graph TB\n    subgraph ArchetypeMem[\"Archetype [Position, Velocity]\"]\n        direction TB\n\n        subgraph Chunk0[\"Chunk 0 (capacity 512)\"]\n            P0[\"ComponentArray&amp;lt;Position&amp;gt;&lt;br/&gt;[p0, p1, ..., p511]\"]\n            V0[\"ComponentArray&amp;lt;Velocity&amp;gt;&lt;br/&gt;[v0, v1, ..., v511]\"]\n            E0[\"SparseSet&amp;lt;Entity&amp;gt;&lt;br/&gt;[e0, e1, ..., e511]\"]\n        end\n\n        subgraph Chunk1[\"Chunk 1 (capacity 512)\"]\n            P1[\"ComponentArray&amp;lt;Position&amp;gt;&lt;br/&gt;[p512, ..., p1023]\"]\n            V1[\"ComponentArray&amp;lt;Velocity&amp;gt;&lt;br/&gt;[v512, ..., v1023]\"]\n            E1[\"SparseSet&amp;lt;Entity&amp;gt;&lt;br/&gt;[e512, ..., e1023]\"]\n        end\n\n        subgraph ChunkN[\"... (until 100,000 entities)\"]\n            PN[\"...\"]\n        end\n    end\n\n    Chunk0 --&gt; P0 &amp; V0 &amp; E0\n    Chunk1 --&gt; P1 &amp; V1 &amp; E1</code></pre> <p>For 100,000 entities with chunk capacity 512: ~196 chunks, each fully packed.</p>"},{"location":"api/archetype-builder/#full-example-spawning-a-grid-of-enemies","title":"Full example: spawning a grid of enemies","text":"<pre><code>// Spawn a 10\u00d710 grid of enemies with unique positions and health values\nscene-&gt;CreateArchetypeBuilder()\n    .WithComponent(Position {})\n    .WithComponent(Health { .max = 50, .current = 50 })\n    .WithComponent(EnemyTag {})\n    .WithEntities(100)\n    .ForEach&lt;Position&gt;([](fr::Entity e, Position&amp; pos) {\n        pos.x = static_cast&lt;float&gt;(e % 10) * 5.f;\n        pos.y = static_cast&lt;float&gt;(e / 10) * 5.f;\n    })\n    .ForEach&lt;Health&gt;([](fr::Entity, Health&amp; hp) {\n        hp.current = hp.max; // ensure full health on spawn\n    })\n    .Build();\n</code></pre>"},{"location":"api/archetype-builder/#when-to-use-createentity-instead","title":"When to use <code>CreateEntity</code> instead","text":"Scenario Use Bulk spawn at startup (100+ entities with same layout) <code>ArchetypeBuilder</code> One-off entity during gameplay <code>Scene::CreateEntity</code> Mixed component sets per entity <code>Scene::CreateEntity</code> Dynamic entity creation based on runtime data <code>Scene::CreateEntity</code> <pre><code>// Player fires a bullet \u2014 single entity, immediate\nscene-&gt;CreateEntity(\n    Position { mPlayer.x, mPlayer.y },\n    Velocity { .dx = 0.f, .dy = 20.f },\n    BulletTag {}\n);\n</code></pre>"},{"location":"api/components/","title":"Components","text":"<p>Components are the data layer of the ECS. They hold entity state and contain no logic.</p>"},{"location":"api/components/#defining-a-component","title":"Defining a component","text":"<p>Inherit from <code>fr::Component</code> and add your fields:</p> <pre><code>#include &lt;Freyr/Freyr.hpp&gt;\n\nstruct Transform : fr::Component {\n    float x        = 0.f;\n    float y        = 0.f;\n    float z        = 0.f;\n    float scaleX   = 1.f;\n    float scaleY   = 1.f;\n    float scaleZ   = 1.f;\n    float rotX     = 0.f;\n    float rotY     = 0.f;\n    float rotZ     = 0.f;\n};\n</code></pre>"},{"location":"api/components/#rules","title":"Rules","text":"<ul> <li>Inherit from <code>fr::Component</code> (empty base)</li> <li>No virtual methods \u2014 components are value types</li> <li>No logic \u2014 behaviour belongs in systems</li> <li>All fields should have sensible defaults so <code>T {}</code> produces a valid state</li> <li>Components are copyable (they are duplicated during archetype migration)</li> </ul>"},{"location":"api/components/#tag-components","title":"Tag components","text":"<p>A component with no fields acts as a tag \u2014 useful to mark entities without adding data overhead:</p> <pre><code>struct PlayerTag : fr::Component {};\nstruct DeadTag   : fr::Component {};\nstruct EnemyTag  : fr::Component {};\nstruct EditorOnly : fr::Component {};\n</code></pre> <p>Tags cost zero bytes in component storage because they have no fields, but they occupy a slot in the archetype signature, which means:</p> <ul> <li>They enable inclusion filtering: <code>query-&gt;Each&lt;PlayerTag, Health&gt;(fn)</code></li> <li>They enable exclusion filtering: <code>query-&gt;Excluding&lt;DeadTag&gt;()-&gt;Count&lt;Health&gt;()</code></li> <li>They prevent archetype collisions: entities with <code>PlayerTag</code> are in a different archetype from those without</li> </ul> <pre><code>// Mark an entity as dead\nscene-&gt;AddComponent&lt;DeadTag&gt;(entity);\n\n// Query only living entities \u2014 exclude DeadTag\nauto alive = scene-&gt;CreateQuery()\n    -&gt;Excluding&lt;DeadTag&gt;()\n    -&gt;Count&lt;Health&gt;();\n</code></pre>"},{"location":"api/components/#registration","title":"Registration","text":"<p>Every component type must be registered with <code>FreyrExtension::WithComponent&lt;T&gt;()</code> before any entity uses it:</p> <pre><code>freyr.WithComponent&lt;Transform&gt;()\n     .WithComponent&lt;Velocity&gt;()\n     .WithComponent&lt;Health&gt;()\n     .WithComponent&lt;PlayerTag&gt;();\n</code></pre> <p>Unregistered components trigger assertions</p> <p>If <code>FREYR_ASSERTIONS</code> is enabled, using an unregistered component triggers a runtime assertion. In release builds without assertions, behaviour is undefined.</p>"},{"location":"api/components/#component-id","title":"Component ID","text":"<p>Each component type receives a unique, stable integer ID at first use:</p> <pre><code>fr::ComponentId id = fr::GetComponentId&lt;Transform&gt;(); // e.g. 0\n</code></pre> <p>IDs are assigned in the order <code>GetComponentId&lt;T&gt;()</code> is first called. They are consistent within a single run but not across runs.</p> <pre><code>// Declaration order determines ID assignment\nstruct Position : fr::Component {};   // ID 0\nstruct Velocity : fr::Component {};   // ID 1\nstruct Health   : fr::Component {};   // ID 2\n</code></pre>"},{"location":"api/components/#concept-check","title":"Concept check","text":"<p>The <code>fr::IsComponent</code> concept validates that a type derives from <code>fr::Component</code>:</p> <pre><code>template &lt;typename T&gt;\nconcept IsComponent = std::is_base_of_v&lt;fr::Component, std::remove_reference_t&lt;T&gt;&gt;;\n</code></pre> <p>Template functions in <code>Scene</code>, <code>Query</code>, <code>ArchetypeBuilder</code>, and <code>FreyrExtension</code> are constrained by this concept, giving clear compile-time errors for incorrect types.</p>"},{"location":"api/components/#design-guidelines","title":"Design guidelines","text":""},{"location":"api/components/#1-keep-components-small","title":"1. Keep components small","text":"<p>Split large component types into focused ones:</p> <pre><code>// AVOID: Giant blob\nstruct Unit : fr::Component {\n    float x, y, z;      // every system loads these\n    float hp, maxHp;    // only HealthSystem needs these\n    int   team;         // only AISystem needs this\n    char  name[64];     // only UISystem needs this\n};\n\n// PREFER: Split by system responsibility\nstruct Position : fr::Component { float x, y, z; };\nstruct Health   : fr::Component { float current, max; };\nstruct TeamTag   : fr::Component { int teamId; };\nstruct NameTag   : fr::Component { char name[64]; };\n</code></pre> <p>Smaller components = less data loaded per system = better cache efficiency.</p>"},{"location":"api/components/#2-avoid-pointers","title":"2. Avoid pointers","text":"<p>Components are copied during archetype migrations. Raw pointers inside components will dangle. Use entity IDs or indices to reference other entities:</p> <pre><code>// WRONG: Pointer becomes invalid after migration\nstruct Targeting : fr::Component {\n    fr::Entity* target; // DANGER: pointer may dangle\n};\n\n// RIGHT: Entity ID is stable\nstruct Targeting : fr::Component {\n    fr::Entity targetId = fr::Entity(-1); // -1 = no target\n};\n</code></pre>"},{"location":"api/components/#3-prefer-pod-types","title":"3. Prefer POD types","text":"<p>Components with trivial copy/move constructors let the engine <code>memcpy</code> entire arrays during migration:</p> <pre><code>struct Position : fr::Component {\n    float x, y, z;   // trivially copyable \u2192 fast migration\n};\n</code></pre> <p>Non-trivial types like <code>std::string</code> incur element-wise copy overhead:</p> <pre><code>struct StringComponent : fr::Component {\n    std::string value; // element-wise copy during migration\n};\n</code></pre>"},{"location":"api/components/#4-use-sensible-defaults","title":"4. Use sensible defaults","text":"<pre><code>struct Health : fr::Component {\n    float current = 100.f;\n    float max     = 100.f;\n    float regen   = 0.f;\n};\n</code></pre> <p>This ensures <code>Health {}</code> always produces a valid, alive entity.</p>"},{"location":"api/components/#5-group-data-by-access-pattern","title":"5. Group data by access pattern","text":"<p>Components that are always accessed together should be separate but considered as a logical pair:</p> <pre><code>// These are always read together by MovementSystem\nstruct Position : fr::Component { float x, y; };\nstruct Velocity : fr::Component { float dx, dy; };\n\n// But they COULD be separate if another system only needs Position\n// (e.g. RenderSystem only reads Position)\n</code></pre>"},{"location":"api/events/","title":"Events","text":"<p>Freyr's event system provides a thread-safe publish/subscribe bus for decoupled communication between systems. Events carry data from publishers to all active subscribers without direct references between systems.</p>"},{"location":"api/events/#defining-an-event","title":"Defining an event","text":"<p>Inherit from <code>fr::Event</code> and add your payload fields:</p> <pre><code>#include &lt;Freyr/Freyr.hpp&gt;\n\nstruct CollisionEvent : fr::Event {\n    fr::Entity entityA;\n    fr::Entity entityB;\n    float      impactForce = 0.f;\n};\n\nstruct EntityDestroyedEvent : fr::Event {\n    fr::Entity entity;\n    std::string reason;\n};\n</code></pre> <p>Events should be small, trivially copyable POD types for best performance.</p>"},{"location":"api/events/#publishing-an-event","title":"Publishing an event","text":"<p>Call <code>Scene::SendEvent</code> from anywhere that has access to the scene:</p> <p>Signature: <code>template &lt;typename T&gt; requires IsEvent&lt;T&gt; void SendEvent(T event)</code></p> <p>Complexity: $O(N)$ where N is the number of active listeners.</p> <p>Thread safety: Fully thread-safe. Multiple threads can publish concurrently.</p> <pre><code>// From within a system's Update\nvoid Update(float dt) override {\n    mScene-&gt;CreateQuery()-&gt;Each&lt;Position, Collider&gt;(\n        [this](fr::Entity a, Position&amp; posA, Collider&amp; colA) {\n            // ... detect overlap ...\n            mScene-&gt;SendEvent(CollisionEvent {\n                .entityA     = a,\n                .entityB     = b,\n                .impactForce = 42.f\n            });\n        });\n}\n</code></pre> <p>Synchronous dispatch</p> <p>Events are dispatched immediately \u2014 all subscribers are called synchronously on the publisher's thread before <code>SendEvent</code> returns. This means listeners run on the worker thread if called from within <code>EachAsync</code>.</p>"},{"location":"api/events/#subscribing-to-an-event","title":"Subscribing to an event","text":""},{"location":"api/events/#via-sceneaddeventlistener-recommended","title":"Via <code>Scene::AddEventListener</code> (recommended)","text":"<p>Signature: <code>template &lt;typename T&gt; requires IsEvent&lt;T&gt; Ref&lt;ListenerHandle&gt; AddEventListener(auto&amp;&amp; listener)</code></p> <p>Complexity: $O(1)$ amortised \u2014 appends to pending listener queue.</p> <p>Thread safety: Thread-safe. Subscriptions are queued and merged before the next flush.</p> <pre><code>class ResponseSystem : public fr::System {\npublic:\n    explicit ResponseSystem(const Ref&lt;fr::Scene&gt;&amp; scene) : System(scene) {\n        // Subscribe and store the handle\n        mCollisionHandle = scene-&gt;AddEventListener&lt;CollisionEvent&gt;(\n            [this](const CollisionEvent&amp; ev) {\n                onCollision(ev);\n            });\n    }\n\n    void Update(float dt) override {\n        // ...\n    }\n\nprivate:\n    void onCollision(const CollisionEvent&amp; ev) {\n        // apply damage, play sound, etc.\n    }\n\n    Ref&lt;fr::ListenerHandle&gt; mCollisionHandle; // keeps subscription alive\n};\n</code></pre>"},{"location":"api/events/#via-eventmanager-direct-injection","title":"Via <code>EventManager</code> (direct injection)","text":"<pre><code>class ResponseSystem : public fr::System {\npublic:\n    ResponseSystem(const Ref&lt;fr::Scene&gt;&amp; scene, Ref&lt;fr::EventManager&gt; events)\n        : System(scene)\n    {\n        mHandle = events-&gt;Subscribe&lt;CollisionEvent&gt;(\n            [](const CollisionEvent&amp; ev) { /* ... */ });\n    }\n\nprivate:\n    Ref&lt;fr::ListenerHandle&gt; mHandle;\n};\n</code></pre>"},{"location":"api/events/#subscription-lifetime","title":"Subscription lifetime","text":"<p>The subscription is active as long as the <code>ListenerHandle</code> shared_ptr is alive:</p> <pre><code>// Active \u2014 mHandle keeps the subscription alive\nRef&lt;fr::ListenerHandle&gt; mHandle = scene-&gt;AddEventListener&lt;MyEvent&gt;(...);\n\n// Unsubscribe explicitly\nmHandle.reset(); // subscription is now dead \u2014 removed on next Flush()\n</code></pre> <p>Don't discard the handle</p> <p>If you do not store the returned <code>Ref&lt;ListenerHandle&gt;</code>, it is destroyed immediately and the callback is never called:</p> <pre><code>// WRONG \u2014 handle destroyed, callback never fires\nscene-&gt;AddEventListener&lt;MyEvent&gt;([](const MyEvent&amp;) { ... });\n\n// CORRECT \u2014 store the handle\nmHandle = scene-&gt;AddEventListener&lt;MyEvent&gt;([](const MyEvent&amp;) { ... });\n</code></pre>"},{"location":"api/events/#event-lifecycle","title":"Event lifecycle","text":"<pre><code>sequenceDiagram\n    participant Publisher as System A (Publisher)\n    participant EM as EventManager\n    participant Sub1 as System B (Subscriber)\n    participant Sub2 as System C (Subscriber)\n\n    Note over Sub1: subscribe\n    Sub1-&gt;&gt;EM: Subscribe&lt;CollisionEvent&gt;(handle, callback)\n    EM-&gt;&gt;EM: Add to pending listeners\n\n    Note over EM: Flush()\n    EM-&gt;&gt;EM: Merge pending listeners into active list\n\n    Note over Publisher: later, in a different frame\n    Publisher-&gt;&gt;EM: SendEvent(CollisionEvent{...})\n    EM-&gt;&gt;Sub1: callback(ev)\n    EM-&gt;&gt;Sub2: callback(ev)\n\n    Note over Sub2: unsubscribe\n    Sub2-&gt;&gt;Sub2: handle.reset()\n    Note over EM: On next Flush(), expired handle removed</code></pre>"},{"location":"api/events/#thread-safety-guarantees","title":"Thread safety guarantees","text":"<p><code>EventManager</code> is fully thread-safe:</p> Operation Safe from multiple threads? Notes <code>Subscribe</code> Yes Queued into pending list, merged at <code>Flush()</code> <code>SendEvent</code> Yes Dispatches synchronously to active listeners <code>Flush</code> No \u2014 call from main thread only Merges pending, cleans expired <p>Thread safety details</p> <ul> <li>The active listener list is protected by an <code>RwLock</code> \u2014 multiple sends can read concurrently</li> <li>The pending listener list has a separate <code>RwLock</code> \u2014 subscriptions don't block sends</li> <li>Listeners registered while <code>SendEvent</code> is in progress are queued and merged before the next <code>Flush()</code></li> <li>They will never receive the current in-flight event</li> </ul>"},{"location":"api/events/#event-id","title":"Event ID","text":"<p>Each event type has a unique runtime ID:</p> <pre><code>fr::EventId id = fr::GetEventId&lt;CollisionEvent&gt;();\n</code></pre> <p>IDs are assigned sequentially in declaration order.</p>"},{"location":"api/events/#complete-example-damageheal-system","title":"Complete example: damage/heal system","text":"<pre><code>struct DamageEvent : fr::Event {\n    fr::Entity target;\n    int        amount;\n};\n\nstruct HealEvent : fr::Event {\n    fr::Entity target;\n    int        amount;\n};\n\nclass HealthSystem : public fr::System {\npublic:\n    HealthSystem(const Ref&lt;fr::Scene&gt;&amp; scene) : System(scene) {\n        mDamageHandle = scene-&gt;AddEventListener&lt;DamageEvent&gt;(\n            [this](const DamageEvent&amp; ev) {\n                mScene-&gt;TryGetComponents&lt;Health&gt;(ev.target,\n                    [&amp;](Health&amp; hp) { hp.current -= ev.amount; });\n            });\n\n        mHealHandle = scene-&gt;AddEventListener&lt;HealEvent&gt;(\n            [this](const HealEvent&amp; ev) {\n                mScene-&gt;TryGetComponents&lt;Health&gt;(ev.target,\n                    [&amp;](Health&amp; hp) {\n                        hp.current = std::min(hp.current + ev.amount, hp.max);\n                    });\n            });\n    }\n\nprivate:\n    Ref&lt;fr::ListenerHandle&gt; mDamageHandle;\n    Ref&lt;fr::ListenerHandle&gt; mHealHandle;\n};\n</code></pre>"},{"location":"api/extension/","title":"FreyrExtension","text":"<p><code>fr::FreyrExtension</code> integrates Freyr into a Skirnir application. It registers all services (Scene, managers, thread pool) into the DI container and wires up components and systems before the application starts.</p>"},{"location":"api/extension/#registration","title":"Registration","text":"<p>Pass it to <code>skr::ApplicationBuilder::AddExtension</code> with a configuration lambda:</p> <pre><code>skr::ApplicationBuilder()\n    .AddExtension&lt;fr::FreyrExtension&gt;([](fr::FreyrExtension&amp; freyr) {\n        freyr\n            .WithOptions([](fr::FreyrOptionsBuilder&amp; opts) {\n                opts.WithMaxEntities(500'000)\n                    .WithThreadCount(4);\n            })\n            .WithComponent&lt;Position&gt;()\n            .WithComponent&lt;Velocity&gt;()\n            .WithPipeline([](fr::PipelineBuilder&amp; pipeline) {\n                pipeline.WithName(\"Main\")\n                    .WithRate(60.0f)\n                    .WithSystem&lt;MovementSystem&gt;()\n                    .WithSystem&lt;CollisionSystem&gt;();\n            });\n    })\n    .Build&lt;MyApp&gt;();\n</code></pre> <p>All <code>With*</code> calls return <code>*this</code>, so they can be chained freely.</p>"},{"location":"api/extension/#configuration-flow","title":"Configuration flow","text":"<pre><code>graph LR\n    subgraph AppBuilder[\"ApplicationBuilder\"]\n        ADD[\"AddExtension&amp;lt;FreyrExtension&amp;gt;\"]\n    end\n\n    subgraph FreyrExt[\"FreyrExtension\"]\n        WO[\"WithOptions(opts)\"]\n        WC[\"WithComponent&amp;lt;T&amp;gt;()\"]\n        WP[\"WithPipeline(fn)\"]\n    end\n\n    subgraph Options[\"FreyrOptionsBuilder\"]\n        MAX[\"WithMaxEntities\"]\n        CHUNK[\"WithArchetypeChunkCapacity\"]\n        THREAD[\"WithThreadCount\"]\n        CORES[\"WithAllPhysicalCores\"]\n    end\n\n    subgraph Pipe[\"PipelineBuilder\"]\n        NAME[\"WithName\"]\n        RATE[\"WithRate\"]\n        WS[\"WithSystem&amp;lt;T&amp;gt;\"]\n    end\n\n    subgraph Services[\"Services Registered\"]\n        SVC[\"Scene, ComponentManager,&lt;br/&gt;EntityManager, EventManager,&lt;br/&gt;SystemManager, ThreadPool\"]\n    end\n\n    ADD --&gt;|configures| FreyrExt\n    WO --&gt;|chains to| Options\n    WP --&gt;|chains to| Pipe\n    FreyrExt --&gt;|registers| SVC</code></pre>"},{"location":"api/extension/#methods","title":"Methods","text":""},{"location":"api/extension/#withcomponentt","title":"<code>WithComponent&lt;T&gt;()</code>","text":"<p>Registers a component type with the <code>ComponentManager</code>.</p> <p>Signature: <code>template &lt;typename T&gt; requires IsComponent&lt;T&gt; FreyrExtension&amp; WithComponent()</code></p> <p>Complexity: $O(1)$ \u2014 inserts into registered components set.</p> <p>Thread safety: Not thread-safe \u2014 call during application setup only.</p> <pre><code>freyr.WithComponent&lt;TransformComponent&gt;();\n</code></pre> <p>Required registration</p> <p>Every component type used in the application must be registered before the first entity that uses it. Failing to register a component results in a runtime assertion (<code>FREYR_ASSERT</code>).</p> <p>Template parameter: - <code>T</code> \u2014 must satisfy <code>fr::IsComponent</code> (i.e. inherit from <code>fr::Component</code>)</p>"},{"location":"api/extension/#withpipelinefn","title":"<code>WithPipeline(fn)</code>","text":"<p>Defines a pipeline containing systems. Systems are constructed in registration order within each pipeline.</p> <p>Signature: <code>FreyrExtension&amp; WithPipeline(std::function&lt;void(PipelineBuilder&amp;)&gt; callback)</code></p> <p>Complexity: $O(1)$ per call \u2014 appends a pipeline config to internal vector.</p> <p>Thread safety: Not thread-safe \u2014 call during application setup only.</p> <pre><code>freyr.WithPipeline([](fr::PipelineBuilder&amp; pipeline) {\n    pipeline.WithName(\"Physics\")\n        .WithRate(60.0f)\n        .WithSystem&lt;PhysicsSystem&gt;();\n});\n</code></pre> <p>If a system needs an <code>EventManager</code>, Skirnir resolves it automatically:</p> <pre><code>class PhysicsSystem : public fr::System {\npublic:\n    PhysicsSystem(const Ref&lt;fr::Scene&gt;&amp; scene, Ref&lt;fr::EventManager&gt; events)\n        : System(scene), mEvents(events) {}\n    // ...\n};\n</code></pre> <p>Callback parameter: - <code>fn</code> \u2014 receives a <code>PipelineBuilder</code> to configure the pipeline</p> <p>See <code>PipelineBuilder</code> for pipeline configuration options.</p>"},{"location":"api/extension/#withoptionsfn","title":"<code>WithOptions(fn)</code>","text":"<p>Configures runtime parameters via a <code>FreyrOptionsBuilder</code> callback.</p> <p>Signature: <code>FreyrExtension&amp; WithOptions(const std::function&lt;void(FreyrOptionsBuilder&amp;)&gt;&amp; func)</code></p> <p>Complexity: $O(1)$.</p> <p>Thread safety: Not thread-safe \u2014 call during application setup only.</p> <pre><code>freyr.WithOptions([](fr::FreyrOptionsBuilder&amp; opts) {\n    opts.WithMaxEntities(1'000'000)\n        .WithArchetypeChunkCapacity(512)\n        .WithThreadCount(std::thread::hardware_concurrency());\n});\n</code></pre> <p>See <code>FreyrOptionsBuilder</code> for all available options.</p>"},{"location":"api/extension/#full-bootstrap-example","title":"Full bootstrap example","text":"<pre><code>int main() {\n    auto app = skr::ApplicationBuilder()\n        .AddExtension&lt;fr::FreyrExtension&gt;([](fr::FreyrExtension&amp; freyr) {\n            freyr\n                .WithOptions([](fr::FreyrOptionsBuilder&amp; opts) {\n                    opts\n                        .WithMaxEntities(2'000'000)\n                        .WithArchetypeChunkCapacity(512)\n                        .WithAllPhysicalCores();\n                })\n                .WithComponent&lt;Position&gt;()\n                .WithComponent&lt;Velocity&gt;()\n                .WithComponent&lt;Health&gt;()\n                .WithComponent&lt;PlayerTag&gt;()\n                .WithPipeline([](fr::PipelineBuilder&amp; pipeline) {\n                    pipeline.WithName(\"Physics\")\n                        .WithRate(60.0f)\n                        .WithSystem&lt;MovementSystem&gt;()\n                        .WithSystem&lt;CollisionSystem&gt;();\n                })\n                .WithPipeline([](fr::PipelineBuilder&amp; pipeline) {\n                    pipeline.WithName(\"AI\")\n                        .WithRate(10.0f)\n                        .WithSystem&lt;AISystem&gt;();\n                });\n        })\n        .Build&lt;MyApp&gt;();\n\n    app-&gt;Run();\n}\n</code></pre>"},{"location":"api/options-builder/","title":"FreyrOptionsBuilder","text":"<p><code>fr::FreyrOptionsBuilder</code> configures Freyr's runtime parameters. Used inside the <code>WithOptions</code> callback of <code>FreyrExtension</code>:</p> <pre><code>freyr.WithOptions([](fr::FreyrOptionsBuilder&amp; opts) {\n    opts.WithMaxEntities(1'000'000)\n        .WithArchetypeChunkCapacity(512)\n        .WithThreadCount(8);\n});\n</code></pre> <p>All methods return <code>*this</code> for chaining. Unset options fall back to the defaults.</p>"},{"location":"api/options-builder/#methods","title":"Methods","text":""},{"location":"api/options-builder/#withmaxentitiesn","title":"<code>WithMaxEntities(n)</code>","text":"<p>Sets the maximum number of live entities allowed simultaneously. Pre-allocates the entity index map at startup.</p> <p>Signature: <code>FreyrOptionsBuilder&amp; WithMaxEntities(const size_t maxEntities)</code></p> <p>Complexity: $O(1)$ \u2014 stores the value; pre-allocation happens during Scene construction.</p> <p>Default: <code>1 048 576</code> (1 Mi)</p> <pre><code>opts.WithMaxEntities(500'000);\n</code></pre> <p>Hard limit</p> <p>Attempting to create more entities than <code>MaxEntities</code> triggers a runtime assertion. The entity index map is sized to this value at startup, so don't set it too high for embedded/console targets.</p>"},{"location":"api/options-builder/#witharchetypechunkcapacityn","title":"<code>WithArchetypeChunkCapacity(n)</code>","text":"<p>Sets the number of entities per archetype chunk. This is the primary knob for task granularity.</p> <p>Signature: <code>FreyrOptionsBuilder&amp; WithArchetypeChunkCapacity(const size_t archetypeChunkCapacity)</code></p> <p>Complexity: $O(1)$ \u2014 stored as configuration value.</p> <p>Default: <code>512</code></p> <pre><code>opts.WithArchetypeChunkCapacity(1024);\n</code></pre> <p>Tuning chunk size</p> Value Effect 128 Fine-grained parallelism, high scheduling overhead 256 Good load balancing for light callbacks 512 Recommended starting point 1024 Better for heavy callbacks (physics, AI) 4096 Minimal overhead, poor for small entity counts <p>Start with 512, then benchmark with 128, 256, 1024, 4096 for your workload.</p> <p>See the Performance Tuning guide for detailed guidance.</p>"},{"location":"api/options-builder/#withthreadcountn","title":"<code>WithThreadCount(n)</code>","text":"<p>Sets the number of worker threads in the task pool.</p> <p>Signature: <code>FreyrOptionsBuilder&amp; WithThreadCount(const size_t threadCount)</code></p> <p>Complexity: $O(1)$ \u2014 thread creation happens at pool construction.</p> <p>Default: <code>4</code></p> <pre><code>opts.WithThreadCount(std::thread::hardware_concurrency());\n</code></pre> <p>Thread count guidelines</p> <ul> <li>Value <code>0</code> is reserved for future auto-detection (currently defaults to 4)</li> <li>Prefer physical core count: use <code>WithAllPhysicalCores()</code></li> <li>Reserve 1 core for the main thread if it does significant work</li> </ul>"},{"location":"api/options-builder/#withallphysicalcores","title":"<code>WithAllPhysicalCores()</code>","text":"<p>Configures the engine to utilize all available physical CPU cores, excluding logical processors created by SMT (Hyper-Threading).</p> <p>Signature: <code>FreyrOptionsBuilder&amp; WithAllPhysicalCores()</code></p> <p>Complexity: $O(N)$ where N is the number of CPU packages \u2014 queries OS topology.</p> <p>Thread safety: Not thread-safe \u2014 call during application setup.</p> <pre><code>opts.WithAllPhysicalCores();\n</code></pre> <p>On a system with 8 physical cores (16 with HT), this sets thread count to 8 (not 16).</p> <p>Platform support</p> <ul> <li>Windows: Uses <code>GetLogicalProcessorInformation</code></li> <li>macOS: Uses <code>sysctlbyname(\"hw.physicalcpu\")</code></li> <li>Linux: Parses <code>/proc/cpuinfo</code> for physical/core IDs</li> </ul>"},{"location":"api/options-builder/#default-values-summary","title":"Default values summary","text":"Option Default Description <code>MaxEntities</code> 1,048,576 Maximum live entity count <code>ArchetypeChunkCapacity</code> 512 Entities per chunk <code>ThreadCount</code> 4 Worker threads"},{"location":"api/options-builder/#underlying-options-struct","title":"Underlying options struct","text":"<p>The builder produces a <code>FreyrOptions</code> struct used internally:</p> <pre><code>struct FreyrOptions {\n    std::uint64_t MaxEntities            = 1024 * 1024;\n    std::uint64_t ArchetypeChunkCapacity = 512;\n    std::uint64_t MaxSystems             = 1024;\n    std::uint64_t ThreadCount            = 4;\n};\n</code></pre> <p><code>MaxSystems</code> is currently fixed and not configurable via the builder. It controls the maximum number of distinct system types that can be registered.</p>"},{"location":"api/pipeline-builder/","title":"PipelineBuilder","text":"<p><code>PipelineBuilder</code> defines a named collection of systems that run at a specific update rate. Pipelines allow you to organize systems into logical groups with different execution frequencies.</p> <p>Obtain a builder from <code>FreyrExtension::WithPipeline()</code>:</p> <pre><code>freyr.WithPipeline([](fr::PipelineBuilder&amp; pipeline) {\n    pipeline.WithName(\"Main\")\n        .WithRate(60.0f)\n        .WithSystem&lt;MovementSystem&gt;()\n        .WithSystem&lt;RenderSystem&gt;();\n});\n</code></pre>"},{"location":"api/pipeline-builder/#pipeline-lifecycle","title":"Pipeline lifecycle","text":"<pre><code>sequenceDiagram\n    participant SM as SystemManager\n    participant Pipeline as Physics Pipeline\n    participant S1 as MovementSystem\n    participant S2 as CollisionSystem\n    participant S3 as RenderSystem\n\n    Note over SM: Each frame, Accumulate(dt)\n    SM-&gt;&gt;SM: accumulator += dt\n\n    alt accumulator &gt;= rate (1/60s)\n        SM-&gt;&gt;Pipeline: PreUpdate\n        Pipeline-&gt;&gt;S1: PreUpdate(dt)\n        Pipeline-&gt;&gt;S2: PreUpdate(dt)\n        Pipeline-&gt;&gt;S3: PreUpdate(dt)\n        SM-&gt;&gt;SM: WaitForAllTasks()\n        SM-&gt;&gt;SM: DestroyEntities()\n\n        SM-&gt;&gt;Pipeline: Update\n        Pipeline-&gt;&gt;S1: Update(dt)\n        Pipeline-&gt;&gt;S2: Update(dt)\n        Pipeline-&gt;&gt;S3: Update(dt)\n        SM-&gt;&gt;SM: WaitForAllTasks()\n        SM-&gt;&gt;SM: DestroyEntities()\n\n        SM-&gt;&gt;Pipeline: PostUpdate\n        Pipeline-&gt;&gt;S1: PostUpdate(dt)\n        Pipeline-&gt;&gt;S2: PostUpdate(dt)\n        Pipeline-&gt;&gt;S3: PostUpdate(dt)\n        SM-&gt;&gt;SM: WaitForAllTasks()\n        SM-&gt;&gt;SM: DestroyEntities()\n\n        SM-&gt;&gt;SM: accumulator -= rate\n    else\n        Note over SM: Pipeline does not execute this frame\n    end</code></pre>"},{"location":"api/pipeline-builder/#methods","title":"Methods","text":""},{"location":"api/pipeline-builder/#withnamename","title":"<code>WithName(name)</code>","text":"<p>Sets the pipeline's display name, used in profiling traces and debugging.</p> <p>Signature: <code>PipelineBuilder&amp; WithName(const std::string_view name)</code></p> <p>Complexity: $O(1)$.</p> <p>Thread safety: Not thread-safe \u2014 call during application setup.</p> Parameter Type Default <code>name</code> <code>std::string_view</code> <code>\"Pipeline {id}\"</code> <pre><code>pipeline.WithName(\"Physics\");\n</code></pre>"},{"location":"api/pipeline-builder/#withraterate","title":"<code>WithRate(rate)</code>","text":"<p>Sets the target update rate for this pipeline in Hz. The pipeline will be scheduled to run at this frequency.</p> <p>Signature: <code>PipelineBuilder&amp; WithRate(const float rate)</code></p> <p>Complexity: $O(1)$.</p> <p>Thread safety: Not thread-safe \u2014 call during application setup.</p> Parameter Type Default <code>rate</code> <code>float</code> <code>0.0</code> (runs every frame) <pre><code>pipeline.WithRate(60.0f); // 60 Hz \u2192 updates every 16.67ms\npipeline.WithRate(30.0f); // 30 Hz \u2192 updates every 33.33ms\npipeline.WithRate(0.0f);  // every frame\n</code></pre> <p>Rate conversion</p> <p>The builder converts <code>rate</code> \u2192 interval: <code>interval = rate &gt; 0 ? 1.0f / rate : 0.0f</code>. An interval of <code>0.0f</code> means the pipeline runs every frame.</p> <p>Use cases for different rates:</p> Rate Typical use 0 Rendering, input 60 Physics, movement 30 Mid-frequency checks 10 AI, pathfinding 1-5 Inventory, economy"},{"location":"api/pipeline-builder/#withsystemt","title":"<code>WithSystem&lt;T&gt;()</code>","text":"<p>Registers a system type to this pipeline. Systems execute in registration order within each phase.</p> <p>Signature: <code>template &lt;typename T&gt; requires IsSystem&lt;T&gt; PipelineBuilder&amp; WithSystem()</code></p> <p>Complexity: $O(1)$ \u2014 appends factory function to internal lists.</p> <p>Thread safety: Not thread-safe \u2014 call during application setup.</p> <pre><code>pipeline\n    .WithSystem&lt;InputSystem&gt;()       // executes first\n    .WithSystem&lt;MovementSystem&gt;()    // depends on input\n    .WithSystem&lt;CollisionSystem&gt;()   // depends on movement\n    .WithSystem&lt;RenderSystem&gt;();     // depends on everything\n</code></pre> <p>System registration also registers DI services</p> <p>Each <code>WithSystem&lt;T&gt;()</code> call automatically registers the system as a singleton in Skirnir's DI container: <pre><code>services.AddSingleton&lt;T&gt;();\n</code></pre> This means systems can inject dependencies via constructor parameters.</p> <p>Template parameter: - <code>T</code> \u2014 must satisfy <code>fr::IsSystem</code> (i.e. inherit from <code>fr::System</code>).</p>"},{"location":"api/pipeline-builder/#multiple-pipeline-configurations","title":"Multiple pipeline configurations","text":"<p>Define separate pipelines for different subsystems:</p> <pre><code>freyr\n    .WithPipeline([](fr::PipelineBuilder&amp; physics) {\n        physics.WithName(\"Physics\")\n            .WithRate(60.0f)\n            .WithSystem&lt;PhysicsSystem&gt;()\n            .WithSystem&lt;CollisionSystem&gt;();\n    })\n    .WithPipeline([](fr::PipelineBuilder&amp; ai) {\n        ai.WithName(\"AI\")\n            .WithRate(10.0f)\n            .WithSystem&lt;MonsterSystem&gt;()\n            .WithSystem&lt;NPCSystem&gt;();\n    })\n    .WithPipeline([](fr::PipelineBuilder&amp; render) {\n        render.WithName(\"Render\")\n            .WithRate(0.0f) // every frame\n            .WithSystem&lt;RenderSystem&gt;();\n    });\n</code></pre>"},{"location":"api/pipeline-builder/#system-execution-order","title":"System execution order","text":"<p>Within a pipeline, systems execute in registration order during each lifecycle phase. If <code>SystemB</code> depends on results produced by <code>SystemA</code>, register <code>SystemA</code> first:</p> <pre><code>pipeline.WithSystem&lt;InputSystem&gt;()      // runs first\n        .WithSystem&lt;MovementSystem&gt;()   // uses input\n        .WithSystem&lt;CollisionSystem&gt;()  // uses movement\n        .WithSystem&lt;RenderSystem&gt;();    // runs last\n</code></pre> <p>Cross-entity dependencies</p> <p>For data dependencies within a system's callback, use <code>ForEach</code> (sequential) rather than <code>ForEachAsync</code> when the callback reads from other entities. <code>EachAsync</code> is safe only when entities are independent.</p>"},{"location":"api/query/","title":"Query","text":"<p><code>fr::Query</code> provides a fluent API for filtering and querying entities by their component composition. Queries support exclusion filters and various terminal operations for collecting or processing matching entities.</p> <p>Obtain a Query instance via <code>Scene::CreateQuery()</code>:</p> <pre><code>auto query = scene-&gt;CreateQuery();\n</code></pre>"},{"location":"api/query/#query-flow","title":"Query flow","text":"<pre><code>graph TB\n    subgraph QueryFlow[\"Query Execution Flow\"]\n        Q[\"Create Query&lt;br/&gt;Scene::CreateQuery()\"]\n        F[\"Configure Filter&lt;br/&gt;query-&gt;Excluding&amp;lt;T&amp;gt;()\"]\n        T[\"Terminal Operation&lt;br/&gt;Each / EachAsync / Count / ...\"]\n        M[\"Match Archetypes&lt;br/&gt;Signature matching\"]\n        D[\"Dispatch&lt;br/&gt;Chunk iteration\"]\n    end\n\n    Q --&gt; F --&gt; T\n    T --&gt; M\n    M --&gt;|match| D\n</code></pre>"},{"location":"api/query/#filter-methods","title":"Filter methods","text":""},{"location":"api/query/#excludingts","title":"<code>Excluding&lt;Ts...&gt;</code>","text":"<p>Adds component types to the exclusion filter. Entities with any of the specified components are excluded from query results.</p> <p>Signature: <pre><code>template &lt;typename... Ts&gt;\n    requires(IsComponent&lt;Ts&gt; and ...)\nQuery&amp; Excluding();\n</code></pre></p> <p>Complexity: $O(K)$ where K is the number of excluded types \u2014 updates the exclusion signature bitset.</p> <p>Thread safety: Not thread-safe \u2014 the filter is local to this query instance.</p> <pre><code>query-&gt;Excluding&lt;DisabledTag, EditorOnly&gt;();\n</code></pre> <p>Inclusion vs exclusion</p> <p>The inclusion filter is specified implicitly via the component template arguments on terminal operations (e.g. <code>Count&lt;Position, Velocity&gt;</code> includes entities with both Position and Velocity). The exclusion filter is specified explicitly via <code>Excluding&lt;Ts...&gt;()</code>.</p>"},{"location":"api/query/#terminal-operations","title":"Terminal operations","text":""},{"location":"api/query/#countts","title":"<code>Count&lt;Ts...&gt;</code>","text":"<p>Returns the total number of entities having all specified component types.</p> <p>Signature: <pre><code>template &lt;typename... Ts&gt;\n    requires(IsComponent&lt;Ts&gt; and ...)\nstd::size_t Count();\n</code></pre></p> <p>Complexity: $O(A)$ where A is the number of archetypes \u2014 iterates all archetypes, sums matching counts.</p> <p>Thread safety: Not thread-safe \u2014 reads archetype data on the calling thread.</p> <pre><code>auto alive = query-&gt;Count&lt;Health&gt;();\nauto movable = query-&gt;Excluding&lt;StunnedTag&gt;()-&gt;Count&lt;Position, Velocity&gt;();\n</code></pre>"},{"location":"api/query/#entitieswithts","title":"<code>EntitiesWith&lt;Ts...&gt;</code>","text":"<p>Returns all entity IDs that have all specified component types.</p> <p>Signature: <pre><code>template &lt;typename... Ts&gt;\n    requires(IsComponent&lt;Ts&gt; and ...)\nstd::vector&lt;Entity&gt; EntitiesWith();\n</code></pre></p> <p>Complexity: $O(N)$ where N is the total number of matching entities \u2014 collects entity IDs from all chunks.</p> <p>Thread safety: Not thread-safe \u2014 reads archetype data on the calling thread.</p> <pre><code>auto players = query-&gt;Excluding&lt;DeadTag&gt;()\n    -&gt;EntitiesWith&lt;PlayerTag, Health&gt;();\n</code></pre> <p>Vector allocation</p> <p><code>EntitiesWith</code> allocates a new vector each call. For frequent use, prefer <code>Each</code> or <code>EachAsync</code> iteration to avoid allocation overhead.</p>"},{"location":"api/query/#finduniquets","title":"<code>FindUnique&lt;Ts...&gt;</code>","text":"<p>Returns the single matching entity, or <code>std::nullopt</code> if zero or more than one entity matches.</p> <p>Signature: <pre><code>template &lt;typename... Ts&gt;\n    requires(IsComponent&lt;Ts&gt; and ...)\nstd::optional&lt;Entity&gt; FindUnique();\n</code></pre></p> <p>Complexity: $O(A)$ where A is the number of archetypes \u2014 early-exits on second match.</p> <p>Thread safety: Not thread-safe.</p> <pre><code>auto camera = query-&gt;FindUnique&lt;CameraComponent&gt;();\nif (camera.has_value()) {\n    // exactly one entity has a CameraComponent\n}\n</code></pre> <p>Use for singletons</p> <p><code>FindUnique</code> is ideal for singleton entities like the camera, player character, or world settings.</p>"},{"location":"api/query/#firstts","title":"<code>First&lt;Ts...&gt;</code>","text":"<p>Returns the first entity matching the query, or <code>std::nullopt</code> if none found.</p> <p>Signature: <pre><code>template &lt;typename... Ts&gt;\n    requires(IsComponent&lt;Ts&gt; and ...)\nstd::optional&lt;Entity&gt; First();\n</code></pre></p> <p>Complexity: $O(A)$ where A is the number of archetypes \u2014 stops at first match.</p> <p>Thread safety: Not thread-safe.</p> <pre><code>auto entity = query-&gt;First&lt;PlayerTag&gt;();\n</code></pre>"},{"location":"api/query/#iteratets","title":"<code>Iterate&lt;Ts...&gt;</code>","text":"<p>Collects all matching entities and their components into a vector of tuples.</p> <p>Signature: <pre><code>template &lt;typename... Ts&gt;\n    requires(IsComponent&lt;Ts&gt; and ...)\nauto Iterate() -&gt; std::vector&lt;std::tuple&lt;Entity, Ts...&gt;&gt;;\n</code></pre></p> <p>Complexity: $O(N)$ where N is the total number of matching entities. Allocates a vector of tuples.</p> <p>Thread safety: Not thread-safe.</p> <pre><code>auto entities = query-&gt;Iterate&lt;Position, Velocity&gt;();\nfor (auto&amp;&amp; [entity, pos, vel] : entities) {\n    // process each entity with its components\n}\n</code></pre> <p>Memory usage</p> <p><code>Iterate</code> copies component data into tuples. For large result sets, prefer <code>Each</code> or <code>EachAsync</code> which process data in-place without copying.</p>"},{"location":"api/query/#transformts","title":"<code>Transform&lt;Ts...&gt;</code>","text":"<p>Maps each entity to a transformed value and returns a vector of results.</p> <p>Signature: <pre><code>template &lt;typename... Ts&gt;\n    requires(IsComponent&lt;Ts&gt; and ...)\nauto Transform(auto&amp;&amp; callback) -&gt; std::vector&lt;decltype(callback(...))&gt;;\n</code></pre></p> <p>Complexity: $O(N)$ where N is the number of matching entities. Allocates a vector of results.</p> <p>Thread safety: Not thread-safe.</p> <p>Callback can accept either <code>(Entity, Ts&amp;...)</code> or <code>(Ts&amp;...)</code>:</p> <pre><code>// With entity ID\nauto distances = query-&gt;Transform&lt;Position&gt;(\n    [origin](Entity e, Position&amp; pos) {\n        return distance(origin, pos);\n    });\n\n// Without entity ID\nauto healthValues = query-&gt;Transform&lt;Health&gt;(\n    [](Health&amp; h) { return h.current; });\n</code></pre>"},{"location":"api/query/#mapts","title":"<code>Map&lt;Ts...&gt;</code>","text":"<p>Applies a transform function and returns results as a vector, ordered by entity.</p> <p>Signature: <pre><code>template &lt;typename... Ts&gt;\n    requires(IsComponent&lt;Ts&gt; and ...)\nauto Map(auto&amp;&amp; f) -&gt; std::vector&lt;decltype(callback(...))&gt;;\n</code></pre></p> <p>Complexity: $O(N)$ where N is the number of matching entities. Pre-allocates result vector.</p> <p>Thread safety: Not thread-safe.</p> <pre><code>auto distances = query-&gt;Map&lt;Position&gt;(\n    [origin](Entity e, Position&amp; pos) {\n        float dx = pos.x - origin.x;\n        float dy = pos.y - origin.y;\n        return std::sqrt(dx*dx + dy*dy);\n    });\n</code></pre> <p>Unlike <code>Transform</code>, <code>Map</code> pre-allocates the result vector and fills by index.</p>"},{"location":"api/query/#reducets","title":"<code>Reduce&lt;Ts...&gt;</code>","text":"<p>Accumulates values across all matching entities.</p> <p>Signature: <pre><code>template &lt;typename... Ts&gt;\n    requires(IsComponent&lt;Ts&gt; and ...)\nauto Reduce(auto&amp;&amp; callback, auto seed) -&gt; decltype(seed);\n</code></pre></p> <p>Complexity: $O(N)$ where N is the number of matching entities.</p> <p>Thread safety: Not thread-safe \u2014 runs on the calling thread.</p> <p>Callback signature: <code>(ResultType, Ts&amp;...) -&gt; ResultType</code>:</p> <pre><code>// Sum all health\nauto totalHealth = query-&gt;Reduce&lt;Health&gt;(\n    [](float acc, Health&amp; h) { return acc + h.current; },\n    0.f);\n\n// Find max velocity\nauto maxSpeed = query-&gt;Reduce&lt;Velocity&gt;(\n    [](float acc, Velocity&amp; v) {\n        float speed = std::sqrt(v.dx*v.dx + v.dy*v.dy + v.dz*v.dz);\n        return std::max(acc, speed);\n    },\n    0.f);\n</code></pre>"},{"location":"api/query/#iteration-methods","title":"Iteration methods","text":""},{"location":"api/query/#eachts","title":"<code>Each&lt;Ts...&gt;</code>","text":"<p>Synchronous iteration over all matching entities.</p> <p>Signature: <pre><code>template &lt;typename... Ts&gt;\n    requires(IsComponent&lt;Ts&gt; and ...)\nQuery&amp; Each(auto&amp;&amp; action);\n</code></pre></p> <p>Complexity: $O(N)$ where N is the number of matching entities.</p> <p>Thread safety: Runs on the calling thread. Safe for cross-entity reads/writes.</p> <pre><code>query-&gt;WithLabel(\"UpdatePositions\")\n    -&gt;Each&lt;Position, Velocity&gt;([](Entity e, Position&amp; pos, Velocity&amp; vel) {\n        pos.x += vel.dx * dt;\n    });\n</code></pre>"},{"location":"api/query/#eachasyncts","title":"<code>EachAsync&lt;Ts...&gt;</code>","text":"<p>Dispatches chunk tasks to the thread pool for parallel execution.</p> <p>Signature: <pre><code>template &lt;typename... Ts&gt;\n    requires(IsComponent&lt;Ts&gt; and ...)\nQuery&amp; EachAsync(auto&amp;&amp; action);\n</code></pre></p> <p>Complexity: $O(N)$ total work, distributed across threads. $O(C)$ overhead where C is chunk count.</p> <p>Thread safety: The action callback must be safe for concurrent invocation on different entities. Each entity is processed by exactly one thread.</p> <pre><code>query-&gt;WithLabel(\"Physics::Integrate\")\n    -&gt;EachAsync&lt;Position, Velocity&gt;([](Entity e, Position&amp; pos, Velocity&amp; vel) {\n        pos.x += vel.dx * dt;\n    });\nscene-&gt;ExecuteTasks(); // wait for completion\n</code></pre> Method Blocking Thread pool Use when <code>Each</code> Yes No Sequential, ordered, cross-entity reads <code>EachAsync</code> No Yes Independent entities, parallel execution"},{"location":"api/query/#utility-methods","title":"Utility methods","text":""},{"location":"api/query/#withlabel","title":"<code>WithLabel</code>","text":"<p>Assigns a human-readable label for profiling and debugging.</p> <p>Signature: <pre><code>Query&amp; WithLabel(const std::string_view name);\n</code></pre></p> <p>Complexity: $O(1)$ \u2014 copies the label string.</p> <p>Thread safety: Not thread-safe.</p> <pre><code>query-&gt;WithLabel(\"PhysicsUpdate\");\n</code></pre> <p>When <code>FREYR_PROFILING=ON</code>, the label appears in Perfetto traces as the trace event name.</p>"},{"location":"api/query/#important-notes","title":"Important notes","text":"<ul> <li>Query instances should not be stored long-term as they hold references to <code>ComponentManager</code></li> <li>Use <code>Scene::CreateQuery()</code> to obtain a fresh query instance when needed</li> <li>The <code>QueryAggregator</code> coordinates async query execution across worker threads</li> <li>Callbacks passed to <code>Each</code> and <code>EachAsync</code> must not throw \u2014 behaviour is undefined in parallel execution</li> <li><code>EachAsync</code> callbacks must not call <code>Scene::Update</code> or <code>DestroyEntity</code> for entities being iterated</li> </ul>"},{"location":"api/scene/","title":"Scene","text":"<p><code>fr::Scene</code> is the central orchestrator of Freyr. It provides the full entity lifecycle API, component operations, event helpers, and the main update loop.</p> <p>Obtain an instance from the service provider:</p> <pre><code>class MyApp : public skr::IApplication {\npublic:\n    explicit MyApp(const Ref&lt;skr::ServiceProvider&gt;&amp; sp) : IApplication(sp) {\n        mScene = sp-&gt;GetService&lt;fr::Scene&gt;();\n    }\n};\n</code></pre>"},{"location":"api/scene/#entity-management","title":"Entity management","text":""},{"location":"api/scene/#createentity","title":"<code>CreateEntity</code>","text":"<p>Creates a new entity, optionally with initial component values.</p> <p>Signature: <pre><code>// Create with component values, returns entity ID\ntemplate &lt;typename... Ts&gt;\n    requires(IsComponent&lt;Ts&gt; and ...)\nEntity CreateEntity(const Ts&amp;... components);\n\n// Create with component values and a callback\ntemplate &lt;typename... Ts, typename TFunc&gt;\n    requires(IsComponent&lt;Ts&gt; and ...) and (not IsComponent&lt;TFunc&gt;)\nvoid CreateEntity(TFunc&amp;&amp; callback, const Ts&amp;... components);\n</code></pre></p> <p>Complexity: $O(1)$ amortised \u2014 allocates entity ID and registers components.</p> <p>Thread safety: Not thread-safe \u2014 call from main thread or within a system's lifecycle hook.</p> <pre><code>// No components \u2014 entity exists but has no data\nfr::Entity e = scene-&gt;CreateEntity();\n\n// With initial components\nfr::Entity e = scene-&gt;CreateEntity(\n    Position { .x = 10.f },\n    Velocity {}\n);\n\n// Via callback \u2014 receives the entity ID immediately\nscene-&gt;CreateEntity([](fr::Entity e) {\n    // e is available here immediately\n}, Position {}, Velocity {});\n</code></pre>"},{"location":"api/scene/#destroyentity","title":"<code>DestroyEntity</code>","text":"<p>Schedules an entity for destruction at the end of the current frame.</p> <p>Signature: <code>void DestroyEntity(const Entity&amp; entity)</code></p> <p>Complexity: $O(1)$ \u2014 inserts into a deferred destruction set.</p> <p>Thread safety: Not thread-safe \u2014 call from main thread or system hooks.</p> <pre><code>scene-&gt;DestroyEntity(e);\n// entity is still alive until Update() returns\n</code></pre> <p>Deferred destruction</p> <p>Destruction is deferred \u2014 the entity is added to a pending set and removed at the end of the current <code>Update</code> call, after all systems have run and all tasks have completed. This prevents iterator invalidation.</p>"},{"location":"api/scene/#component-operations","title":"Component operations","text":""},{"location":"api/scene/#addcomponentt","title":"<code>AddComponent&lt;T&gt;</code>","text":"<p>Adds a single component to an existing entity.</p> <p>Signature: <pre><code>template &lt;typename T&gt;\n    requires IsComponent&lt;T&gt;\nvoid AddComponent(const Entity&amp; entity, const T&amp; component = {});\n</code></pre></p> <p>Complexity: $O(A)$ amortised \u2014 may trigger archetype migration.</p> <p>Thread safety: Not thread-safe \u2014 call from main thread or system hooks.</p> <pre><code>scene-&gt;AddComponent&lt;Health&gt;(entity, Health { .hp = 100 });\n</code></pre>"},{"location":"api/scene/#addcomponentsts","title":"<code>AddComponents&lt;Ts...&gt;</code>","text":"<p>Adds multiple components in one call. More efficient than calling <code>AddComponent</code> repeatedly.</p> <p>Signature: <pre><code>template &lt;typename... Ts&gt;\n    requires(IsComponent&lt;Ts&gt; and ...)\nvoid AddComponents(const Entity entity, const Ts&amp;... components);\n</code></pre></p> <p>Complexity: $O(A)$ amortised \u2014 single archetype migration instead of multiple.</p> <p>Thread safety: Not thread-safe.</p> <pre><code>scene-&gt;AddComponents&lt;Position, Velocity&gt;(\n    entity,\n    Position {},\n    Velocity { .dx = 1.f });\n</code></pre>"},{"location":"api/scene/#removecomponentt","title":"<code>RemoveComponent&lt;T&gt;</code>","text":"<p>Removes a component and migrates the entity to the appropriate archetype.</p> <p>Signature: <pre><code>template &lt;typename T&gt;\n    requires IsComponent&lt;T&gt;\nvoid RemoveComponent(const Entity entity);\n</code></pre></p> <p>Complexity: $O(A)$ amortised \u2014 may trigger archetype migration.</p> <p>Thread safety: Not thread-safe.</p> <pre><code>scene-&gt;RemoveComponent&lt;Velocity&gt;(entity); // entity stops moving\n</code></pre>"},{"location":"api/scene/#removecomponentsts","title":"<code>RemoveComponents&lt;Ts...&gt;</code>","text":"<p>Removes multiple components from an entity in a single call.</p> <p>Signature: <pre><code>template &lt;typename... Ts&gt;\n    requires(IsComponent&lt;Ts&gt; and ...)\nvoid RemoveComponents(const Entity entity);\n</code></pre></p> <p>Complexity: $O(A)$ amortised \u2014 single archetype migration.</p> <p>Thread safety: Not thread-safe.</p> <pre><code>scene-&gt;RemoveComponents&lt;Velocity, Health&gt;(entity);\n</code></pre>"},{"location":"api/scene/#hascomponentt-hascomponentsts","title":"<code>HasComponent&lt;T&gt;</code> / <code>HasComponents&lt;Ts...&gt;</code>","text":"<p>Checks if an entity has a specific component or set of components.</p> <p>Signature: <pre><code>template &lt;typename T&gt;\n    requires IsComponent&lt;T&gt;\nbool HasComponent(const Entity&amp; entity) const;\n\ntemplate &lt;typename... Ts&gt;\n    requires(IsComponent&lt;Ts&gt; and ...)\nbool HasComponents(const Entity&amp; entity) const;\n</code></pre></p> <p>Complexity: $O(1)$ \u2014 direct archetype lookup.</p> <p>Thread safety: Thread-safe for reads (uses archetype signature bitset).</p> <pre><code>if (scene-&gt;HasComponents&lt;Position, Velocity&gt;(entity)) {\n    // safe to iterate\n}\n</code></pre>"},{"location":"api/scene/#trygetcomponentsts","title":"<code>TryGetComponents&lt;Ts...&gt;</code>","text":"<p>Retrieves multiple components and invokes callback if all exist.</p> <p>Signature: <pre><code>template &lt;typename... Ts&gt;\n    requires(IsComponent&lt;Ts&gt; and ...)\nbool TryGetComponents(const Entity&amp; entity, auto&amp;&amp; callback);\n</code></pre></p> <p>Complexity: $O(1)$ \u2014 archetype membership test followed by direct component array access.</p> <p>Thread safety: Thread-safe for reads.</p> <pre><code>bool found = scene-&gt;TryGetComponents&lt;Position, Health&gt;(entity,\n    [](Position&amp; pos, Health&amp; hp) {\n        pos.x += 1.f;\n        hp.current -= 5;\n    });\n</code></pre> <p>Returns <code>true</code> if all components existed and callback was invoked.</p>"},{"location":"api/scene/#queries","title":"Queries","text":""},{"location":"api/scene/#createquery","title":"<code>CreateQuery</code>","text":"<p>Creates a new Query instance for entity searching.</p> <p>Signature: <pre><code>Ref&lt;Query&gt; CreateQuery() const;\n\ntemplate &lt;typename TQuery&gt;\n    requires(std::is_base_of_v&lt;Query, TQuery&gt;)\nRef&lt;TQuery&gt; CreateQuery();\n</code></pre></p> <p>Complexity: $O(1)$ \u2014 retrieved from DI container.</p> <p>Thread safety: Not thread-safe.</p> <pre><code>auto query = scene-&gt;CreateQuery();\nauto players = query-&gt;Excluding&lt;DeadTag&gt;()\n    -&gt;EntitiesWith&lt;PlayerTag, Health&gt;();\n\n// Specialized query type\nauto customQuery = scene-&gt;CreateQuery&lt;CustomQuery&gt;();\n</code></pre> <p>See the Query reference for the full fluent query API.</p>"},{"location":"api/scene/#event-helpers","title":"Event helpers","text":""},{"location":"api/scene/#addeventlistenert","title":"<code>AddEventListener&lt;T&gt;</code>","text":"<p>Subscribes to event type <code>T</code>.</p> <p>Signature: <pre><code>template &lt;typename T&gt;\n    requires IsEvent&lt;T&gt;\nRef&lt;fr::ListenerHandle&gt; AddEventListener(auto&amp;&amp; listener);\n</code></pre></p> <p>Complexity: $O(1)$ amortised \u2014 adds to pending listener queue.</p> <p>Thread safety: Thread-safe \u2014 listeners are queued and merged at flush time.</p> <pre><code>mHandle = scene-&gt;AddEventListener&lt;CollisionEvent&gt;(\n    [](const CollisionEvent&amp; ev) { /* ... */ });\n</code></pre>"},{"location":"api/scene/#sendeventt","title":"<code>SendEvent&lt;T&gt;</code>","text":"<p>Publishes an event immediately to all active subscribers.</p> <p>Signature: <pre><code>template &lt;typename T&gt;\n    requires IsEvent&lt;T&gt;\nvoid SendEvent(T event);\n</code></pre></p> <p>Complexity: $O(N)$ where N is the number of active listeners.</p> <p>Thread safety: Fully thread-safe.</p> <pre><code>scene-&gt;SendEvent(CollisionEvent { .entityA = a, .entityB = b });\n</code></pre>"},{"location":"api/scene/#update-loop","title":"Update loop","text":""},{"location":"api/scene/#updatedeltatime","title":"<code>Update(deltaTime)</code>","text":"<p>Drives the full update cycle.</p> <p>Signature: <code>void Update(float deltaTime)</code></p> <p>Complexity: Depends on registered systems and entity count.</p> <p>Thread safety: Call from a single thread (typically the main game loop).</p> <pre><code>class MyApp : public skr::IApplication {\n    void Run() override {\n        while (running) {\n            float dt = clock.restart();\n            mScene-&gt;Update(dt);\n        }\n    }\n};\n</code></pre>"},{"location":"api/scene/#update-sequence","title":"Update sequence","text":"<pre><code>graph TB\n    START([\"Scene::Update(dt)\"])\n    FLUSH[\"1. EventManager::Flush()&lt;br/&gt;Merge pending listeners&lt;br/&gt;Clean expired handles\"]\n    WORKERS[\"2. ThreadPool::StartWorkers()\"]\n    ACCUM[\"3. SystemManager::Accumulate(dt)&lt;br/&gt;Track elapsed time per pipeline\"]\n    PRE[\"4. PreUpdate phase&lt;br/&gt;Systems::PreUpdate(dt)\"]\n    PRE_WAIT[\"WaitForAllTasks() + DestroyEntities()\"]\n    UPD[\"5. Update phase&lt;br/&gt;Systems::Update(dt) \u2190 Queries here\"]\n    UPD_WAIT[\"WaitForAllTasks() + DestroyEntities()\"]\n    POST[\"6. PostUpdate phase&lt;br/&gt;Systems::PostUpdate(dt)\"]\n    POST_WAIT[\"WaitForAllTasks() + DestroyEntities()\"]\n    FINISH([\"End\"])\n\n    START --&gt; FLUSH --&gt; WORKERS --&gt; ACCUM --&gt; PRE --&gt; PRE_WAIT --&gt; UPD --&gt; UPD_WAIT --&gt; POST --&gt; POST_WAIT --&gt; FINISH</code></pre>"},{"location":"api/scene/#executetasks","title":"<code>ExecuteTasks()</code>","text":"<p>Starts all workers, flushes the query aggregator, and waits for all enqueued chunk tasks to complete.</p> <p>Signature: <code>void ExecuteTasks()</code></p> <p>Complexity: $O(T)$ where T is the time to complete all pending tasks.</p> <pre><code>// Schedule async work\nscene-&gt;CreateQuery()-&gt;EachAsync&lt;Position, Velocity&gt;(fn);\n\n// Do other work...\n\n// Sync\nscene-&gt;ExecuteTasks();\n</code></pre>"},{"location":"api/scene/#archetype-builder","title":"Archetype builder","text":""},{"location":"api/scene/#createarchetypebuilder","title":"<code>CreateArchetypeBuilder</code>","text":"<p>Returns an <code>ArchetypeBuilder</code> for efficient bulk entity creation.</p> <p>Signature: <code>ArchetypeBuilder CreateArchetypeBuilder() const</code></p> <pre><code>scene-&gt;CreateArchetypeBuilder()\n    .WithComponent(Position { .x = 0.f, .y = 0.f })\n    .WithComponent(Velocity { .dx = 1.f })\n    .WithEntities(100'000)\n    .Build();\n</code></pre> <p>See the ArchetypeBuilder reference.</p>"},{"location":"api/scene/#profiling","title":"Profiling","text":"<pre><code>void BeginProfiling();\nvoid EndProfiling() const;   // flushes trace file to disk\nvoid BeginTrace(const char* label);\nvoid EndTrace();\n</code></pre> <p>See the Profiling guide.</p>"},{"location":"api/systems/","title":"Systems","text":"<p>Systems contain the logic of the simulation. Each system declares which components it needs and processes all matching entities during the update loop.</p>"},{"location":"api/systems/#defining-a-system","title":"Defining a system","text":"<p>Inherit from <code>fr::System</code> and override the lifecycle hooks you need:</p> <pre><code>#include &lt;Freyr/Freyr.hpp&gt;\n\nclass MovementSystem : public fr::System {\npublic:\n    explicit MovementSystem(const Ref&lt;fr::Scene&gt;&amp; scene) : System(scene) {}\n\n    void Update(float deltaTime) override {\n        mScene-&gt;CreateQuery()-&gt;EachAsync&lt;Position, Velocity&gt;(\n            [deltaTime](fr::Entity, Position&amp; pos, const Velocity&amp; vel) {\n                pos.x += vel.dx * deltaTime;\n                pos.y += vel.dy * deltaTime;\n                pos.z += vel.dz * deltaTime;\n            });\n    }\n};\n</code></pre> <p>The constructor must accept <code>const Ref&lt;fr::Scene&gt;&amp;</code> as its first argument. Additional dependencies are resolved by Skirnir's DI container.</p>"},{"location":"api/systems/#lifecycle-hooks","title":"Lifecycle hooks","text":"<pre><code>graph TB\n    subgraph Frame[\"Single Frame Update\"]\n        subgraph PreUpdatePhase[\"PreUpdate Phase\"]\n            PU[\"System::PreUpdate(dt)\"]\n            PU_WAIT[\"WaitForAllTasks()\"]\n            PU_DESTROY[\"DestroyEntities()\"]\n        end\n\n        subgraph UpdatePhase[\"Update Phase\"]\n            U[\"System::Update(dt)\"]\n            U_QUERY[\"\u2190 Query::Each / EachAsync\"]\n            U_WAIT[\"WaitForAllTasks()\"]\n            U_DESTROY[\"DestroyEntities()\"]\n        end\n\n        subgraph PostUpdatePhase[\"PostUpdate Phase\"]\n            POU[\"System::PostUpdate(dt)\"]\n            POU_WAIT[\"WaitForAllTasks()\"]\n            POU_DESTROY[\"DestroyEntities()\"]\n        end\n\n        PU --&gt; PU_WAIT --&gt; PU_DESTROY --&gt; U --&gt; U_QUERY --&gt; U_WAIT --&gt; U_DESTROY --&gt; POU --&gt; POU_WAIT --&gt; POU_DESTROY\n    end\n</code></pre> <p>Systems are called in this order every frame by <code>Scene::Update(dt)</code>:</p> <pre><code>PreUpdate(dt) \u2192 Update(dt) \u2192 PostUpdate(dt)\n</code></pre> Hook Typical use Notes <code>PreUpdate(dt)</code> Input gathering, reset accumulators Runs before main work <code>Update(dt)</code> Main simulation logic Primary hook \u2014 most systems use this <code>PostUpdate(dt)</code> Post-processing, late reads Runs after all Update work <p>All hooks have a default empty implementation \u2014 override only those you need.</p>"},{"location":"api/systems/#dependency-injection","title":"Dependency injection","text":"<p>Systems are singletons registered in Skirnir's DI container. Any service registered in the container can be injected via the constructor:</p> <pre><code>class CollisionSystem : public fr::System {\npublic:\n    CollisionSystem(const Ref&lt;fr::Scene&gt;&amp; scene,           // required: first parameter\n                    Ref&lt;fr::EventManager&gt; events)          // injected automatically\n        : System(scene), mEvents(events) {}\n\n    void Update(float dt) override {\n        mScene-&gt;CreateQuery()-&gt;EachAsync&lt;Position, Collider&gt;(\n            [this](fr::Entity a, Position&amp; posA, Collider&amp; colA) {\n                // detect collisions and publish events\n                mScene-&gt;SendEvent(CollisionEvent { .entityA = a });\n            });\n    }\n\nprivate:\n    Ref&lt;fr::EventManager&gt; mEvents;\n};\n</code></pre>"},{"location":"api/systems/#injectable-types","title":"Injectable types","text":"<p>Any service registered via Skirnir can be injected:</p> <ul> <li><code>Ref&lt;fr::Scene&gt;</code> \u2014 the scene (always available)</li> <li><code>Ref&lt;fr::EventManager&gt;</code> \u2014 event bus</li> <li><code>Ref&lt;fr::ComponentManager&gt;</code> \u2014 component registry</li> <li>Custom services registered in your application</li> </ul>"},{"location":"api/systems/#registration","title":"Registration","text":"<p>Register systems within a pipeline using <code>FreyrExtension::WithPipeline()</code>:</p> <pre><code>freyr\n    .WithPipeline([](fr::PipelineBuilder&amp; pipeline) {\n        pipeline.WithName(\"Main\")\n            .WithRate(60.0f)\n            .WithSystem&lt;InputSystem&gt;()      // registered first, runs first\n            .WithSystem&lt;MovementSystem&gt;()\n            .WithSystem&lt;CollisionSystem&gt;()\n            .WithSystem&lt;RenderSystem&gt;();    // registered last, runs last\n    });\n</code></pre> <p>Systems are instantiated and wired in registration order. Multiple pipelines can be defined with different rates.</p>"},{"location":"api/systems/#what-registration-does","title":"What registration does","text":"<p><code>WithSystem&lt;T&gt;()</code> performs two steps:</p> <ol> <li>Registers a factory in <code>SystemManager</code>: <code>mSystemFactories[GetSystemId&lt;T&gt;()] = fn</code></li> <li>Registers a singleton in Skirnir's DI container: <code>services.AddSingleton&lt;T&gt;()</code></li> </ol> <p>This means the first pipeline that declares a system \"owns\" it. If a system appears in multiple pipelines, it is constructed once and shared.</p>"},{"location":"api/systems/#system-id","title":"System ID","text":"<p>Each system type has a unique runtime ID, assigned at first use:</p> <pre><code>fr::SystemId id = fr::GetSystemId&lt;MovementSystem&gt;();\n</code></pre> <p>IDs are assigned sequentially in declaration order across translation units.</p>"},{"location":"api/systems/#accessing-the-scene","title":"Accessing the scene","text":"<p>The protected <code>mScene</code> member provides access to all <code>Scene</code> operations:</p> <pre><code>class HealthSystem : public fr::System {\npublic:\n    explicit HealthSystem(const Ref&lt;fr::Scene&gt;&amp; scene) : System(scene) {}\n\n    void Update(float dt) override {\n        // Query entities\n        mScene-&gt;CreateQuery()-&gt;Each&lt;Health&gt;([](fr::Entity e, Health&amp; hp) {\n            hp.current = std::min(hp.current + hp.regen * dt, hp.max);\n        });\n\n        // Destroy entities\n        mScene-&gt;CreateQuery()-&gt;Each&lt;Health&gt;([this](fr::Entity e, Health&amp; hp) {\n            if (hp.current &lt;= 0)\n                mScene-&gt;DestroyEntity(e);\n        });\n\n        // Add/remove components\n        mScene-&gt;CreateQuery()-&gt;Each&lt;Health&gt;([this](fr::Entity e, Health&amp; hp) {\n            if (hp.isPoisoned)\n                mScene-&gt;RemoveComponent&lt;PoisonedTag&gt;(e);\n        });\n\n        // Send events\n        mScene-&gt;SendEvent(HealthCheckEvent {});\n    }\n};\n</code></pre>"},{"location":"api/systems/#system-execution-patterns","title":"System execution patterns","text":""},{"location":"api/systems/#1-parallel-processing-preferred","title":"1. Parallel processing (preferred)","text":"<pre><code>void Update(float dt) override {\n    mScene-&gt;CreateQuery()-&gt;EachAsync&lt;Position, Velocity&gt;(\n        [dt](fr::Entity, Position&amp; pos, Velocity&amp; vel) {\n            pos.x += vel.dx * dt;\n        });\n}\n</code></pre> <p>Use when entities are independent. Chunks are distributed across all worker threads.</p>"},{"location":"api/systems/#2-sequential-processing","title":"2. Sequential processing","text":"<pre><code>void Update(float dt) override {\n    mScene-&gt;CreateQuery()-&gt;Each&lt;Position, Velocity&gt;(\n        [dt](fr::Entity e1, Position&amp; pos1, Velocity&amp; vel1) {\n            // safe to read/write other entities\n            mScene-&gt;CreateQuery()-&gt;Each&lt;Position&gt;(\n                [&amp;](fr::Entity e2, Position&amp; pos2) {\n                    // compute interaction between e1 and e2\n                });\n        });\n}\n</code></pre> <p>Use when entities interact. Runs on the calling thread.</p>"},{"location":"api/systems/#3-mixed-parallelism","title":"3. Mixed parallelism","text":"<pre><code>void Update(float dt) override {\n    // Parallel: movement is independent per entity\n    mScene-&gt;CreateQuery()-&gt;EachAsync&lt;Position, Velocity&gt;(\n        [dt](fr::Entity e, Position&amp; p, Velocity&amp; v) {\n            p.x += v.dx * dt;\n        });\n\n    // Sequential: AI may read other entities\n    mScene-&gt;CreateQuery()-&gt;Each&lt;AIState&gt;(\n        [this](fr::Entity e, AIState&amp; ai) {\n            ai.think(mScene);\n        });\n\n    // Sync parallel work\n    mScene-&gt;ExecuteTasks();\n}\n</code></pre>"},{"location":"api/systems/#ordering-and-dependencies","title":"Ordering and dependencies","text":"<p>Systems run in registration order, sequentially per pipeline phase. If <code>SystemB</code> depends on results produced by <code>SystemA</code>, register <code>SystemA</code> first:</p> <pre><code>pipeline\n    .WithSystem&lt;PhysicsSystem&gt;()     // computes positions\n    .WithSystem&lt;CollisionSystem&gt;()   // reads positions\n    .WithSystem&lt;RenderSystem&gt;();     // reads everything\n</code></pre> <p>Pipeline execution is sequential</p> <p>While systems within a pipeline run sequentially, multiple pipelines accumulate time independently. Two pipelines at different rates may interleave their execution across frames.</p>"},{"location":"concepts/architecture/","title":"Architecture","text":""},{"location":"concepts/architecture/#high-level-overview","title":"High-level overview","text":"<pre><code>graph TB\n    subgraph Build[\"Build Phase\"]\n        FB[\"skr::ApplicationBuilder\"]\n        FE[\"FreyrExtension\"]\n        OPT[\"FreyrOptions&lt;br/&gt;MaxEntities, ThreadCount,&lt;br/&gt;ChunkCapacity\"]\n    end\n\n    subgraph Bootstrap[\"Bootstrap\"]\n        SC[\"Scene&lt;br/&gt;Central orchestrator\"]\n        CM[\"ComponentManager&lt;br/&gt;Archetype routing\"]\n        EM[\"EntityManager&lt;br/&gt;ID allocation &amp;amp; recycling\"]\n        SM[\"SystemManager&lt;br/&gt;Pipeline scheduling\"]\n        EVM[\"EventManager&lt;br/&gt;Pub/sub bus\"]\n        TP[\"ThreadPool&lt;br/&gt;Work stealing pool\"]\n    end\n\n    subgraph Storage[\"Data Layer\"]\n        ARCH[\"Archetype[]&lt;br/&gt;Grouped by component signature\"]\n        CHUNK[\"ArchetypeChunk[]&lt;br/&gt;Fixed-size storage units\"]\n    end\n\n    subgraph Execution[\"Execution\"]\n        WORKERS[\"Worker Threads\"]\n        QUERY[\"Query / QueryAggregator&lt;br/&gt;Filter &amp;amp; dispatch\"]\n    end\n\n    FB --&gt;|AddExtension| FE\n    FE --&gt;|registers components| CM\n    FE --&gt;|registers systems| SM\n    FE --&gt;|configures| OPT\n\n    FB --&gt;|Build| SC\n    SC --&gt; CM\n    SC --&gt; EM\n    SC --&gt; SM\n    SC --&gt; EVM\n    SC --&gt; TP\n\n    CM --&gt; ARCH\n    ARCH --&gt; CHUNK\n\n    SM --&gt; QUERY\n    QUERY --&gt; CM\n    QUERY --&gt;|enqueue chunk tasks| TP\n    TP --&gt;|distribute| WORKERS\n    WORKERS --&gt;|process| CHUNK\n</code></pre>"},{"location":"concepts/architecture/#scene-the-central-orchestrator","title":"Scene \u2014 the central orchestrator","text":"<p><code>Scene</code> owns all managers and drives the update loop. All entity and component operations flow through <code>Scene</code>, which delegates to the appropriate manager.</p> <pre><code>graph TB\n    subgraph SceneInternals[\"Internal Structure of Scene\"]\n        SC[\"Scene\"]\n\n        subgraph Managers[\"Managers\"]\n            direction TB\n            CM[\"ComponentManager&lt;br/&gt;- Archetype list&lt;br/&gt;- Entity\u2192Archetype map&lt;br/&gt;- Component registration\"]\n            EM[\"EntityManager&lt;br/&gt;- Entity generation&lt;br/&gt;- MPMC free list\"]\n            SM[\"SystemManager&lt;br/&gt;- Pipeline list&lt;br/&gt;- System factory map\"]\n            EVM[\"EventManager&lt;br/&gt;- Publisher map&lt;br/&gt;- Pending listener queues\"]\n        end\n\n        subgraph Exec[\"Execution\"]\n            TP[\"ThreadPool&lt;br/&gt;- Worker threads&lt;br/&gt;- Per-worker MPMC queues\"]\n            QA[\"QueryAggregator&lt;br/&gt;- Pending query batch\"]\n        end\n\n        subgraph Data[\"Deferred Data\"]\n            DT[\"mEntitiesToDestroy&lt;br/&gt;(SparseSet&amp;lt;Entity&amp;gt;)\"]\n        end\n\n        SC --&gt; CM\n        SC --&gt; EM\n        SC --&gt; SM\n        SC --&gt; EVM\n        SC --&gt; TP\n        SC --&gt; QA\n        SC --&gt; DT\n    end\n</code></pre>"},{"location":"concepts/architecture/#update-loop-in-detail","title":"Update loop in detail","text":"<pre><code>Scene::Update(dt)\n\u2502\n\u251c\u2500 1. EventManager::Flush()\n\u2502      Merge pending subscribers into active lists\n\u2502      Clear expired listener handles\n\u2502\n\u251c\u2500 2. ThreadPool::StartWorkers()\n\u2502      Signal workers to begin pulling tasks\n\u2502\n\u251c\u2500 3. SystemManager::Accumulate(dt)\n\u2502      For each Pipeline:\n\u2502        accumulator += dt\n\u2502        if accumulator &gt;= rate_interval \u2192 mark pipeline ready\n\u2502\n\u251c\u2500 4. SystemManager::PreUpdate(dt)\n\u2502      For each ready pipeline:\n\u2502        For each system in pipeline:\n\u2502          system-&gt;PreUpdate(dt)\n\u2502      ThreadPool::WaitForAllTasks()\n\u2502      Scene::DestroyEntities()\n\u2502\n\u251c\u2500 5. SystemManager::Update(dt)          \u2190 Main work happens here\n\u2502      For each ready pipeline:\n\u2502        For each system in pipeline:\n\u2502          system-&gt;Update(dt)  \u2190 systems call Query::Each/EachAsync\n\u2502      ThreadPool::WaitForAllTasks()\n\u2502      Scene::DestroyEntities()\n\u2502\n\u2514\u2500 6. SystemManager::PostUpdate(dt)\n       For each ready pipeline:\n         For each system in pipeline:\n           system-&gt;PostUpdate(dt)\n       ThreadPool::WaitForAllTasks()\n       Scene::DestroyEntities()\n</code></pre>"},{"location":"concepts/architecture/#componentmanager-archetype-routing","title":"ComponentManager \u2014 archetype routing","text":"<p>The <code>ComponentManager</code> maintains:</p> <ul> <li>A flat list of <code>Archetype</code> shared pointers</li> <li>A <code>std::vector&lt;EntityIndex&gt;</code> mapping each entity ID to its current <code>(Archetype*, ArchetypeChunk*)</code></li> </ul> <p>When a component is added or removed, <code>ComponentManager</code>:</p> <ol> <li>Computes the new component signature</li> <li>Searches existing archetypes for a matching signature</li> <li>If found \u2192 migrate entity to existing archetype</li> <li>If not found \u2192 create new archetype, extend the archetype list</li> <li>Returns the new <code>(archetype, chunk)</code> pair</li> </ol>"},{"location":"concepts/architecture/#archetype-migration","title":"Archetype migration","text":"<pre><code>graph LR\n    subgraph Before[\"Before: AddComponent&amp;lt;Health&amp;gt; to Entity 5\"]\n        A1[\"Archetype [Position, Velocity]\"]\n        A1 --&gt; C1[\"Chunk: Ent 0, Ent 1, Ent 5, Ent 2\"]\n    end\n\n    subgraph After[\"After\"]\n        A1_b[\"Archetype [Position, Velocity]\"]\n        A2[\"Archetype [Position, Velocity, Health]\"]\n        A1_b --&gt; C1_b[\"Chunk: Ent 0, Ent 1, Ent 2\"]\n        A2 --&gt; C2[\"Chunk: Ent 5, ...\"]\n    end\n\n    Before --&gt;|\"ComponentManager&lt;br/&gt;moves entity 5\"| After</code></pre> <p>Data is copied, not moved</p> <p>During migration, component data is copied from the source chunk to the target chunk using <code>ComponentArray&lt;T&gt;::CopyComponent</code>. Components should be cheap to copy (prefer POD types).</p>"},{"location":"concepts/architecture/#entitymanager-id-allocation","title":"EntityManager \u2014 ID allocation","text":"<p>The <code>EntityManager</code> uses:</p> <ul> <li>A <code>rigtorp::MPMCQueue&lt;Entity&gt;</code> (lock-free multi-producer/multi-consumer queue) for recycled IDs</li> <li>An <code>std::atomic&lt;Entity&gt;</code> counter for new entity generation</li> </ul> <p>When <code>CreateEntity()</code> is called:</p> <ol> <li>Try to pop from the free list (MPMC queue) \u2192 fast path for recycled IDs</li> <li>If empty, atomically increment the living count \u2192 new sequential ID</li> </ol> <p>When <code>DestroyEntity()</code> is called:</p> <ol> <li>Push the ID back onto the free list for immediate reuse</li> </ol> <pre><code>Entity CreateEntity() {\n    if (Entity entity; mAvailableEntities.try_pop(entity))\n        return entity;                // recycled ID\n    return mLivingEntityCount++;      // new ID\n}\n\nvoid DestroyEntity(Entity entity) {\n    mAvailableEntities.try_push(entity);  // return to pool\n}\n</code></pre>"},{"location":"concepts/architecture/#systemmanager-pipeline-scheduling","title":"SystemManager \u2014 pipeline scheduling","text":"<p>The <code>SystemManager</code> holds:</p> <ul> <li>A <code>std::vector&lt;Pipeline&gt;</code> \u2014 each pipeline has a name, rate, accumulator, and list of system IDs</li> <li>A <code>SparseSet&lt;SystemId&gt;</code> of registered systems</li> <li>A <code>std::vector&lt;skr::ServiceFactory&gt;</code> \u2014 factory functions for lazy system construction</li> </ul>"},{"location":"concepts/architecture/#pipeline-timing","title":"Pipeline timing","text":"<pre><code>struct Pipeline {\n    std::string_view Name;\n    float            Rate;           // update interval in seconds\n    float            Accumulator;    // elapsed time since last execution\n    std::vector&lt;SystemId&gt; Systems;\n};\n</code></pre> <p>Pipelines track their own elapsed time. A pipeline with <code>WithRate(60.0f)</code> has <code>Rate = 1/60 \u2248 0.0167s</code>. The accumulator is incremented each frame by <code>dt</code>. When <code>accumulator &gt;= Rate</code>, the pipeline executes.</p> <pre><code>timeline\n    title Pipeline Execution Over Frames\n    Frame 1 : dt = 16ms : accumulator = 0 \u2192 16 : Physics pipe executes\n    Frame 2 : dt = 8ms  : accumulator = 0 \u2192 8  : doesn't execute\n    Frame 3 : dt = 12ms : accumulator = 8 \u2192 20 : Physics pipe executes again</code></pre>"},{"location":"concepts/architecture/#eventmanager-pubsub-bus","title":"EventManager \u2014 pub/sub bus","text":"<p>The <code>EventManager</code> is fully thread-safe:</p> <ul> <li><code>Publisher&lt;T&gt;</code> instances per event type, indexed by <code>EventId</code></li> <li>Pending listener queue \u2014 subscribers added during <code>Publish()</code> are queued and merged before the next flush</li> <li>Expired handle cleanup \u2014 listeners with destroyed handles are removed during <code>Flush()</code></li> </ul> <pre><code>graph TB\n    subgraph EventSystem[\"Event Manager Internals\"]\n        EVM[\"EventManager\"]\n        EVM --&gt; P1[\"Publisher&amp;lt;CollisionEvent&amp;gt;\"]\n        EVM --&gt; P2[\"Publisher&amp;lt;DamageEvent&amp;gt;\"]\n        EVM --&gt; P3[\"Publisher&amp;lt;HealEvent&amp;gt;\"]\n\n        subgraph Pub1[\"Publisher&amp;lt;T&amp;gt;\"]\n            direction LR\n            ACTIVE[\"Active Listeners&lt;br/&gt;(vector)\"]\n            PENDING[\"Pending Listeners&lt;br/&gt;(vector)\"]\n            LOCK[\"RwLock\"]\n        end\n\n        P1 --&gt; Pub1\n    end\n\n    S1[\"System A&lt;br/&gt;subscribes\"] --&gt;|Subscribe| PENDING\n    S2[\"System B&lt;br/&gt;publishes\"] --&gt;|Publish| ACTIVE\n    ACTIVE --&gt;|Flush| CLEANUP[\"Clear expired&lt;br/&gt;Merge pending\"]</code></pre>"},{"location":"concepts/architecture/#threadpool-work-stealing","title":"ThreadPool \u2014 work stealing","text":"<p>The <code>ThreadPool</code> uses:</p> <ul> <li>One <code>rigtorp::MPMCQueue&lt;Task&gt;</code> per worker \u2014 MPMC queues allow any thread to push, any thread to pop</li> <li>Work stealing via LCG hashing \u2014 <code>AddTask</code> distributes tasks across queues using a linear congruential generator</li> <li><code>TaskCounter</code> \u2014 atomic counter tracking pending tasks for synchronisation</li> </ul> <pre><code>void AddTask(auto&amp;&amp; func) {\n    mTaskCounter-&gt;AddTasks(1);\n    mQueueLcgState = mQueueLcgState * LCG_MULTIPLIER + LCG_INCREMENT;\n    const auto nextQueue = mQueueLcgState % mWorkerQueues.size();\n    mWorkerQueues[nextQueue]-&gt;push(std::forward&lt;decltype(func)&gt;(func));\n}\n</code></pre> <p>When a worker finishes its queue, it tries to pop from other workers' queues \u2014 this is work stealing.</p>"},{"location":"concepts/architecture/#key-design-decisions","title":"Key design decisions","text":"Decision Rationale Archetype-based storage Maximises cache locality \u2014 entities with same components are stored together Fixed-size chunks Enables uniform task granularity for parallel dispatch Per-worker MPMC queues Minimises contention \u2014 producers hash to different queues Deferred entity destruction Prevents iterator invalidation during iteration RwLock on archetypes Allows concurrent reads (multiple queries) with exclusive writes (migration) Skirnir DI integration Systems can inject any dependency via constructor"},{"location":"concepts/ecs-overview/","title":"ECS Overview","text":""},{"location":"concepts/ecs-overview/#what-is-ecs","title":"What is ECS?","text":"<p>Entity-Component-System (ECS) is a software architectural pattern popular in real-time simulations and games. It follows the principle of composition over inheritance: objects in the world are not defined by class hierarchies, but by the data attached to them.</p> Concept Role Entity A lightweight identifier (integer ID) representing a world object Component A plain data structure holding one aspect of an entity's state System Logic that processes all entities matching a specific set of components"},{"location":"concepts/ecs-overview/#entities-are-not-objects","title":"Entities are not objects","text":"<p>In traditional OOP, a \"Player\" is an object with data and methods. In ECS, a \"Player\" is just an integer with a <code>PlayerTag</code> component attached. The entity is the sum of its components \u2014 nothing more.</p> <pre><code>// OOP: Entity is a class with state and behaviour\nclass Player : public GameObject {\n    void Update(float dt) override;\n    float hp, maxHp;\n    float x, y;\n};\n\n// ECS: Entity is an ID, data lives in components\nstruct Health : fr::Component { float current, max; };\nstruct Position : fr::Component { float x, y; };\n\n// Player = Entity 42 + Health + Position + PlayerTag\nauto player = scene-&gt;CreateEntity(\n    Health  { .current = 100, .max = 100 },\n    Position { .x = 0, .y = 0 },\n    PlayerTag {}\n);\n</code></pre>"},{"location":"concepts/ecs-overview/#why-ecs","title":"Why ECS?","text":""},{"location":"concepts/ecs-overview/#traditional-oop-problems","title":"Traditional OOP problems","text":"<p>In a typical game, a <code>Character</code> class might inherit from <code>PhysicsObject</code>, which inherits from <code>GameObject</code>. Adding a new behaviour (e.g. swimming) requires modifying the hierarchy or adding awkward mixins. Data and behaviour are tightly coupled, and related data is scattered across unrelated memory addresses.</p> <pre><code>graph TD\n    subgraph OOP[\"Traditional OOP Inheritance\"]\n        GO[\"GameObject\"] --&gt; PO[\"PhysicsObject\"]\n        PO --&gt; Char[\"Character\"]\n        PO --&gt; Enemy[\"Enemy\"]\n        Char --&gt; Mage[\"Mage\"]\n        Char --&gt; Warrior[\"Warrior\"]\n    end\n\n    subgraph ECS[\"ECS Composition\"]\n        E1[\"Entity 1\"] --&gt; P1[\"Position\"]\n        E1 --&gt; V1[\"Velocity\"]\n        E1 --&gt; H1[\"Health\"]\n        E1 --&gt; PT1[\"PlayerTag\"]\n\n        E2[\"Entity 2\"] --&gt; P2[\"Position\"]\n        E2 --&gt; V2[\"Velocity\"]\n        E2 --&gt; AT2[\"AITag\"]\n    end</code></pre>"},{"location":"concepts/ecs-overview/#ecs-advantages","title":"ECS advantages","text":"<p>Data-Oriented Design : Components are stored contiguously in memory. When a system processes 1,000,000 positions, it reads a packed array \u2014 no pointer chasing, maximum cache efficiency.</p> <p>Parallel-friendly : Systems that operate on different component sets have no shared state by default. They can run in parallel with no synchronisation. Within a single system, individual entities have no dependencies, so chunk-level parallelism is safe.</p> <p>Dynamic behaviour : Adding or removing a component at runtime changes which systems process that entity \u2014 without recompilation or virtual dispatch.</p> <pre><code>// An enemy becomes paralyzed \u2014 just remove its MovementTag\nscene-&gt;RemoveComponent&lt;MovementTag&gt;(enemy);\n\n// Now MovementSystem skips it automatically\n// Query::Each&lt;Position, Velocity, MovementTag&gt; will not include it\n</code></pre>"},{"location":"concepts/ecs-overview/#cache-performance-comparison","title":"Cache performance comparison","text":"<pre><code>graph LR\n    subgraph OOPMem[\"OOP Memory Layout\"]\n        O1[\"Player 1&lt;br/&gt;HP | Pos | Vel |...\"]\n        O2[\"Player 2&lt;br/&gt;HP | Pos | Vel |...\"]\n        O3[\"Enemy 1&lt;br/&gt;HP | Pos | AI |...\"]\n    end\n\n    subgraph ECSMem[\"ECS Memory Layout\"]\n        subgraph HealthArr[\"ComponentArray&amp;lt;Health&amp;gt;\"]\n            HA[\"H1 | H2 | H3 | H4 | ...\"]\n        end\n        subgraph PosArr[\"ComponentArray&amp;lt;Position&amp;gt;\"]\n            PA[\"P1 | P2 | P3 | P4 | ...\"]\n        end\n        subgraph VelArr[\"ComponentArray&amp;lt;Velocity&amp;gt;\"]\n            VA[\"V1 | V2 | V3 | V4 | ...\"]\n        end\n    end</code></pre> <p>When <code>MovementSystem</code> iterates all positions:</p> <ul> <li>OOP: Pointer chase from <code>GameObject*</code> \u2192 <code>Transform*</code> \u2192 <code>x, y</code> \u2014 each entity might be anywhere in memory</li> <li>ECS: Linear scan of <code>ComponentArray&lt;Position&gt;</code> \u2014 all positions are adjacent, entire array fits in cache</li> </ul>"},{"location":"concepts/ecs-overview/#pros-and-cons","title":"Pros and cons","text":""},{"location":"concepts/ecs-overview/#pros","title":"Pros","text":"<ul> <li>Contiguous data layout \u2192 fewer cache misses \u2192 higher throughput</li> <li>Natural fit for multi-core processing</li> <li>Entities change behaviour by gaining/losing components, not by subclassing</li> <li>Scales to millions of entities</li> <li>Systems are reusable across different entity types</li> </ul>"},{"location":"concepts/ecs-overview/#cons","title":"Cons","text":"<ul> <li>Higher initial complexity than OOP</li> <li>Overkill for small numbers of unique, one-off objects</li> <li>Requires careful component design to avoid data duplication</li> <li>Can be harder to debug (no clear \"object\" to inspect)</li> </ul>"},{"location":"concepts/ecs-overview/#ecs-in-freyr","title":"ECS in Freyr","text":"<p>Freyr extends the basic ECS model with archetypes \u2014 groups of entities that share the same set of component types. All entities in an archetype are stored together in fixed-size chunks.</p> <pre><code>Archetype [Position, Velocity]\n\u251c\u2500\u2500 Chunk 0   \u2192 entities 0\u2013511\n\u251c\u2500\u2500 Chunk 1   \u2192 entities 512\u20131023\n\u2514\u2500\u2500 Chunk 2   \u2192 entities 1024\u20131535\n\nArchetype [Position, Health]\n\u2514\u2500\u2500 Chunk 0   \u2192 entities 1536\u20132047\n</code></pre> <p>When you call <code>Query::EachAsync&lt;Position, Velocity&gt;</code>, Freyr:</p> <ol> <li>Finds all archetypes whose signature contains <code>Position</code> and <code>Velocity</code></li> <li>Enqueues each matching chunk as an independent task</li> <li>Worker threads pull and process chunks concurrently</li> </ol> <p>This design means: - Cache locality \u2014 all positions in a chunk are contiguous - Zero false sharing \u2014 each chunk belongs to exactly one task at a time - Scalable \u2014 more threads = more chunks processed in parallel</p>"},{"location":"concepts/ecs-overview/#entity-identity","title":"Entity identity","text":"<p>An entity is an unsigned integer (<code>fr::Entity</code>, <code>uint32_t</code>). It has no data of its own \u2014 its identity is the union of its components.</p> <pre><code>using fr::Entity = std::uint32_t;\n</code></pre> <p>ID stability</p> <p>An entity's internal ID may change when components are added or removed, as the entity migrates to a different archetype. Do not persist raw entity IDs across frames if the entity's component set changes, unless you use a stable mapping layer.</p> <p>Entity recycling</p> <p>Destroyed entity IDs are recycled via a lock-free MPMC queue. The next <code>CreateEntity</code> call reuses a freed slot immediately, keeping memory usage bounded by <code>MaxEntities</code>.</p>"},{"location":"concepts/ecs-overview/#when-to-use-ecs","title":"When to use ECS","text":"<p>Freyr is ideal for:</p> <ul> <li>Large-scale simulations: 100k+ entities with shared behaviour</li> <li>Games with many actor types: different combinations of components define different behaviours</li> <li>Data-heavy systems: physics, particle systems, AI crowd simulation</li> <li>Multi-core workloads: automatic chunk-level parallelism</li> </ul> <p>Freyr is less suited for:</p> <ul> <li>Simple applications with few unique objects</li> <li>UI-centric applications</li> <li>Situations where entities have wildly different update schedules</li> </ul>"},{"location":"concepts/memory-layout/","title":"Memory Layout","text":"<p>Understanding Freyr's memory model is key to writing high-performance systems. This page explains how components, archetypes, and chunks are arranged in memory, and why it matters for cache performance.</p>"},{"location":"concepts/memory-layout/#the-problem-random-memory-access","title":"The problem: random memory access","text":"<p>In a traditional OOP game engine, objects are scattered across the heap:</p> <pre><code>class GameObject {\n    uint64_t id;\n    Transform* transform;    // allocated separately\n    Health*    health;       // allocated separately\n    Renderable* renderable;  // allocated separately\n};\n</code></pre> <p>Iterating all objects means following pointers to unrelated memory locations \u2014 each access is a potential cache miss. On modern CPUs, a cache miss costs ~100 cycles while a cache hit costs ~4 cycles.</p>"},{"location":"concepts/memory-layout/#freyrs-solution-archetype-chunks","title":"Freyr's solution: archetype chunks","text":"<p>Freyr groups entities by their component signature into archetypes. Each archetype divides storage into fixed-size chunks. Inside each chunk, component data is stored in contiguous arrays:</p> <pre><code>graph TB\n    subgraph Archetype[\"Archetype [Position, Velocity]\"]\n        direction TB\n        C0[\"Chunk 0&lt;br/&gt;&lt;small&gt;Capacity: 512 entities&lt;/small&gt;\"]\n        C1[\"Chunk 1&lt;br/&gt;&lt;small&gt;Capacity: 512 entities&lt;/small&gt;\"]\n        C2[\"Chunk 2&lt;br/&gt;&lt;small&gt;Capacity: 512 entities&lt;/small&gt;\"]\n    end\n\n    subgraph ChunkDetail[\"Inside Chunk 0\"]\n        direction TB\n        POS[\"ComponentArray&amp;lt;Position&amp;gt;&lt;br/&gt;[p0, p1, p2, ..., p511]&lt;br/&gt;\u2190 512 \u00d7 sizeof(Position) contiguous\"]\n        VEL[\"ComponentArray&amp;lt;Velocity&amp;gt;&lt;br/&gt;[v0, v1, v2, ..., v511]&lt;br/&gt;\u2190 512 \u00d7 sizeof(Velocity) contiguous\"]\n        ENT[\"SparseSet&amp;lt;Entity&amp;gt;&lt;br/&gt;[e0, e1, e2, ..., e511]&lt;br/&gt;\u2190 entity IDs, dense storage\"]\n    end\n\n    C0 --&gt; ChunkDetail\n</code></pre>"},{"location":"concepts/memory-layout/#what-occupies-each-chunk","title":"What occupies each chunk","text":"<p>A single <code>ArchetypeChunk</code> owns:</p> <ul> <li>One <code>ComponentArray&lt;T&gt;</code> per registered component type \u2014 <code>std::vector&lt;T&gt;</code> contiguous storage</li> <li>A <code>SparseSet&lt;Entity&gt;</code> \u2014 entity IDs in dense array for fast iteration</li> </ul> <pre><code>Offset 0:   ComponentArray&lt;Position&gt;   \u2192 512 \u00d7 sizeof(Position)  = contiguous\nOffset N:   ComponentArray&lt;Velocity&gt;   \u2192 512 \u00d7 sizeof(Velocity)  = contiguous\nOffset M:   ComponentArray&lt;Health&gt;     \u2192 512 \u00d7 sizeof(Health)    = contiguous\n...\nOffset Z:   SparseSet&lt;Entity&gt;          \u2192 dense vector of entity IDs\n</code></pre>"},{"location":"concepts/memory-layout/#the-sparseset-data-structure","title":"The SparseSet data structure","text":"<p>Freyr uses a <code>SparseSet&lt;T&gt;</code> for entity storage within each chunk. This is not the same as an archetype \u2014 it's an implementation detail of <code>ArchetypeChunk</code>.</p> <pre><code>graph LR\n    subgraph SparseSet[\"SparseSet&amp;lt;Entity&amp;gt;\"]\n        SPARSE[\"Sparse array (bucket table)&lt;br/&gt;size_t[] \u2014 maps entity ID \u2192 dense index\"]\n        DENSE[\"Dense array (vector)&lt;br/&gt;[ Entity IDs in insertion order ]\"]\n    end\n\n    E1[\"Entity 42\"] --&gt;|hash/bucket| SPARSE\n    SPARSE --&gt;|index 5| DENSE\n    DENSE --&gt;|position 5| E1\n</code></pre>"},{"location":"concepts/memory-layout/#sparseset-operations","title":"SparseSet operations","text":"Operation Complexity Notes <code>insert(e)</code> O(1) amortised Appends to dense array, updates sparse bucket <code>remove(e)</code> O(1) Swap-with-last removal in dense array <code>contains(e)</code> O(1) Two array lookups <code>getIndex(e)</code> O(1) Returns position in dense array Iteration O(N) Linear scan of dense array <p>The swap-with-last removal pattern:</p> <pre><code>Before removal: Dense = [42, 17, 35, 99, 63]\nRemove entity 17:\n  Dense[1] = Dense[4]  (63)\n  Update sparse[63] = 1\n  Pop back\nAfter removal:  Dense = [42, 63, 35, 99]\n</code></pre> <p>This O(1) removal maintains dense iteration order at the cost of losing insertion order stability.</p>"},{"location":"concepts/memory-layout/#signature-matching","title":"Signature matching","text":"<p>Each archetype has a <code>Signature</code> \u2014 a bitset indicating which component types it contains:</p> <pre><code>class Signature {\n    static constexpr size_t BITSET_SIZE = 128;\n    using BitSet = std::bitset&lt;128&gt;;\n    std::vector&lt;BitSet&gt; mBitSets;  // extendable for &gt;128 component types\n};\n</code></pre> <p>A signature with 3 component types might look like:</p> <pre><code>Component IDs:    Position=0, Velocity=1, Health=2, PlayerTag=3, Mesh=4\nSignature bits:   [1, 1, 0, 1, 0, 0, 0, ...]\n                    \u2191  \u2191     \u2191\n                    P  V     PT\n</code></pre> <p>When you call <code>Query::Each&lt;Position, PlayerTag&gt;</code>, Freyr:</p> <ol> <li>Builds a signature with bits for Position (0) and PlayerTag (3)</li> <li>Tests every archetype: <code>querySignature.Match(archetypeSignature)</code></li> <li>For each match, iterates every chunk in that archetype</li> </ol> <pre><code>bool MatchArchetype(const Archetype* archetype) const {\n    const auto&amp; archetypeSignature = archetype-&gt;GetSignature();\n\n    // Exclusion filter: reject if any excluded component is present\n    if (!mExcludeSignature.IsEmpty()) {\n        if (mExcludeSignature.Match(archetypeSignature))\n            return false;\n    }\n\n    // Inclusion filter: require all included components\n    if (!mIncludeSignature.IsEmpty()) {\n        return mIncludeSignature.Match(archetypeSignature);\n    }\n\n    return true;\n}\n</code></pre>"},{"location":"concepts/memory-layout/#entity-index-map","title":"Entity index map","text":"<p>The <code>ComponentManager</code> maintains a flat <code>std::vector&lt;EntityIndex&gt;</code> mapping entity IDs to their storage location:</p> <pre><code>struct EntityIndex {\n    Archetype*      archetype;      // which archetype the entity belongs to\n    ArchetypeChunk* archetypeChunk; // which chunk within that archetype\n};\n</code></pre> <p>For entity ID <code>E</code>, <code>mEntityIndexes[E]</code> gives a direct pointer to the archetype and chunk. This is a dense index \u2014 all entity IDs from 0 to <code>MaxEntities - 1</code> have a pre-allocated slot.</p> <pre><code>graph TB\n    subgraph Index[\"Entity Index Map (vector)\"]\n        E0[\"ID 0 \u2192 (Arch*, Chunk*)\"]\n        E1[\"ID 1 \u2192 (Arch*, Chunk*)\"]\n        E2[\"ID 2 \u2192 (nullptr, nullptr)\"]\n        E3[\"ID 3 \u2192 (Arch*, Chunk*)\"]\n        E4[\"...\"]\n    end\n\n    Index --&gt; A1[\"Archetype A [Pos, Vel]\"]\n    Index --&gt; A2[\"Archetype B [Pos, Health]\"]\n\n    A1 --&gt; C0[\"Chunk 0\"]\n    A1 --&gt; C1[\"Chunk 1\"]\n</code></pre>"},{"location":"concepts/memory-layout/#archetype-migration","title":"Archetype migration","text":"<p>When a component is added or removed from an entity, it may need to move to a different archetype. This involves:</p> <ol> <li>Creating the new signature (add/remove bits)</li> <li>Finding or creating the target archetype</li> <li>Allocating a slot in the target chunk</li> <li>Copying component data from old chunk to new chunk</li> <li>Removing the entity from the old chunk</li> <li>Updating the entity index map</li> </ol> <pre><code>// Simplified: ComponentManager::CreateOrUpdateEntityIndexWith&lt;Ts...&gt;()\nauto signature = actualArchetype-&gt;GetSignature();\n// Modify signature for added/removed components...\n\nif (signature != actualArchetype-&gt;GetSignature()) {\n    // Find or create new archetype\n    Ref&lt;Archetype&gt; newArchetype = FindOrCreateArchetype(signature);\n\n    // Allocate in new chunk, schedule data migration\n    auto newChunk = newArchetype-&gt;AddEntity(entity);\n    actualChunk-&gt;EnqueueTask([=] {\n        actualChunk-&gt;MoveData(entity, newChunk);\n    });\n\n    // Update entity index\n    actualChunk     = newChunk;\n    actualArchetype = newArchetype.get();\n}\n</code></pre> <p>Migration cost</p> <p>Archetype migration involves copying all component data. While the copy itself is fast (especially for trivially copyable types), the allocation and signature matching add overhead. Avoid adding/removing components every frame \u2014 prefer tag components that remain stable.</p>"},{"location":"concepts/memory-layout/#cache-behaviour-summary","title":"Cache behaviour summary","text":"<pre><code>graph LR\n    subgraph Cold[\"Cold Cache (First Iteration)\"]\n        MISS[\"Load archetype list&lt;br/&gt;\u2192 cache miss\"]\n        CHUNK_MISS[\"Load chunk pointer&lt;br/&gt;\u2192 cache miss\"]\n        ARRAY_MISS[\"Load ComponentArray&lt;br/&gt;\u2192 cache miss\"]\n        DATA_MISS[\"Read entity data&lt;br/&gt;\u2192 cache miss\"]\n    end\n\n    subgraph Hot[\"Hot Cache (Subsequent Iterations)\"]\n        HIT_POS[\"Read Position[0..N]&lt;br/&gt;\u2190 L1/L2 cache hit\"]\n        HIT_VEL[\"Read Velocity[0..N]&lt;br/&gt;\u2190 L1/L2 cache hit\"]\n    end\n\n    Cold --&gt;|First Chunk| Hot\n</code></pre> <p>Key takeaways:</p> <ol> <li>All components of the same type are contiguous \u2014 iterating 1M positions is a linear cache-friendly scan</li> <li>Multiple component arrays are traversed in parallel \u2014 <code>ForEach&lt;Position, Velocity&gt;</code> strides both arrays    simultaneously, keeping data in cache</li> <li>Chunks provide spatial locality \u2014 512 entities worth of data fits in L2 cache on most CPUs</li> <li>Archetype separation \u2014 entities with different component sets don't pollute each other's cache lines</li> <li>Entity index is O(1) \u2014 direct array lookup, no hash or tree traversal</li> </ol> <p>Optimising for cache</p> <ul> <li>Keep components small (ideally \u2264 cache line size, 64 bytes)</li> <li>Group frequently-accessed data together (Position + Velocity instead of separate Transform)</li> <li>Use tag components (empty structs) for classification \u2014 zero memory overhead</li> <li>Avoid storing pointers inside components (they become invalid during migration)</li> </ul>"},{"location":"getting-started/installation/","title":"Installation","text":""},{"location":"getting-started/installation/#cmake-fetchcontent-recommended","title":"CMake FetchContent (recommended)","text":"<p>Add the following to your <code>CMakeLists.txt</code>:</p> <pre><code>cmake_minimum_required(VERSION 3.29)\nproject(my_project CXX)\n\nset(CMAKE_CXX_STANDARD 23)\nset(CMAKE_CXX_STANDARD_REQUIRED ON)\n\ninclude(FetchContent)\n\nFetchContent_Declare(\n    freyr\n    GIT_REPOSITORY https://github.com/gilmar-sales/Freyr.git\n    GIT_TAG        main  # pin to a tag or commit SHA for reproducibility\n)\n\nFetchContent_MakeAvailable(freyr)\n\nadd_executable(my_app main.cpp)\ntarget_link_libraries(my_app PRIVATE Freyr)\n</code></pre> <p>Then include the single umbrella header:</p> <pre><code>#include &lt;Freyr/Freyr.hpp&gt;\n</code></pre>"},{"location":"getting-started/installation/#building-from-source","title":"Building from source","text":"<pre><code>git clone --recurse-submodules https://github.com/gilmar-sales/Freyr.git\ncd Freyr\ncmake -B build -DCMAKE_BUILD_TYPE=Release\ncmake --build build --parallel\n</code></pre> <p>Clone with submodules</p> <p>Freyr requires Skirnir (fetched automatically via FetchContent by default) and optionally Perfetto (as a git submodule). Use <code>--recurse-submodules</code> to clone the full source tree.</p>"},{"location":"getting-started/installation/#tests","title":"Tests","text":"<pre><code>cd build\nctest --output-on-failure\n</code></pre>"},{"location":"getting-started/installation/#benchmarks","title":"Benchmarks","text":"<pre><code>./build/benchmarks/ComponentArray/freyr_component_array_benchmark\n./build/benchmarks/ExecutionStrategy/freyr_execution_strategy_benchmark\n</code></pre>"},{"location":"getting-started/installation/#build-options","title":"Build options","text":"Option Default Description <code>FREYR_BUILD_TESTS</code> <code>ON</code> Build the test suite <code>FREYR_BUILD_BENCHMARKS</code> <code>OFF</code> Build benchmarks <code>FREYR_BUILD_EXAMPLES</code> <code>OFF</code> Build example applications <code>FREYR_ASSERTIONS</code> <code>OFF</code> Enable runtime assertions (<code>FREYR_ASSERT</code> macro) <code>FREYR_PROFILING</code> <code>OFF</code> Enable Perfetto tracing support <p>Pass options via <code>-D</code> on the CMake command line:</p> <pre><code>cmake -B build -DFREYR_ASSERTIONS=ON -DFREYR_BUILD_BENCHMARKS=ON\n</code></pre>"},{"location":"getting-started/installation/#optional-perfetto-profiling","title":"Optional: Perfetto profiling","text":"<p>Freyr integrates with Perfetto for trace-based profiling. Enable it with the <code>FREYR_PROFILING</code> option. Perfetto is pulled as a git submodule from <code>vendor/perfetto</code>.</p> <pre><code>cmake -B build -DFREYR_PROFILING=ON -DCMAKE_BUILD_TYPE=RelWithDebInfo\ncmake --build build --parallel\n</code></pre> <p>Profiling build type</p> <p>Use <code>RelWithDebInfo</code> or <code>Release</code> for profiling. Debug builds have significantly higher overhead and produce distorted timing data.</p> <p>See the Profiling guide for usage details.</p>"},{"location":"getting-started/installation/#optional-enable-assertions","title":"Optional: Enable assertions","text":"<p>Runtime assertions help catch programming errors during development:</p> <pre><code>target_compile_definitions(my_app PRIVATE FREYR_ASSERTIONS)\n</code></pre> <p>Alternatively, pass <code>-DFREYR_ASSERTIONS=ON</code> when configuring Freyr itself. Assertions check for:</p> <ul> <li>Component registration before use</li> <li>Entity ID range validity</li> <li>Archetype chunk capacity violations</li> </ul>"},{"location":"getting-started/installation/#using-with-an-existing-project","title":"Using with an existing project","text":"<p>If you already have a <code>CMakeLists.txt</code>, add Freyr as a dependency:</p> <pre><code># In your project's CMakeLists.txt\ninclude(FetchContent)\n\nFetchContent_Declare(\n    freyr\n    GIT_REPOSITORY https://github.com/gilmar-sales/Freyr.git\n    GIT_TAG v1.0.0  # use a specific tag for stability\n)\n\nFetchContent_MakeAvailable(freyr)\n\ntarget_link_libraries(your_target PRIVATE Freyr)\n</code></pre> <p>Freyr will automatically find or fetch its own dependencies (Skirnir, Google Test).</p>"},{"location":"getting-started/installation/#troubleshooting","title":"Troubleshooting","text":""},{"location":"getting-started/installation/#c23-not-supported","title":"\"C++23 not supported\"","text":"<p>Ensure your compiler is recent enough:</p> <ul> <li>GCC \u2265 13</li> <li>Clang \u2265 16</li> <li>MSVC \u2265 19.37 (Visual Studio 2022 17.7+)</li> </ul>"},{"location":"getting-started/installation/#skirnir-not-found","title":"\"Skirnir not found\"","text":"<p>Freyr fetches Skirnir via <code>FetchContent</code> automatically. If you have a local copy, set <code>FETCHCONTENT_SOURCE_DIR_SKIRNIR</code>:</p> <pre><code>cmake -B build -DFETCHCONTENT_SOURCE_DIR_SKIRNIR=/path/to/skirnir\n</code></pre>"},{"location":"getting-started/installation/#build-is-slow","title":"Build is slow","text":"<ul> <li>Use <code>ccache</code> to cache compilations</li> <li>Use <code>-j</code> / <code>--parallel</code> with your build tool</li> <li>For development, build only Freyr: <code>cmake --build build --target Freyr</code></li> </ul>"},{"location":"getting-started/quick-start/","title":"Quick Start","text":"<p>This guide walks you through building a minimal simulation: entities with <code>Position</code> and <code>Velocity</code> components updated by a <code>MovementSystem</code>, running in parallel across 8 threads.</p>"},{"location":"getting-started/quick-start/#1-define-components","title":"1. Define components","text":"<p>Components are plain data structs that inherit from <code>fr::Component</code>. They hold only data \u2014 no logic, no methods.</p> <pre><code>// components.hpp\n#include &lt;Freyr/Freyr.hpp&gt;\n\nstruct Position : fr::Component {\n    float x = 0.f;\n    float y = 0.f;\n    float z = 0.f;\n};\n\nstruct Velocity : fr::Component {\n    float dx = 0.f;\n    float dy = 0.f;\n    float dz = 0.f;\n};\n</code></pre> <p>Default values matter</p> <p>Always provide sensible defaults for all fields. Components are value-initialized (<code>T {}</code>) during archetype creation, and defaults ensure every entity starts in a valid state.</p>"},{"location":"getting-started/quick-start/#2-define-a-system","title":"2. Define a system","text":"<p>Systems inherit from <code>fr::System</code> and override lifecycle hooks. The constructor must accept <code>Ref&lt;fr::Scene&gt;</code>.</p> <pre><code>// movement_system.hpp\n#include &lt;Freyr/Freyr.hpp&gt;\n#include \"components.hpp\"\n\nclass MovementSystem : public fr::System {\npublic:\n    explicit MovementSystem(const Ref&lt;fr::Scene&gt;&amp; scene) : System(scene) {}\n\n    void Update(float deltaTime) override {\n        // EachAsync \u2192 one task per chunk \u2192 distributed across all threads\n        mScene-&gt;CreateQuery()\n            -&gt;WithLabel(\"MovementSystem::Integrate\")  // (1)!\n            -&gt;EachAsync&lt;Position, Velocity&gt;(\n                [deltaTime](fr::Entity, Position&amp; pos, const Velocity&amp; vel) {\n                    pos.x += vel.dx * deltaTime;\n                    pos.y += vel.dy * deltaTime;\n                    pos.z += vel.dz * deltaTime;\n                });\n    }\n};\n</code></pre> <ol> <li>Labels appear in Perfetto traces when profiling is enabled. Always label your queries for debuggability.</li> </ol>"},{"location":"getting-started/quick-start/#3-define-the-application","title":"3. Define the application","text":"<p>The application creates an <code>ArchetypeBuilder</code> to spawn a million entities efficiently at startup.</p> <pre><code>// app.hpp\n#include &lt;Freyr/Freyr.hpp&gt;\n#include \"components.hpp\"\n\nclass MyApp : public skr::IApplication {\npublic:\n    explicit MyApp(const Ref&lt;skr::ServiceProvider&gt;&amp; sp) : IApplication(sp) {\n        mScene = sp-&gt;GetService&lt;fr::Scene&gt;();\n\n        // Bulk-create 1 million entities \u2014 fast path via archetype pre-allocation\n        mScene-&gt;CreateArchetypeBuilder()\n            .WithComponent(Position {})\n            .WithComponent(Velocity { .dx = 1.f, .dy = 0.5f })\n            .WithEntities(1'000'000)\n            .Build();\n    }\n\n    void Run() override {\n        constexpr float dt = 1.0f / 60.0f;\n        while (true)          // (1)!\n            mScene-&gt;Update(dt);\n    }\n\nprivate:\n    Ref&lt;fr::Scene&gt; mScene;\n};\n</code></pre> <ol> <li>In a real game you'd have a window event loop (<code>PollEvents</code>, <code>ShouldClose</code>, etc.). This infinite loop     demonstrates the conceptual frame structure.</li> </ol>"},{"location":"getting-started/quick-start/#4-bootstrap-with-freyrextension","title":"4. Bootstrap with FreyrExtension","text":"<p>The <code>FreyrExtension</code> wires everything together: registers components, configures options, and sets up pipelines.</p> <pre><code>// main.cpp\n#include &lt;Freyr/Freyr.hpp&gt;\n#include \"app.hpp\"\n#include \"components.hpp\"\n#include \"movement_system.hpp\"\n\nint main() {\n    auto app = skr::ApplicationBuilder()\n        .AddExtension&lt;fr::FreyrExtension&gt;([](fr::FreyrExtension&amp; freyr) {\n            freyr\n                .WithOptions([](fr::FreyrOptionsBuilder&amp; opts) {\n                    opts\n                        .WithMaxEntities(1'500'000)        // max live entities\n                        .WithArchetypeChunkCapacity(512)   // entities per chunk\n                        .WithThreadCount(8);               // worker threads\n                })\n                .WithComponent&lt;Position&gt;()\n                .WithComponent&lt;Velocity&gt;()\n                .WithPipeline([](fr::PipelineBuilder&amp; pipeline) {\n                    pipeline.WithName(\"Main\")\n                        .WithRate(60.0f)\n                        .WithSystem&lt;MovementSystem&gt;();\n                });\n        })\n        .Build&lt;MyApp&gt;();\n\n    app-&gt;Run();\n    return 0;\n}\n</code></pre>"},{"location":"getting-started/quick-start/#5-what-happens-at-runtime","title":"5. What happens at runtime","text":"<pre><code>sequenceDiagram\n    participant Main as main()\n    participant Builder as ApplicationBuilder\n    participant Extension as FreyrExtension\n    participant Scene as Scene\n    participant App as MyApp\n\n    Main-&gt;&gt;Builder: Build&lt;MyApp&gt;()\n    Builder-&gt;&gt;Extension: ConfigureServices\n    Extension-&gt;&gt;Extension: Register Position, Velocity\n    Extension-&gt;&gt;Extension: Register MovementSystem\n    Extension-&gt;&gt;Extension: Configure options\n    Builder-&gt;&gt;Scene: Create Scene\n    Builder-&gt;&gt;App: Create MyApp\n    App-&gt;&gt;Scene: CreateArchetypeBuilder()\n    Note over App,Scene: 1,000,000 entities created&lt;br/&gt;in chunks of 512\n    App-&gt;&gt;App: Enter Run loop\n\n    loop Every frame (dt = 1/60s)\n        App-&gt;&gt;Scene: Update(dt)\n        Scene-&gt;&gt;Scene: Flush events\n        Scene-&gt;&gt;Scene: PreUpdate(dt)\n        Scene-&gt;&gt;Scene: Update(dt)\n        Scene-&gt;&gt;MovementSystem: Update(dt)\n        MovementSystem-&gt;&gt;Query: EachAsync&lt;Pos, Vel&gt;\n        Query-&gt;&gt;ThreadPool: Enqueue chunk tasks\n        ThreadPool-&gt;&gt;ThreadPool: Distribute to workers\n        Note over ThreadPool: 8 threads process chunks concurrently\n        Scene-&gt;&gt;Scene: PostUpdate(dt)\n        Scene-&gt;&gt;Scene: DestroyEntities()\n    end</code></pre> <p>Each frame, the following happens:</p> <pre><code>Scene::Update(dt)\n\u251c\u2500 Flush()                     \u2192 merge pending event listeners\n\u251c\u2500 PreUpdate(dt)               \u2192 all systems: PreUpdate\n\u2502  \u251c\u2500 WaitForAllTasks()\n\u2502  \u2514\u2500 DestroyEntities()\n\u251c\u2500 Update(dt)                  \u2192 all systems: Update  \u2190 systems query here\n\u2502  \u251c\u2500 WaitForAllTasks()\n\u2502  \u2514\u2500 DestroyEntities()\n\u2514\u2500 PostUpdate(dt)              \u2192 all systems: PostUpdate\n   \u251c\u2500 WaitForAllTasks()\n   \u2514\u2500 DestroyEntities()\n</code></pre>"},{"location":"getting-started/quick-start/#6-memory-layout-at-runtime","title":"6. Memory layout at runtime","text":"<p>When the million entities are created, the memory looks like this:</p> <pre><code>graph LR\n    subgraph Archetype[\"Archetype [Position, Velocity]\"]\n        direction LR\n        C0[\"Chunk 0&lt;br/&gt;&lt;small&gt;Entities 0-511&lt;/small&gt;\"]\n        C1[\"Chunk 1&lt;br/&gt;&lt;small&gt;Entities 512-1023&lt;/small&gt;\"]\n        C2[\"...\"]\n        C1953[\"Chunk 1953&lt;br/&gt;&lt;small&gt;Entities 999488-999999&lt;/small&gt;\"]\n    end\n\n    subgraph Chunk0[\"Inside Chunk 0\"]\n        PA[\"Position[0..511]&lt;br/&gt;\u2193 \u2193 \u2193 \u2193 \u2193\"]\n        VA[\"Velocity[0..511]&lt;br/&gt;\u2193 \u2193 \u2193 \u2193 \u2193\"]\n    end\n\n    Archetype --&gt; C0\n    Archetype --&gt; C1\n    Archetype --&gt; C2\n    Archetype --&gt; C1953\n    C0 --&gt; Chunk0</code></pre> <p>With chunk capacity = 512,  1,000,000 entities \u2192 1,954 chunks \u2192 1,954 tasks per <code>EachAsync</code> call. Each chunk is an independent parallel unit.</p>"},{"location":"getting-started/quick-start/#7-verify-it-works","title":"7. Verify it works","text":"<p>Build and run:</p> <pre><code>cmake -B build -DCMAKE_BUILD_TYPE=Release\ncmake --build build --parallel\n./build/examples/QuickStart/freyr_quick_start  # if examples enabled\n</code></pre> <p>Or write your own <code>main.cpp</code> using the code above and link against Freyr.</p>"},{"location":"getting-started/quick-start/#next-steps","title":"Next steps","text":"<ul> <li>ECS Overview \u2014 understand the model behind the API</li> <li>Architecture \u2014 deep dive into Scene internals</li> <li>Scene API \u2014 full reference for entity and component operations</li> <li>ArchetypeBuilder \u2014 efficient bulk entity creation</li> <li>PipelineBuilder \u2014 organize systems into pipelines</li> <li>Parallel Processing guide \u2014 tune parallelism</li> </ul>"},{"location":"guides/parallel-processing/","title":"Parallel Processing","text":"<p>Freyr's parallelism model is built around chunk-level task dispatch. Understanding how work is distributed and how to choose the right iteration method is key to maximising performance.</p>"},{"location":"guides/parallel-processing/#the-parallelism-model","title":"The parallelism model","text":"<pre><code>graph TB\n    subgraph System[\"System::Update(dt)\"]\n        Q[\"CreateQuery()-&gt;EachAsync&amp;lt;Pos, Vel&amp;gt;(fn)\"]\n    end\n\n    subgraph QueryExec[\"Query Execution\"]\n        M[\"Match archetypes by signature\"]\n        C[\"For each matching archetype:\"]\n        CHUNKS[\"For each chunk in archetype:\"]\n        TASK[\"Enqueue chunk task to ThreadPool\"]\n    end\n\n    subgraph Workers[\"Worker Threads\"]\n        direction LR\n\n        subgraph Pool0[\"Worker 0\"]\n            Q0[\"Queue 0\"] --&gt; W0[\"Worker 0&lt;br/&gt;steals from Q1,Q2,Q3\"]\n        end\n\n        subgraph Pool1[\"Worker 1\"]\n            Q1[\"Queue 1\"] --&gt; W1[\"Worker 1&lt;br/&gt;steals from Q0,Q2,Q3\"]\n        end\n\n        subgraph Pool2[\"Worker 2\"]\n            Q2[\"Queue 2\"] --&gt; W2[\"Worker 2&lt;br/&gt;steals from Q0,Q1,Q3\"]\n        end\n\n        subgraph Pool3[\"Worker 3\"]\n            Q3[\"Queue 3\"] --&gt; W3[\"Worker 3&lt;br/&gt;steals from Q0,Q1,Q2\"]\n        end\n    end\n\n    Q --&gt; M --&gt; C --&gt; CHUNKS --&gt; TASK\n    TASK --&gt;|LCG hash| Q0\n    TASK --&gt;|LCG hash| Q1\n    TASK --&gt;|LCG hash| Q2\n    TASK --&gt;|LCG hash| Q3\n    W0 -.-&gt;|steal| Q1\n    W0 -.-&gt;|steal| Q2\n    W1 -.-&gt;|steal| Q0\n    W2 -.-&gt;|steal| Q3\n</code></pre> <p>When <code>EachAsync</code> is called:</p> <ol> <li>Freyr finds all archetypes matching the requested component signature</li> <li>For each matching archetype, every chunk becomes an independent task</li> <li>Tasks are enqueued to per-worker MPMC queues using LCG-based distribution</li> <li>Workers pop tasks from their own queue; idle workers steal from others</li> <li><code>ExecuteTasks()</code> or <code>WaitForAllTasks()</code> blocks until all tasks complete</li> </ol>"},{"location":"guides/parallel-processing/#synchronous-vs-asynchronous-iteration","title":"Synchronous vs asynchronous iteration","text":""},{"location":"guides/parallel-processing/#each-synchronous","title":"<code>Each</code> \u2014 synchronous","text":"<pre><code>mScene-&gt;CreateQuery()-&gt;Each&lt;Position, Velocity&gt;(\n    [dt](fr::Entity e, Position&amp; pos, Velocity&amp; vel) {\n        pos.x += vel.dx * dt;\n    });\n</code></pre> <ul> <li>Runs on the calling thread</li> <li>Guarantees sequential ordered iteration (by entity ID within each chunk)</li> <li>Safe for cross-entity reads/writes</li> <li>No synchronisation needed</li> </ul>"},{"location":"guides/parallel-processing/#eachasync-asynchronous","title":"<code>EachAsync</code> \u2014 asynchronous","text":"<pre><code>mScene-&gt;CreateQuery()-&gt;EachAsync&lt;Position, Velocity&gt;(\n    [dt](fr::Entity e, Position&amp; pos, Velocity&amp; vel) {\n        pos.x += vel.dx * dt;\n    });\nmScene-&gt;ExecuteTasks(); // sync point\n</code></pre> <ul> <li>Distributes chunks across all worker threads</li> <li>Entities are independent \u2014 no cross-entity communication within the callback</li> <li>Requires explicit synchronisation via <code>ExecuteTasks()</code> or the scene's built-in sync points</li> <li>Best for compute-heavy, embarrassingly parallel workloads</li> </ul> Method Blocking Thread pool Entity order Cross-entity reads Use for <code>Each</code> Yes No Stable Safe AI, interactions, debugging <code>EachAsync</code> No Yes Unstable Unsafe Physics, movement, particles"},{"location":"guides/parallel-processing/#work-stealing","title":"Work stealing","text":"<p>Each worker thread has its own MPMC queue. When <code>AddTask</code> is called, the task is pushed to one worker's queue using LCG-based distribution:</p> <pre><code>void AddTask(auto&amp;&amp; func) {\n    mTaskCounter-&gt;AddTasks(1);\n    mQueueLcgState = mQueueLcgState * LCG_MULTIPLIER + LCG_INCREMENT;\n    const auto nextQueue = mQueueLcgState % mWorkerQueues.size();\n    mWorkerQueues[nextQueue]-&gt;push(std::forward&lt;decltype(func)&gt;(func));\n}\n</code></pre> <p>When a worker's queue is empty, it tries to pop from other workers' queues. This work stealing ensures:</p> <ul> <li>Good load balance even with uneven task durations</li> <li>No single point of contention</li> <li>Automatic adaptation to heterogeneous workloads</li> </ul>"},{"location":"guides/parallel-processing/#chunk-level-parallelism","title":"Chunk-level parallelism","text":"<p>Each archetype chunk is the unit of parallel work. One task = one chunk.</p> <pre><code>System::Update(dt)\n  \u2514\u2500 Query::EachAsync&lt;Position, Velocity&gt;\n       \u251c\u2500 Archetype A [Position, Velocity] has 3 chunks\n       \u2502    \u251c\u2500 Task: chunk 0 (512 entities)\n       \u2502    \u251c\u2500 Task: chunk 1 (512 entities)\n       \u2502    \u2514\u2500 Task: chunk 2 (512 entities)\n       \u2514\u2500 Archetype B [Position, Velocity, Health] has 1 chunk\n            \u2514\u2500 Task: chunk 0 (512 entities)\n</code></pre>"},{"location":"guides/parallel-processing/#task-count-formula","title":"Task count formula","text":"<pre><code>Task count  =  \u03a3  ceiling(chunk_count_per_archetype)\n</code></pre> <p>For 1,000,000 entities with chunk capacity 512:</p> <pre><code>1,000,000 \u00f7 512 = 1,953.125 \u2192 1,954 chunks \u2192 1,954 tasks\n</code></pre> <p>More chunks = finer parallelism but higher scheduling overhead. Fewer chunks = less overhead but coarser load balancing.</p>"},{"location":"guides/parallel-processing/#overlapping-parallel-work","title":"Overlapping parallel work","text":"<p>To maximise throughput, overlap parallel computation with sequential work:</p> <pre><code>void Update(float dt) override {\n    // 1. Start parallel physics integration\n    mScene-&gt;CreateQuery()-&gt;WithLabel(\"Integrate\")\n        -&gt;EachAsync&lt;Position, Velocity&gt;([dt](fr::Entity e, Position&amp; pos, Velocity&amp; vel) {\n            pos.x += vel.dx * dt;\n            pos.y += vel.dy * dt;\n        });\n\n    // 2. Do sequential AI work while physics runs in background\n    mScene-&gt;CreateQuery()-&gt;WithLabel(\"AI Think\")\n        -&gt;Each&lt;AIState&gt;([dt](fr::Entity e, AIState&amp; ai) {\n            ai.thinkTimer -= dt;\n            if (ai.thinkTimer &lt;= 0.f)\n                ai.nextAction = computeNextAction(ai);\n        });\n\n    // 3. Sync \u2014 wait for all parallel tasks\n    mScene-&gt;ExecuteTasks();\n    // Now positions are consistent\n}\n</code></pre>"},{"location":"guides/parallel-processing/#timeline-diagram","title":"Timeline diagram","text":"<pre><code>gantt\n    title Overlapping Parallel Work\n    dateFormat  X\n    axisFormat  %s\n\n    section Main Thread\n    Schedule Physics     : 0, 1\n    Sequential AI        : 1, 3\n    Sync                 : 3, 4\n\n    section Worker 1\n    Process Chunk 0      : 0, 2\n    Steal Chunk 3        : 2, 4\n\n    section Worker 2\n    Process Chunk 1      : 0, 3\n    Idle                 : 3, 4\n\n    section Worker 3\n    Process Chunk 2      : 0, 4</code></pre>"},{"location":"guides/parallel-processing/#synchronisation-points","title":"Synchronisation points","text":"<p>Freyr has implicit and explicit sync points:</p>"},{"location":"guides/parallel-processing/#implicit-inside-sceneupdate","title":"Implicit (inside Scene::Update)","text":"<pre><code>PreUpdate  phase \u2192 WaitForAllTasks() + DestroyEntities()\nUpdate     phase \u2192 WaitForAllTasks() + DestroyEntities()\nPostUpdate phase \u2192 WaitForAllTasks() + DestroyEntities()\n</code></pre>"},{"location":"guides/parallel-processing/#explicit-user-controlled","title":"Explicit (user-controlled)","text":"<pre><code>mScene-&gt;ExecuteTasks(); // flush query aggregator + wait\n</code></pre> <p>Use explicit sync when you need to interleave parallel and sequential work within a single system.</p>"},{"location":"guides/parallel-processing/#avoiding-dependencies","title":"Avoiding dependencies","text":"<p>The biggest impact on parallel performance is avoiding dependencies between tasks:</p> <pre><code>// BAD: Each entity reads data from another entity\nmScene-&gt;CreateQuery()-&gt;EachAsync&lt;Position&gt;([this](fr::Entity e, Position&amp; p) {\n    // This system reads positions from other entities \u2014 RACE CONDITION!\n    auto otherPos = mScene-&gt;GetComponent&lt;Position&gt;(otherEntity);\n    p.x += otherPos.x;\n});\n\n// GOOD: Independent per-entity work\nmScene-&gt;CreateQuery()-&gt;EachAsync&lt;Position, Velocity&gt;(\n    [dt](fr::Entity e, Position&amp; p, Velocity&amp; v) {\n        p.x += v.dx * dt; // only reads/writes own data\n    });\n</code></pre>"},{"location":"guides/parallel-processing/#golden-rules","title":"Golden rules","text":"<ol> <li>Don't modify archetype structure during iteration \u2014 adding/removing components is deferred to <code>DestroyEntities()</code></li> <li>Avoid reading data written by another task in the same frame \u2014 use <code>ExecuteTasks()</code> to create sync points</li> <li>Don't call <code>Scene::Update</code> from within an <code>EachAsync</code> callback \u2014 undefined behaviour</li> <li>Don't throw exceptions from callbacks \u2014 behaviour is undefined in parallel execution</li> </ol>"},{"location":"guides/performance-tuning/","title":"Performance Tuning","text":"<p>This guide provides concrete strategies for maximising throughput in Freyr-based applications.</p>"},{"location":"guides/performance-tuning/#measure-first","title":"Measure first","text":"<p>Before optimising, measure. Freyr's Perfetto integration makes it easy to see where time is spent.</p> <pre><code>cmake -B build -DFREYR_PROFILING=ON -DCMAKE_BUILD_TYPE=RelWithDebInfo\ncmake --build build --parallel\n./my_app\n# Open the .perfetto-trace file in https://ui.perfetto.dev\n</code></pre> <p>Look for:</p> <ul> <li>Time per system \u2014 which system takes the most wall time?</li> <li>Thread utilisation \u2014 are all workers busy? Or are some idle?</li> <li>Chunk iteration time \u2014 how long does each chunk take?</li> <li>Scheduling overhead \u2014 time between <code>EnqueueTask</code> and first worker pickup</li> </ul>"},{"location":"guides/performance-tuning/#chunk-capacity-tuning","title":"Chunk capacity tuning","text":"<p><code>ArchetypeChunkCapacity</code> is the single most impactful configuration parameter. It controls task granularity.</p> Capacity Entities per task Task count (1M entities) Best for 128 128 ~7,813 Very fast callbacks, high thread count 256 256 ~3,906 Light callbacks, good load balance 512 512 ~1,953 Recommended starting point 1024 1024 ~977 Medium callbacks, physics 4096 4096 ~244 Heavy callbacks, pathfinding"},{"location":"guides/performance-tuning/#selecting-chunk-size-by-workload","title":"Selecting chunk size by workload","text":"<p>Light callbacks (microseconds per entity, e.g. simple transforms):</p> <pre><code>opts.WithArchetypeChunkCapacity(256);  // more tasks = better load balance\n</code></pre> <p>Medium callbacks (tens of microseconds, e.g. basic physics):</p> <pre><code>opts.WithArchetypeChunkCapacity(512);  // balanced\n</code></pre> <p>Heavy callbacks (hundreds of microseconds, e.g. AI pathfinding):</p> <pre><code>opts.WithArchetypeChunkCapacity(2048); // fewer tasks = less overhead\n</code></pre>"},{"location":"guides/performance-tuning/#thread-count","title":"Thread count","text":"<pre><code>opts.WithThreadCount(0);  // 0 = auto-detect (not yet implemented \u2014 defaults to 4)\nopts.WithThreadCount(8);  // explicit\nopts.WithAllPhysicalCores(); // use physical cores only (excludes SMT/HT)\n</code></pre> <p>Guidelines:</p> <ul> <li>Don't oversubscribe \u2014 using more threads than physical cores adds context-switching overhead</li> <li>Use <code>WithAllPhysicalCores()</code> on desktop/server \u2014 it queries OS topology to exclude HT/SMT logical cores</li> <li>Reserve 1 core for the main thread \u2014 if the main thread does non-trivial work besides <code>Scene::Update</code></li> </ul> <pre><code>// Example: 8 physical cores \u2192 7 workers, 1 for main thread\nopts.WithThreadCount(7);\n</code></pre>"},{"location":"guides/performance-tuning/#component-design","title":"Component design","text":""},{"location":"guides/performance-tuning/#keep-components-small","title":"Keep components small","text":"<p>Smaller components = more fit in cache = faster iteration.</p> <pre><code>// POOR: Giant blob component \u2014 all systems pay for data they don't use\nstruct EntityData : fr::Component {\n    float x, y, z;           // position\n    float dx, dy, dz;        // velocity\n    float maxHp, hp;         // health\n    float armor;             // damage reduction\n    int   teamId;            // faction\n    char  name[64];          // display name\n    // ... 20 more fields\n};\n\n// BETTER: Split into focused components\nstruct Position  : fr::Component { float x, y, z; };\nstruct Velocity  : fr::Component { float dx, dy, dz; };\nstruct Health    : fr::Component { float max, current; };\nstruct TeamTag   : fr::Component { int teamId; };\nstruct NameTag   : fr::Component { char name[64]; };\n</code></pre> <p>Now <code>MovementSystem</code> only reads <code>Position</code> + <code>Velocity</code> (24 bytes) instead of the full blob (100+ bytes).</p>"},{"location":"guides/performance-tuning/#use-pod-types","title":"Use POD types","text":"<p>Trivially copyable types enable <code>memcpy</code>-based component migration:</p> <pre><code>// FAST: Trivially copyable \u2192 memcpy during migration\nstruct Position : fr::Component {\n    float x, y, z;\n};\n\n// SLOWER: Non-trivial copy \u2192 element-wise copy\nstruct StringComponent : fr::Component {\n    std::string value; // copy involves allocation\n};\n</code></pre>"},{"location":"guides/performance-tuning/#use-tag-components","title":"Use tag components","text":"<p>An empty struct costs zero bytes of component storage but is useful for filtering:</p> <pre><code>struct DeadTag   : fr::Component {};\nstruct PlayerTag : fr::Component {};\nstruct EnemyTag  : fr::Component {};\n</code></pre> <pre><code>// Exclude dead entities from queries\nquery-&gt;Excluding&lt;DeadTag&gt;()-&gt;Each&lt;Position&gt;([](Entity e, Position&amp; p) { ... });\n</code></pre>"},{"location":"guides/performance-tuning/#iteration-patterns","title":"Iteration patterns","text":""},{"location":"guides/performance-tuning/#prefer-eachasync-for-independent-work","title":"Prefer <code>EachAsync</code> for independent work","text":"<p>If entities don't interact within a system, use <code>EachAsync</code> for free parallelism:</p> <pre><code>// Parallel \u2014 no entity reads another's data\nmScene-&gt;CreateQuery()-&gt;EachAsync&lt;Position, Velocity&gt;(fn);\n</code></pre>"},{"location":"guides/performance-tuning/#use-each-for-sequential-dependencies","title":"Use <code>Each</code> for sequential dependencies","text":"<p>When a system needs to read data written by another entity in the same iteration, use synchronous <code>Each</code>:</p> <pre><code>// Sequential \u2014 safe for cross-entity reads\nmScene-&gt;CreateQuery()-&gt;Each&lt;AIState&gt;([](Entity e, AIState&amp; ai) {\n    // reads data from other entities\n});\n</code></pre>"},{"location":"guides/performance-tuning/#batch-parallel-work","title":"Batch parallel work","text":"<pre><code>void Update(float dt) override {\n    // Start parallel work\n    mScene-&gt;CreateQuery()-&gt;EachAsync&lt;Position, Velocity&gt;(\"Integrate\", [dt](auto e, auto&amp; p, auto&amp; v) {\n        p.x += v.dx * dt;\n    });\n\n    // Do sequential work while integration runs\n    mScene-&gt;CreateQuery()-&gt;Each&lt;AIState&gt;(\"AI\", [dt](auto e, auto&amp; ai) {\n        ai.think(dt);\n    });\n\n    // Wait for parallel work\n    mScene-&gt;ExecuteTasks();\n\n    // Now positions are consistent\n}\n</code></pre>"},{"location":"guides/performance-tuning/#archetypebuilder-for-bulk-creation","title":"ArchetypeBuilder for bulk creation","text":"<p><code>ArchetypeBuilder</code> is much faster than calling <code>CreateEntity</code> in a loop because it:</p> <ol> <li>Pre-allocates all required chunks upfront</li> <li>Registers components once for the entire batch</li> <li>Creates entities in bulk with a single archetype lookup</li> </ol> <pre><code>// FAST: ArchetypeBuilder \u2014 single allocation, single registration\nscene-&gt;CreateArchetypeBuilder()\n    .WithComponent(Position {})\n    .WithComponent(Velocity {})\n    .WithEntities(100'000)\n    .Build();\n\n// SLOW: Loop \u2014 100k separate allocations and lookups\nfor (int i = 0; i &lt; 100'000; i++) {\n    scene-&gt;CreateEntity(Position {}, Velocity {});\n}\n</code></pre>"},{"location":"guides/performance-tuning/#event-system-overhead","title":"Event system overhead","text":"<p>Events have minimal overhead for the dispatch itself, but listeners add cost:</p> <ul> <li>Each <code>SendEvent</code> calls every active listener synchronously</li> <li>Listener callbacks run on the publisher's thread</li> <li>Use events for communication between dissimilar systems, not for high-frequency data passing</li> </ul> <pre><code>// APPROPRIATE: Collision events between unrelated systems\nscene-&gt;SendEvent(CollisionEvent {.entityA = a, .entityB = b});\n\n// OVERKILL: Don't use events for position updates \u2014 just add components\nscene-&gt;SendEvent(PositionUpdateEvent {.entity = e, .x = 10});  // \u2639 avoid\n</code></pre>"},{"location":"guides/performance-tuning/#avoid-archetype-thrashing","title":"Avoid archetype thrashing","text":"<p>Adding and removing components every frame causes repeated archetype migration, which copies all component data:</p> <pre><code>// BAD: Every frame moves entity between archetypes\nvoid Update(float dt) override {\n    mScene-&gt;AddComponent&lt;FrozenTag&gt;(entity);   // migrate \u2192 Archetype A + FrozenTag\n    // ... do frozen logic ...\n    mScene-&gt;RemoveComponent&lt;FrozenTag&gt;(entity); // migrate back\n}\n\n// BETTER: Check tag in system logic instead\nstruct FrozenTag : fr::Component { float timer; };\n\n// In system: check timer instead of adding/removing\nmScene-&gt;CreateQuery()-&gt;Each&lt;FrozenTag&gt;([dt](Entity e, FrozenTag&amp; f) {\n    f.timer -= dt;\n    if (f.timer &lt;= 0)\n        mScene-&gt;RemoveComponent&lt;FrozenTag&gt;(e);\n});\n</code></pre>"},{"location":"guides/performance-tuning/#memory-allocation","title":"Memory allocation","text":"<p>Freyr pre-allocates all entity index slots at startup (<code>MaxEntities</code>). Additional allocations happen when:</p> <ul> <li>New archetypes are created (component set never seen before)</li> <li>New chunks are created (existing chunks are full)</li> <li>Query results are collected (vector allocations)</li> </ul> <p>For predictable performance, ensure:</p> <ol> <li><code>MaxEntities</code> covers worst-case entity count</li> <li>All component combinations you'll use are registered upfront</li> <li>Avoid queries that collect large result sets every frame (prefer <code>ForEach</code> iteration)</li> </ol>"},{"location":"guides/performance-tuning/#checklist","title":"Checklist","text":"<ul> <li> Profile with Perfetto before optimising</li> <li> Tune chunk capacity to match callback weight</li> <li> Use <code>WithAllPhysicalCores()</code> for thread count</li> <li> Split large components into focused types</li> <li> Use tag components instead of runtime checks</li> <li> Prefer <code>EachAsync</code> for entity-independent work</li> <li> Use <code>ArchetypeBuilder</code> for bulk creation</li> <li> Avoid archetype migration every frame</li> <li> Keep components trivially copyable</li> <li> Avoid pointers inside components</li> </ul>"},{"location":"guides/profiling/","title":"Profiling with Perfetto","text":"<p>Freyr integrates with Perfetto to produce detailed execution traces. Traces can be opened in the Perfetto UI to visualise system timings, chunk iteration durations, and thread utilisation.</p>"},{"location":"guides/profiling/#enable-profiling","title":"Enable profiling","text":"<p>Define <code>FREYR_PROFILING</code> before building:</p> CMake optionCompiler flag <pre><code>cmake -B build -DFREYR_PROFILING=ON -DCMAKE_BUILD_TYPE=RelWithDebInfo\n</code></pre> <pre><code>target_compile_definitions(my_app PRIVATE FREYR_PROFILING)\n</code></pre> <p>Zero overhead when disabled</p> <p>When the flag is absent, all profiling macros compile to no-ops with zero runtime overhead.</p>"},{"location":"guides/profiling/#basic-usage","title":"Basic usage","text":"<p>Wrap the section you want to trace with <code>BeginProfiling</code> / <code>EndProfiling</code>:</p> <pre><code>void Run() override {\n    mScene-&gt;BeginProfiling();\n\n    for (int i = 0; i &lt; 200; i++)\n        mScene-&gt;Update(1.0f / 60.0f);\n\n    mScene-&gt;EndProfiling(); // flushes the trace file to disk\n}\n</code></pre> <p><code>EndProfiling</code> writes a <code>.perfetto-trace</code> file to the working directory (e.g. <code>freyr_trace.perfetto-trace</code>).</p>"},{"location":"guides/profiling/#viewing-the-trace","title":"Viewing the trace","text":"<ol> <li>Open ui.perfetto.dev in Chrome or Edge</li> <li>Click Open trace file and select the <code>.perfetto-trace</code> file</li> <li>Navigate the timeline to inspect each system and chunk</li> </ol>"},{"location":"guides/profiling/#what-youll-see","title":"What you'll see","text":"<pre><code>gantt\n    title Perfetto Trace View (simplified)\n    dateFormat  X\n    axisFormat  %s\n\n    section Thread 0 (Main)\n    Scene::Update       : 0, 10\n    PreUpdate           : 0, 2\n    Update              : 2, 8\n    PostUpdate          : 8, 10\n\n    section Thread 1 (Worker)\n    Physics::Integrate Chunk 0 : 2, 4\n    Physics::Integrate Chunk 4 : 5, 7\n\n    section Thread 2 (Worker)\n    Physics::Integrate Chunk 1 : 2, 5\n    Physics::Integrate Chunk 5 : 6, 8\n\n    section Thread 3 (Worker)\n    Physics::Integrate Chunk 2 : 2, 3\n    Physics::Integrate Chunk 3 : 3, 4\n    AI::Think Chunk 0          : 4, 8</code></pre> <p>You will see:</p> <ul> <li>One track per thread (main thread + each worker thread)</li> <li>Named spans for each labelled <code>Query::Each</code> / <code>Query::EachAsync</code> call</li> <li>System lifecycle boundaries \u2014 <code>PreUpdate</code>, <code>Update</code>, <code>PostUpdate</code> per pipeline</li> <li>Perfetto categories \u2014 <code>FREYR</code> for internal events, <code>USER</code> for user code</li> </ul>"},{"location":"guides/profiling/#custom-spans","title":"Custom spans","text":"<p>Add your own named spans around any code section:</p> <pre><code>void Update(float dt) override {\n    mScene-&gt;BeginTrace(\"CollisionBroadphase\");\n    runBroadphase();\n    mScene-&gt;EndTrace();\n\n    mScene-&gt;BeginTrace(\"CollisionNarrowphase\");\n    runNarrowphase();\n    mScene-&gt;EndTrace();\n}\n</code></pre> <p>Spans are nested under the calling thread's track in the Perfetto UI.</p>"},{"location":"guides/profiling/#labelling-iterations","title":"Labelling iterations","text":"<p>Set a label via <code>WithLabel</code> before iterating:</p> <pre><code>mScene-&gt;CreateQuery()\n    -&gt;WithLabel(\"Physics::Integrate\")\n    -&gt;Each&lt;Position, Velocity&gt;(fn);\n\nmScene-&gt;CreateQuery()\n    -&gt;WithLabel(\"Render::CullFrustum\")\n    -&gt;EachAsync&lt;Position&gt;(fn);\n</code></pre> <p>Without a label, the lambda's type name is used (often unreadable like <code>main::{lambda(auto:1)#1}</code>). Always use explicit labels in any code you want to profile.</p>"},{"location":"guides/profiling/#profiling-example","title":"Profiling example","text":"<p>The <code>examples/Profiling</code> directory contains a profiling-ready scenario:</p> <pre><code>mScene-&gt;BeginProfiling();\n\n// 2M entities with Position only\nmScene-&gt;CreateArchetypeBuilder()\n    .WithComponent(Position {})\n    .WithEntities(2'000'000)\n    .Build();\n\n// 2M entities with Position + Velocity\nmScene-&gt;CreateArchetypeBuilder()\n    .WithComponent(Position {})\n    .WithComponent(Velocity {})\n    .WithEntities(2'000'000)\n    .Build();\n\nfor (auto i = 0; i &lt; 100; i++)\n    mScene-&gt;Update(1.0f);\n\nmScene-&gt;EndProfiling();\n</code></pre> <p>Build and run:</p> <pre><code>cmake -B build -DCMAKE_BUILD_TYPE=Release -DFREYR_PROFILING=ON\ncmake --build build --target freyr_profiling_example\n./build/examples/Profiling/freyr_profiling_example\n</code></pre> <p>Then open the resulting trace in ui.perfetto.dev.</p>"},{"location":"guides/profiling/#what-to-look-for","title":"What to look for","text":""},{"location":"guides/profiling/#chunk-iteration-time","title":"Chunk iteration time","text":"<p>Select a chunk span and check its duration. Compare across different chunks:</p> <ul> <li>If all chunks take similar time \u2192 workload is homogeneous</li> <li>If some chunks take much longer \u2192 load imbalance (try smaller chunks)</li> </ul>"},{"location":"guides/profiling/#thread-utilisation","title":"Thread utilisation","text":"<p>Look at the worker tracks:</p> <ul> <li>All workers busy \u2192 good utilisation</li> <li>Some workers idle \u2192 imbalance (too few chunks, or work stealing is insufficient)</li> <li>All workers idle while main thread runs \u2192 expected \u2014 main thread does sequential work</li> </ul>"},{"location":"guides/profiling/#profiling-overhead","title":"Profiling overhead","text":"Aspect Impact Trace event emission ~50-100 ns per event File write ~100 MB/s (bounded by disk) Memory ~10-20 MB buffer (configurable) <p>Profiling overhead is generally negligible for workloads processing &gt;100K entities.</p>"},{"location":"guides/profiling/#tips","title":"Tips","text":"<ul> <li>Profile Release builds \u2014 Debug builds have much higher per-entity overhead that distorts results</li> <li>Run multiple warm-up frames before the profiled section to avoid cold-cache skew</li> <li>Test different chunk capacities to find the optimal task granularity</li> <li>Use the Slice details panel in Perfetto to see exact durations and thread assignments per chunk</li> <li>Enable Flow events in Perfetto to track task scheduling latency</li> <li>For long profiling sessions, reduce the sampling frequency to keep file sizes manageable</li> </ul>"}]}