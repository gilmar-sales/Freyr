{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Freyr","text":"<p>Freyr is a high-performance, multithreaded Entity-Component-System (ECS) library for C++23, designed for real-time simulations and games.</p> <p>Its core idea is simple: organize entities into archetype chunks \u2014 contiguous memory blocks grouped by component signature \u2014 and distribute those chunks across a thread pool as independent tasks. The result is predictable cache behaviour and straightforward parallelism without manual synchronization.</p>"},{"location":"#highlights","title":"Highlights","text":"<ul> <li> <p> High performance by design</p> <p>Entities are stored in contiguous archetype chunks, minimising cache misses. Two scheduling strategies let you tune between cache affinity and load balancing.</p> </li> <li> <p> Simple, composable API</p> <p>Fluent builder pattern throughout \u2014 configure components, systems, and options with a single, readable call chain.</p> </li> <li> <p> Built-in multithreading</p> <p><code>ForEachAsync</code> distribute chunk processing across a lock-free thread pool with zero boilerplate.</p> </li> <li> <p> Decoupled event system</p> <p>Thread-safe publish/subscribe bus lets systems communicate without direct dependencies.</p> </li> </ul>"},{"location":"#at-a-glance","title":"At a glance","text":"<pre><code>#include &lt;Freyr/Freyr.hpp&gt;\n\nstruct Position : fr::Component { float x, y, z; };\nstruct Velocity : fr::Component { float dx, dy, dz; };\n\nclass MovementSystem : public fr::System {\npublic:\n    explicit MovementSystem(const Ref&lt;fr::Scene&gt;&amp; scene) : System(scene) {}\n\n    void Update(float dt) override {\n        mScene-&gt;ForEachAsync&lt;Position, Velocity&gt;(\n            [dt](fr::Entity, Position&amp; pos, Velocity&amp; vel) {\n                pos.x += vel.dx * dt;\n                pos.y += vel.dy * dt;\n                pos.z += vel.dz * dt;\n            });\n    }\n};\n\nint main() {\n    auto app = skr::ApplicationBuilder()\n        .AddExtension&lt;fr::FreyrExtension&gt;([](fr::FreyrExtension&amp; freyr) {\n            freyr\n                .WithOptions([](fr::FreyrOptionsBuilder&amp; opts) {\n                    opts.WithMaxEntities(1'000'000).WithThreadCount(8);\n                })\n                .WithComponent&lt;Position&gt;()\n                .WithComponent&lt;Velocity&gt;()\n                .WithPipeline([](fr::PipelineBuilder&amp; pipeline) {\n                    pipeline.WithName(\"Physics\")\n                        .WithRate(60.0f)\n                        .WithSystem&lt;MovementSystem&gt;();\n                });\n        })\n        .Build&lt;MyApp&gt;();\n\n    app-&gt;Run();\n}\n</code></pre>"},{"location":"#requirements","title":"Requirements","text":"Requirement Minimum version C++ standard C++23 CMake 3.29 GCC 13 Clang 16 MSVC 19.37"},{"location":"#next-steps","title":"Next steps","text":"<ul> <li>Installation \u2014 Add Freyr to your project with CMake FetchContent</li> <li>Quick Start \u2014 Build a full example in minutes</li> <li>ECS Overview \u2014 Understand the concepts behind the library</li> </ul>"},{"location":"api/archetype-builder/","title":"ArchetypeBuilder","text":"<p><code>ArchetypeBuilder</code> efficiently creates large numbers of entities with the same component layout. It pre-allocates all chunks at once and populates them in parallel, making it significantly faster than calling <code>Scene::CreateEntity</code> in a loop for bulk spawning.</p> <p>Obtain a builder from the scene:</p> <pre><code>auto builder = scene-&gt;CreateArchetypeBuilder();\n</code></pre>"},{"location":"api/archetype-builder/#basic-usage","title":"Basic usage","text":"<pre><code>scene-&gt;CreateArchetypeBuilder()\n    .WithComponent(Position { .x = 0.f, .y = 0.f })\n    .WithComponent(Velocity { .dx = 1.f })\n    .WithEntities(100'000)\n    .Build();\n</code></pre> <p>This creates 100 000 entities, each with a <code>Position</code> initialised to <code>{0, 0}</code> and a <code>Velocity</code> initialised to <code>{dx: 1}</code>.</p>"},{"location":"api/archetype-builder/#methods","title":"Methods","text":""},{"location":"api/archetype-builder/#withcomponenttvalue","title":"<code>WithComponent&lt;T&gt;(value)</code>","text":"<p>Declares a component type and sets the default value applied to every entity created by this builder.</p> <pre><code>builder.WithComponent(Health { .max = 100, .current = 100 });\n</code></pre> <p>Template parameter: <code>T</code> is inferred from the argument type \u2014 must satisfy <code>fr::IsComponent</code>.</p> <p>Calling <code>WithComponent</code> with the same type more than once is ignored.</p>"},{"location":"api/archetype-builder/#withentitiescount","title":"<code>WithEntities(count)</code>","text":"<p>Sets the number of entities to create.</p> <pre><code>builder.WithEntities(50'000);\n</code></pre> <p>Calling <code>Build()</code> with <code>count = 0</code> returns <code>nullptr</code> without creating any entities.</p>"},{"location":"api/archetype-builder/#foreachtsfn","title":"<code>ForEach&lt;Ts...&gt;(fn)</code>","text":"<p>Registers a post-creation callback to customise individual entities. The callback is called with <code>(fr::Entity entity, Ts&amp;... components)</code> for every entity after they are created.</p> <pre><code>builder\n    .WithComponent(Position {})\n    .WithEntities(1000)\n    .ForEach&lt;Position&gt;([](fr::Entity e, Position&amp; pos) {\n        pos.x = static_cast&lt;float&gt;(e) * 2.f; // spread entities along X\n    });\n</code></pre> <p>Multiple <code>ForEach</code> calls are executed in registration order.</p>"},{"location":"api/archetype-builder/#build","title":"<code>Build()</code>","text":"<p>Commits the builder, creates the entities, and returns a <code>Ref&lt;Archetype&gt;</code>.</p> <pre><code>auto archetype = builder.Build(); // nullptr if WithEntities(0)\n</code></pre> <p>Archetype reuse: if an archetype with the same component signature already exists (from a previous <code>Build()</code> call or from <code>CreateEntity</code>), the new entities are appended to the existing archetype rather than creating a new one. The returned <code>Ref&lt;Archetype&gt;</code> points to the shared archetype.</p> <pre><code>auto a1 = scene-&gt;CreateArchetypeBuilder()\n    .WithComponent(Position { .x = 0.f })\n    .WithEntities(100)\n    .Build();\n\nauto a2 = scene-&gt;CreateArchetypeBuilder()\n    .WithComponent(Position { .x = 999.f })\n    .WithEntities(50)\n    .Build();\n\nassert(a1 == a2);        // same archetype object\nassert(a1-&gt;Count() == 150); // 150 total entities\n</code></pre>"},{"location":"api/archetype-builder/#full-example","title":"Full example","text":"<pre><code>// Spawn a grid of 10\u00d710 enemies, each with unique positions\nscene-&gt;CreateArchetypeBuilder()\n    .WithComponent(Position {})\n    .WithComponent(Health { .max = 50, .current = 50 })\n    .WithComponent(EnemyTag {})\n    .WithEntities(100)\n    .ForEach&lt;Position&gt;([](fr::Entity e, Position&amp; pos) {\n        pos.x = static_cast&lt;float&gt;(e % 10) * 5.f;\n        pos.y = static_cast&lt;float&gt;(e / 10) * 5.f;\n    })\n    .Build();\n</code></pre>"},{"location":"api/archetype-builder/#when-to-prefer-createentity","title":"When to prefer <code>CreateEntity</code>","text":"<p>Use <code>ArchetypeBuilder</code> when spawning many entities at startup with the same layout.</p> <p>Use <code>Scene::CreateEntity</code> for one-off entity creation during gameplay:</p> <pre><code>// Player fires a bullet \u2014 single entity, immediate\nscene-&gt;CreateEntity(\n    Position { mPlayer.x, mPlayer.y },\n    Velocity { .dx = 0.f, .dy = 20.f },\n    BulletTag {}\n);\n</code></pre>"},{"location":"api/components/","title":"Components","text":"<p>Components are the data layer of the ECS. They hold entity state and contain no logic.</p>"},{"location":"api/components/#defining-a-component","title":"Defining a component","text":"<p>Inherit from <code>fr::Component</code> and add your fields:</p> <pre><code>#include &lt;Freyr/Freyr.hpp&gt;\n\nstruct Transform : fr::Component {\n    float x        = 0.f;\n    float y        = 0.f;\n    float z        = 0.f;\n    float scaleX   = 1.f;\n    float scaleY   = 1.f;\n    float scaleZ   = 1.f;\n    float rotX     = 0.f;\n    float rotY     = 0.f;\n    float rotZ     = 0.f;\n};\n</code></pre>"},{"location":"api/components/#rules","title":"Rules","text":"<ul> <li>Inherit from <code>fr::Component</code> (empty base)</li> <li>No virtual methods \u2014 components are value types</li> <li>No logic \u2014 behaviour belongs in systems</li> <li>All fields should have sensible defaults so <code>T {}</code> produces a valid state</li> <li>Components are copyable (they are duplicated during archetype migration)</li> </ul>"},{"location":"api/components/#tag-components","title":"Tag components","text":"<p>A component with no fields acts as a tag \u2014 useful to mark entities without adding data:</p> <pre><code>struct PlayerTag : fr::Component {};\nstruct DeadTag   : fr::Component {};\n</code></pre> <pre><code>// Mark an entity as dead\nscene-&gt;AddComponent&lt;DeadTag&gt;(entity);\n\n// Query only living entities\nscene-&gt;ForEach&lt;Health&gt;([&amp;](fr::Entity e, Health&amp; hp) {\n    if (hp.current &lt;= 0)\n        scene-&gt;AddComponent&lt;DeadTag&gt;(e);\n});\n</code></pre>"},{"location":"api/components/#registration","title":"Registration","text":"<p>Every component type must be registered with <code>FreyrExtension::WithComponent&lt;T&gt;()</code> before any entity uses it:</p> <pre><code>freyr.WithComponent&lt;Transform&gt;()\n     .WithComponent&lt;Velocity&gt;()\n     .WithComponent&lt;Health&gt;()\n     .WithComponent&lt;PlayerTag&gt;();\n</code></pre>"},{"location":"api/components/#component-id","title":"Component ID","text":"<p>Each component type receives a unique, stable integer ID at first use:</p> <pre><code>fr::ComponentId id = fr::GetComponentId&lt;Transform&gt;(); // e.g. 0\n</code></pre> <p>IDs are assigned in the order <code>GetComponentId&lt;T&gt;()</code> is first called. They are consistent within a single run but not across runs.</p>"},{"location":"api/components/#concept-check","title":"Concept check","text":"<p>The <code>fr::IsComponent</code> concept validates that a type derives from <code>fr::Component</code>:</p> <pre><code>template &lt;typename T&gt;\nconcept IsComponent = std::is_base_of_v&lt;fr::Component, std::remove_reference_t&lt;T&gt;&gt;;\n</code></pre> <p>Template functions in <code>Scene</code> and <code>ArchetypeBuilder</code> are constrained by this concept, giving clear compile-time errors for unregistered or incorrect types.</p>"},{"location":"api/components/#design-tips","title":"Design tips","text":"<p>Keep components small. A component should represent a single, coherent aspect of an entity. Split large components into focused ones \u2014 <code>Position</code>, <code>Rotation</code>, and <code>Scale</code> rather than one <code>Transform</code> blob. This lets systems subscribe only to the data they actually need.</p> <p>Avoid pointers. Components are copied during archetype migrations. Raw pointers inside components will dangle. Use entity IDs or indices to reference other entities.</p> <p>Prefer POD types. Components with trivial copy/move constructors let the engine memcpy entire arrays during migration, which is much faster than element-wise construction.</p>"},{"location":"api/events/","title":"Events","text":"<p>Freyr's event system provides a thread-safe publish/subscribe bus for decoupled communication between systems. Events carry data from publishers to all active subscribers without direct references between systems.</p>"},{"location":"api/events/#defining-an-event","title":"Defining an event","text":"<p>Inherit from <code>fr::Event</code> and add your payload fields:</p> <pre><code>#include &lt;Freyr/Freyr.hpp&gt;\n\nstruct CollisionEvent : fr::Event {\n    fr::Entity entityA;\n    fr::Entity entityB;\n    float      impactForce = 0.f;\n};\n\nstruct EntityDestroyedEvent : fr::Event {\n    fr::Entity entity;\n    std::string reason;\n};\n</code></pre>"},{"location":"api/events/#publishing-an-event","title":"Publishing an event","text":"<p>Call <code>Scene::SendEvent</code> from anywhere that has access to the scene:</p> <pre><code>// From within a system's Update\nvoid Update(float dt) override {\n    mScene-&gt;ForEach&lt;Position, Collider&gt;(\n        [this](fr::Entity a, Position&amp; posA, Collider&amp; colA) {\n            // ... detect overlap ...\n            mScene-&gt;SendEvent(CollisionEvent {\n                .entityA     = a,\n                .entityB     = b,\n                .impactForce = 42.f\n            });\n        });\n}\n</code></pre> <p>Events are dispatched immediately \u2014 all subscribers are called synchronously before <code>SendEvent</code> returns.</p>"},{"location":"api/events/#subscribing-to-an-event","title":"Subscribing to an event","text":""},{"location":"api/events/#via-sceneaddeventlistener-recommended","title":"Via <code>Scene::AddEventListener</code> (recommended)","text":"<pre><code>class ResponseSystem : public fr::System {\npublic:\n    explicit ResponseSystem(const Ref&lt;fr::Scene&gt;&amp; scene) : System(scene) {\n        // Subscribe and store the handle\n        mCollisionHandle = scene-&gt;AddEventListener&lt;CollisionEvent&gt;(\n            [this](const CollisionEvent&amp; ev) {\n                onCollision(ev);\n            });\n    }\n\nprivate:\n    void onCollision(const CollisionEvent&amp; ev) {\n        // apply damage, play sound, etc.\n    }\n\n    Ref&lt;fr::ListenerHandle&gt; mCollisionHandle;\n};\n</code></pre>"},{"location":"api/events/#via-eventmanager-direct-injection","title":"Via <code>EventManager</code> (direct injection)","text":"<pre><code>class ResponseSystem : public fr::System {\npublic:\n    ResponseSystem(const Ref&lt;fr::Scene&gt;&amp; scene, Ref&lt;fr::EventManager&gt; events)\n        : System(scene)\n    {\n        mHandle = events-&gt;Subscribe&lt;CollisionEvent&gt;(\n            [](const CollisionEvent&amp; ev) { /* ... */ });\n    }\n\nprivate:\n    Ref&lt;fr::ListenerHandle&gt; mHandle;\n};\n</code></pre>"},{"location":"api/events/#subscription-lifetime","title":"Subscription lifetime","text":"<p>The subscription is active as long as the <code>ListenerHandle</code> shared_ptr is alive. When the <code>ListenerHandle</code> is destroyed (or reset), the subscription is automatically removed before the next <code>Publish</code> call.</p> <pre><code>// Active \u2014 mHandle keeps the subscription alive\nRef&lt;fr::ListenerHandle&gt; mHandle = scene-&gt;AddEventListener&lt;MyEvent&gt;(...);\n\n// Unsubscribe explicitly\nmHandle.reset(); // subscription is now dead\n</code></pre> <p>Don't discard the handle</p> <p>If you do not store the returned <code>Ref&lt;ListenerHandle&gt;</code>, it is destroyed immediately and the callback is never called.</p> <pre><code>// WRONG \u2014 handle destroyed, callback never fires\nscene-&gt;AddEventListener&lt;MyEvent&gt;([](const MyEvent&amp;) { ... });\n\n// CORRECT \u2014 store the handle\nmHandle = scene-&gt;AddEventListener&lt;MyEvent&gt;([](const MyEvent&amp;) { ... });\n</code></pre>"},{"location":"api/events/#thread-safety","title":"Thread safety","text":"<p><code>EventManager</code> is fully thread-safe:</p> <ul> <li>Multiple threads can <code>Subscribe</code> concurrently</li> <li>Multiple threads can <code>Send</code> concurrently</li> <li>Subscriptions registered while a <code>Publish</code> is in progress are queued and merged before the next call \u2014 they will never receive the current in-flight event</li> </ul>"},{"location":"api/events/#event-id","title":"Event ID","text":"<p>Each event type has a unique runtime ID:</p> <pre><code>fr::EventId id = fr::GetEventId&lt;CollisionEvent&gt;();\n</code></pre>"},{"location":"api/events/#multiple-events-example","title":"Multiple events example","text":"<pre><code>struct DamageEvent : fr::Event {\n    fr::Entity target;\n    int        amount;\n};\n\nstruct HealEvent : fr::Event {\n    fr::Entity target;\n    int        amount;\n};\n\nclass HealthSystem : public fr::System {\npublic:\n    HealthSystem(const Ref&lt;fr::Scene&gt;&amp; scene) : System(scene) {\n        mDamageHandle = scene-&gt;AddEventListener&lt;DamageEvent&gt;(\n            [this](const DamageEvent&amp; ev) {\n                mScene-&gt;TryGetComponents&lt;Health&gt;(ev.target,\n                    [&amp;](Health&amp; hp) { hp.current -= ev.amount; });\n            });\n\n        mHealHandle = scene-&gt;AddEventListener&lt;HealEvent&gt;(\n            [this](const HealEvent&amp; ev) {\n                mScene-&gt;TryGetComponents&lt;Health&gt;(ev.target,\n                    [&amp;](Health&amp; hp) {\n                        hp.current = std::min(hp.current + ev.amount, hp.max);\n                    });\n            });\n    }\n\nprivate:\n    Ref&lt;fr::ListenerHandle&gt; mDamageHandle;\n    Ref&lt;fr::ListenerHandle&gt; mHealHandle;\n};\n</code></pre>"},{"location":"api/extension/","title":"FreyrExtension","text":"<p><code>fr::FreyrExtension</code> integrates Freyr into a Skirnir application. It registers all services (Scene, managers, thread pool) into the DI container and wires up components and systems before the application starts.</p>"},{"location":"api/extension/#registration","title":"Registration","text":"<p>Pass it to <code>skr::ApplicationBuilder::AddExtension</code> with a configuration lambda:</p> <pre><code>skr::ApplicationBuilder()\n    .AddExtension&lt;fr::FreyrExtension&gt;([](fr::FreyrExtension&amp; freyr) {\n        freyr\n            .WithOptions([](fr::FreyrOptionsBuilder&amp; opts) {\n                opts.WithMaxEntities(500'000)\n                    .WithThreadCount(4);\n            })\n            .WithComponent&lt;Position&gt;()\n            .WithComponent&lt;Velocity&gt;()\n            .WithPipeline([](fr::PipelineBuilder&amp; pipeline) {\n                pipeline.WithName(\"Main\")\n                    .WithRate(60.0f)\n                    .WithSystem&lt;MovementSystem&gt;()\n                    .WithSystem&lt;CollisionSystem&gt;();\n            });\n    })\n    .Build&lt;MyApp&gt;();\n</code></pre> <p>All <code>With*</code> calls return <code>*this</code>, so they can be chained freely.</p>"},{"location":"api/extension/#methods","title":"Methods","text":""},{"location":"api/extension/#withcomponentt","title":"<code>WithComponent&lt;T&gt;()</code>","text":"<p>Registers a component type with the <code>ComponentManager</code>.</p> <pre><code>freyr.WithComponent&lt;TransformComponent&gt;();\n</code></pre> <p>Warning</p> <p>Every component type used in the application must be registered before the first entity that uses it is created. Failing to register a component results in a runtime assertion.</p> <p>Template parameter: <code>T</code> \u2014 must satisfy <code>fr::IsComponent</code> (i.e. inherit from <code>fr::Component</code>).</p>"},{"location":"api/extension/#withpipelinefn","title":"<code>WithPipeline(fn)</code>","text":"<p>Defines a pipeline containing systems. Systems are constructed in registration order within each pipeline. If a system needs an <code>EventManager</code>, Skirnir resolves it automatically:</p> <pre><code>class PhysicsSystem : public fr::System {\npublic:\n    PhysicsSystem(const Ref&lt;fr::Scene&gt;&amp; scene, Ref&lt;fr::EventManager&gt; events)\n        : System(scene), mEvents(events) {}\n    // ...\n};\n</code></pre> <p>Callback parameter: <code>fn</code> \u2014 receives a <code>PipelineBuilder</code> to configure the pipeline.</p> <p>See <code>PipelineBuilder</code> for pipeline configuration options.</p>"},{"location":"api/extension/#withoptionsfn","title":"<code>WithOptions(fn)</code>","text":"<p>Configures runtime parameters via a <code>FreyrOptionsBuilder</code> callback.</p> <pre><code>freyr.WithOptions([](fr::FreyrOptionsBuilder&amp; opts) {\n    opts.WithMaxEntities(1'000'000)\n        .WithArchetypeChunkCapacity(512)\n        .WithThreadCount(std::thread::hardware_concurrency())\n        .WithExecutionStrategy(fr::FreyrExecutionStategy::ChunkAffinity);\n});\n</code></pre> <p>See <code>FreyrOptionsBuilder</code> for all available options.</p>"},{"location":"api/options-builder/","title":"FreyrOptionsBuilder","text":"<p><code>fr::FreyrOptionsBuilder</code> configures Freyr's runtime parameters. It is used inside the <code>WithOptions</code> callback of <code>FreyrExtension</code>:</p> <pre><code>freyr.WithOptions([](fr::FreyrOptionsBuilder&amp; opts) {\n    opts.WithMaxEntities(1'000'000)\n        .WithArchetypeChunkCapacity(512)\n        .WithThreadCount(8)\n        .WithExecutionStrategy(fr::FreyrExecutionStategy::ChunkAffinity);\n});\n</code></pre> <p>All methods return <code>*this</code> for chaining. Unset options fall back to the defaults shown below.</p>"},{"location":"api/options-builder/#methods","title":"Methods","text":""},{"location":"api/options-builder/#withmaxentitiesn","title":"<code>WithMaxEntities(n)</code>","text":"<p>Sets the maximum number of live entities allowed simultaneously.</p> Parameter Type Default <code>n</code> <code>size_t</code> <code>16 777 216</code> (16 M) <pre><code>opts.WithMaxEntities(500'000);\n</code></pre>"},{"location":"api/options-builder/#witharchetypechunkcapacityn","title":"<code>WithArchetypeChunkCapacity(n)</code>","text":"<p>Sets the number of entities per archetype chunk. This is the primary knob for task granularity.</p> Parameter Type Default <code>n</code> <code>size_t</code> <code>512</code> <pre><code>opts.WithArchetypeChunkCapacity(1024);\n</code></pre> <p>Tuning</p> <p>Smaller values \u2192 more chunks \u2192 finer parallelism, higher scheduling overhead. Larger values \u2192 fewer chunks \u2192 better sequential throughput, coarser parallelism. Start with 512, then benchmark with 128, 256, 1024, 4096.</p>"},{"location":"api/options-builder/#withthreadcountn","title":"<code>WithThreadCount(n)</code>","text":"<p>Sets the number of worker threads in the task pool.</p> Parameter Type Default <code>n</code> <code>size_t</code> <code>4</code> <pre><code>opts.WithThreadCount(std::thread::hardware_concurrency());\n</code></pre>"},{"location":"api/options-builder/#withexecutionstrategystrategy","title":"<code>WithExecutionStrategy(strategy)</code>","text":"<p>Selects how chunk tasks are distributed across worker threads.</p> Parameter Type Default <code>strategy</code> <code>FreyrExecutionStategy</code> <code>ChunkAffinity</code> <pre><code>opts.WithExecutionStrategy(fr::FreyrExecutionStategy::DispatchOrder);\n</code></pre> Value Description <code>ChunkAffinity</code> Chunks are preferentially processed by the same worker thread frame-over-frame, maximising cache reuse <code>DispatchOrder</code> Chunks are dispatched in creation order; simpler, more predictable scheduling"},{"location":"api/options-builder/#default-values-summary","title":"Default values summary","text":"Option Default <code>MaxEntities</code> 16 777 216 <code>ArchetypeChunkCapacity</code> 512 <code>ThreadCount</code> 4 <code>ExecutionStrategy</code> <code>ChunkAffinity</code>"},{"location":"api/pipeline-builder/","title":"PipelineBuilder","text":"<p><code>PipelineBuilder</code> defines a named collection of systems that run at a specific update rate. Pipelines allow you to organize systems into logical groups with different execution frequencies.</p> <p>Obtain a builder from <code>FreyrExtension::WithPipeline()</code>:</p> <pre><code>freyr.WithPipeline([](fr::PipelineBuilder&amp; pipeline) {\n    pipeline.WithName(\"Main\")\n        .WithRate(60.0f)\n        .WithSystem&lt;MovementSystem&gt;()\n        .WithSystem&lt;RenderSystem&gt;();\n});\n</code></pre>"},{"location":"api/pipeline-builder/#methods","title":"Methods","text":""},{"location":"api/pipeline-builder/#withnamename","title":"<code>WithName(name)</code>","text":"<p>Sets the pipeline's display name, used in profiling traces.</p> Parameter Type Default <code>name</code> <code>std::string_view</code> <code>\"Pipeline {id}\"</code> <pre><code>pipeline.WithName(\"Physics\");\n</code></pre>"},{"location":"api/pipeline-builder/#withraterate","title":"<code>WithRate(rate)</code>","text":"<p>Sets the target update rate for this pipeline in Hz. The pipeline will be scheduled to run at this frequency.</p> Parameter Type Default <code>rate</code> <code>float</code> <code>0.0</code> (runs every frame) <pre><code>pipeline.WithRate(60.0f); // 60 Hz\npipeline.WithRate(30.0f); // 30 Hz\npipeline.WithRate(0.0f);  // every frame\n</code></pre>"},{"location":"api/pipeline-builder/#withsystemt","title":"<code>WithSystem&lt;T&gt;()</code>","text":"<p>Registers a system type to this pipeline. Systems are constructed in registration order.</p> <pre><code>pipeline.WithSystem&lt;InputSystem&gt;()\n        .WithSystem&lt;MovementSystem&gt;()\n        .WithSystem&lt;CollisionSystem&gt;();\n</code></pre> <p>Template parameter: <code>T</code> \u2014 must satisfy <code>fr::IsSystem</code> (i.e. inherit from <code>fr::System</code>).</p> <p>Systems are automatically added as singletons to the DI container and can receive injected dependencies:</p> <pre><code>class PhysicsSystem : public fr::System {\npublic:\n    PhysicsSystem(const Ref&lt;fr::Scene&gt;&amp; scene, Ref&lt;fr::EventManager&gt; events)\n        : System(scene), mEvents(events) {}\n\n    void Update(float dt) override {\n        // ...\n    }\n\nprivate:\n    Ref&lt;fr::EventManager&gt; mEvents;\n};\n</code></pre>"},{"location":"api/pipeline-builder/#multiple-pipelines","title":"Multiple pipelines","text":"<p>You can define multiple pipelines with different rates:</p> <pre><code>freyr\n    .WithPipeline([](fr::PipelineBuilder&amp; physics) {\n        physics.WithName(\"Physics\")\n            .WithRate(60.0f)\n            .WithSystem&lt;PhysicsSystem&gt;()\n            .WithSystem&lt;CollisionSystem&gt;();\n    })\n    .WithPipeline([](fr::PipelineBuilder&amp; render) {\n        render.WithName(\"AI\")\n            .WithRate(5.0f)\n            .WithSystem&lt;MonsterSystem&gt;()\n            .WithSystem&lt;NPCSystem&gt;();\n    });\n</code></pre>"},{"location":"api/pipeline-builder/#system-execution-order","title":"System execution order","text":"<p>Within a pipeline, systems execute in registration order. If <code>SystemB</code> depends on results produced by <code>SystemA</code>, register <code>SystemA</code> first:</p> <pre><code>pipeline.WithSystem&lt;InputSystem&gt;()      // runs first\n        .WithSystem&lt;MovementSystem&gt;()   // uses input\n        .WithSystem&lt;CollisionSystem&gt;()  // uses movement\n        .WithSystem&lt;RenderSystem&gt;();    // runs last\n</code></pre> <p>For data dependencies within a system iteration, use <code>ForEach</code> (sequential) rather than <code>ForEachAsync</code> when the callback reads from other entities.</p>"},{"location":"api/query/","title":"Query","text":"<p><code>fr::Query</code> provides a fluent API for filtering and querying entities by their component composition. Queries support include/exclude filters, various terminal operations for collecting or processing matching entities, and can be scheduled for async execution.</p> <p>Obtain a Query instance via <code>Scene::CreateQuery()</code>:</p> <pre><code>auto query = scene-&gt;CreateQuery();\n</code></pre>"},{"location":"api/query/#filter-configuration","title":"Filter configuration","text":""},{"location":"api/query/#excludingts","title":"<code>Excluding&lt;Ts...&gt;</code>","text":"<pre><code>Query&amp; Excluding() requires(IsComponent&lt;Ts&gt; and ...);\n</code></pre> <p>Adds component types to the exclusion filter. Entities with any of the specified components will be excluded from query results.</p> <pre><code>query-&gt;Excluding&lt;DisabledTag, EditorOnly&gt;();\n</code></pre>"},{"location":"api/query/#terminal-operations","title":"Terminal operations","text":""},{"location":"api/query/#countts","title":"<code>Count&lt;Ts...&gt;</code>","text":"<pre><code>std::size_t Count() requires(IsComponent&lt;Ts&gt; and ...);\n</code></pre> <p>Returns the total number of entities matching the query.</p> <pre><code>auto alive = query.Count&lt;Health&gt;();\n</code></pre>"},{"location":"api/query/#entitieswithts","title":"<code>EntitiesWith&lt;Ts...&gt;</code>","text":"<pre><code>std::vector&lt;Entity&gt; EntitiesWith() requires(IsComponent&lt;Ts&gt; and ...);\n</code></pre> <p>Returns all entity IDs that have all specified component types.</p> <pre><code>auto players = query-&gt;EntitiesWith&lt;PlayerTag, Health&gt;();\n</code></pre>"},{"location":"api/query/#finduniquets","title":"<code>FindUnique&lt;Ts...&gt;</code>","text":"<pre><code>std::optional&lt;Entity&gt; FindUnique() requires(IsComponent&lt;Ts&gt; and ...);\n</code></pre> <p>Asserts that exactly one entity matches, then returns it. Returns <code>std::nullopt</code> if zero or more than one entity matches. Useful for singleton entities (camera, player).</p> <pre><code>auto camera = query-&gt;FindUnique&lt;CameraComponent&gt;();\n</code></pre>"},{"location":"api/query/#firstts","title":"<code>First&lt;Ts...&gt;</code>","text":"<pre><code>std::optional&lt;Entity&gt; First() requires(IsComponent&lt;Ts&gt; and ...);\n</code></pre> <p>Returns the first entity matching the query, or <code>std::nullopt</code> if none found.</p> <pre><code>auto entity = query-&gt;First&lt;PlayerTag&gt;();\n</code></pre>"},{"location":"api/query/#iteratets","title":"<code>Iterate&lt;Ts...&gt;</code>","text":"<pre><code>std::vector&lt;std::tuple&lt;Entity, Ts...&gt;&gt; Iterate() requires(IsComponent&lt;Ts&gt; and ...);\n</code></pre> <p>Collects all matching entities and their components into a vector of tuples.</p> <pre><code>auto entities = query-&gt;Iterate&lt;Position, Velocity&gt;();\nfor (auto&amp;&amp; [entity, pos, vel] : entities) {\n    // ...\n}\n</code></pre>"},{"location":"api/query/#transformts","title":"<code>Transform&lt;Ts...&gt;</code>","text":"<pre><code>auto Transform(auto&amp;&amp; callback) -&gt; std::vector&lt;decltype(callback(...))&gt;;\n</code></pre> <p>Maps each entity to a transformed value. Callback can accept either <code>(Entity, Ts&amp;...)</code> or <code>(Ts&amp;...)</code>.</p> <pre><code>auto distances = query-&gt;Transform&lt;Position&gt;([origin](Entity, Position&amp; pos) {\n    return std::sqrt(pos.x * pos.x + pos.y * pos.y);\n});\n</code></pre>"},{"location":"api/query/#mapts","title":"<code>Map&lt;Ts...&gt;</code>","text":"<pre><code>auto Map(auto&amp;&amp; f) -&gt; std::vector&lt;decltype(f(...))&gt;;\n</code></pre> <p>Applies a transform function and returns results as a vector, ordered by entity.</p> <pre><code>auto distances = query-&gt;Map&lt;Position&gt;([origin](Entity e, Position&amp; pos) {\n    float dx = pos.x - origin.x;\n    float dy = pos.y - origin.y;\n    return std::sqrt(dx*dx + dy*dy);\n});\n</code></pre>"},{"location":"api/query/#reducets","title":"<code>Reduce&lt;Ts...&gt;</code>","text":"<pre><code>auto Reduce(auto&amp;&amp; callback, auto seed) -&gt; decltype(seed);\n</code></pre> <p>Accumulates values across all matching entities. Callback signature: <code>(ResultType, Ts&amp;...) -&gt; ResultType</code>.</p> <pre><code>auto totalHealth = query-&gt;Reduce&lt;Health&gt;([](float acc, Health&amp; h) {\n    return acc + h.current;\n}, 0.f);\n</code></pre>"},{"location":"api/query/#iteration","title":"Iteration","text":""},{"location":"api/query/#eachts","title":"<code>Each&lt;Ts...&gt;</code>","text":"<pre><code>Query&amp; Each(auto&amp;&amp; action) requires(IsComponent&lt;Ts&gt; and ...);\n</code></pre> <p>Synchronous iteration over all matching entities. Callback receives <code>(Entity, Ts&amp;...)</code>.</p> <pre><code>query-&gt;Including&lt;Position, Velocity&gt;()\n    -&gt;WithLabel(\"UpdatePositions\")\n    -&gt;Each&lt;Position, Velocity&gt;([](Entity e, Position&amp; pos, Velocity&amp; vel) {\n        pos.x += vel.dx * dt;\n    });\n</code></pre>"},{"location":"api/query/#eachasyncts","title":"<code>EachAsync&lt;Ts...&gt;</code>","text":"<pre><code>Query&amp; EachAsync(auto&amp;&amp; action) requires(IsComponent&lt;Ts&gt; and ...);\n</code></pre> <p>Dispatches chunk tasks to the thread pool for parallel execution. Call <code>ExecuteTasks()</code> on the scene to wait for completion.</p> <pre><code>query-&gt;EachAsync&lt;Position, Velocity&gt;([](Entity e, Position&amp; pos, Velocity&amp; vel) {\n    pos.x += vel.dx * dt;\n});\nscene-&gt;ExecuteTasks();\n</code></pre>"},{"location":"api/query/#utility","title":"Utility","text":""},{"location":"api/query/#withlabel","title":"<code>WithLabel</code>","text":"<pre><code>Query&amp; WithLabel(const std::string_view name);\n</code></pre> <p>Assigns a human-readable label for profiling and debugging. When <code>FREYR_PROFILING=ON</code>, the label appears in Perfetto traces.</p> <pre><code>query-&gt;WithLabel(\"PhysicsUpdate\");\n</code></pre>"},{"location":"api/query/#notes","title":"Notes","text":"<ul> <li>Query instances should not be stored long-term as they hold references to <code>ComponentManager</code>.</li> <li>Use <code>Scene::CreateQuery()</code> to obtain a fresh query instance when needed.</li> <li>The QueryAggregator coordinates async query execution across worker threads.</li> </ul>"},{"location":"api/scene/","title":"Scene","text":"<p><code>fr::Scene</code> is the central orchestrator of Freyr. It provides the full entity lifecycle API, component operations, iteration methods, event helpers, and the main update loop.</p> <p>Obtain an instance from the service provider:</p> <pre><code>class MyApp : public skr::IApplication {\npublic:\n    explicit MyApp(const Ref&lt;skr::ServiceProvider&gt;&amp; sp) : IApplication(sp) {\n        mScene = sp-&gt;GetService&lt;fr::Scene&gt;();\n    }\n};\n</code></pre>"},{"location":"api/scene/#entity-management","title":"Entity management","text":""},{"location":"api/scene/#createentity","title":"<code>CreateEntity</code>","text":"<pre><code>// Create with components, returns entity ID\nEntity CreateEntity(const Ts&amp;... components);\n\n// Create with components and receive the ID via callback\nvoid CreateEntity(TFunc&amp;&amp; callback, const Ts&amp;... components);\n</code></pre> <pre><code>// No components\nfr::Entity e = scene-&gt;CreateEntity();\n\n// With initial components\nfr::Entity e = scene-&gt;CreateEntity(Position { .x = 10.f }, Velocity {});\n\n// Via callback\nscene-&gt;CreateEntity([](fr::Entity e) {\n    // e is available here immediately\n}, Position {}, Velocity {});\n</code></pre>"},{"location":"api/scene/#destroyentity","title":"<code>DestroyEntity</code>","text":"<pre><code>void DestroyEntity(const Entity&amp; entity);\n</code></pre> <p>Destruction is deferred \u2014 the entity is added to a pending set and removed at the end of the current <code>Update</code> call.</p> <pre><code>scene-&gt;DestroyEntity(e);\n// entity is still alive until Update() returns\n</code></pre>"},{"location":"api/scene/#component-operations","title":"Component operations","text":""},{"location":"api/scene/#addcomponentt","title":"<code>AddComponent&lt;T&gt;</code>","text":"<pre><code>void AddComponent&lt;T&gt;(const Entity&amp; entity, const T&amp; component = {});\n</code></pre> <p>Adds a single component to an existing entity. If the entity does not yet belong to an archetype that includes <code>T</code>, it is migrated to the correct one.</p> <pre><code>scene-&gt;AddComponent&lt;Health&gt;(entity, Health { .hp = 100 });\n</code></pre>"},{"location":"api/scene/#addcomponentsts","title":"<code>AddComponents&lt;Ts...&gt;</code>","text":"<pre><code>void AddComponents&lt;Ts...&gt;(const Entity&amp; entity, const Ts&amp;... components);\n</code></pre> <p>Adds multiple components in one call. More efficient than calling <code>AddComponent</code> repeatedly.</p> <pre><code>scene-&gt;AddComponents&lt;Position, Velocity&gt;(entity, Position {}, Velocity { .dx = 1.f });\n</code></pre>"},{"location":"api/scene/#removecomponentt","title":"<code>RemoveComponent&lt;T&gt;</code>","text":"<pre><code>void RemoveComponent&lt;T&gt;(const Entity&amp; entity);\n</code></pre> <p>Removes a component and migrates the entity to the appropriate archetype.</p> <pre><code>scene-&gt;RemoveComponent&lt;Velocity&gt;(entity); // entity stops moving\n</code></pre>"},{"location":"api/scene/#hascomponentt-hascomponentsts","title":"<code>HasComponent&lt;T&gt;</code> / <code>HasComponents&lt;Ts...&gt;</code>","text":"<pre><code>bool HasComponent&lt;T&gt;(const Entity&amp; entity) const;\nbool HasComponents&lt;Ts...&gt;(const Entity&amp; entity) const;\n</code></pre> <pre><code>if (scene-&gt;HasComponents&lt;Position, Velocity&gt;(entity)) {\n    // safe to iterate\n}\n</code></pre>"},{"location":"api/scene/#trygetcomponentsts","title":"<code>TryGetComponents&lt;Ts...&gt;</code>","text":"<pre><code>bool TryGetComponents&lt;Ts...&gt;(const Entity&amp; entity, auto&amp;&amp; callback);\n</code></pre> <p>Invokes <code>callback(Ts&amp;...)</code> only if the entity has all requested components. Returns <code>true</code> on success.</p> <pre><code>bool found = scene-&gt;TryGetComponents&lt;Position, Health&gt;(entity,\n    [](Position&amp; pos, Health&amp; hp) {\n        pos.x += 1.f;\n        hp.current -= 5;\n    });\n</code></pre>"},{"location":"api/scene/#queries","title":"Queries","text":""},{"location":"api/scene/#createquery","title":"<code>CreateQuery</code>","text":"<pre><code>Ref&lt;Query&gt; CreateQuery() const;\ntemplate &lt;typename TQuery&gt; requires(std::is_base_of_v&lt;Query, TQuery&gt;)\nRef&lt;TQuery&gt; CreateQuery();\n</code></pre> <p>Creates a new Query instance for entity searching. Query instances should not be stored long-term as they hold references to ComponentManager.</p> <pre><code>auto query = scene-&gt;CreateQuery();\nauto players = query-&gt;Including&lt;PlayerTag, Health&gt;()\n    -&gt;EntitiesWith&lt;PlayerTag, Health&gt;();\n</code></pre> <p>See the Query reference for the full fluent query API.</p>"},{"location":"api/scene/#event-helpers","title":"Event helpers","text":""},{"location":"api/scene/#addeventlistenert","title":"<code>AddEventListener&lt;T&gt;</code>","text":"<pre><code>Ref&lt;ListenerHandle&gt; AddEventListener&lt;T&gt;(auto&amp;&amp; listener);\n</code></pre> <p>Subscribes to event type <code>T</code>. The subscription is alive as long as the returned <code>ListenerHandle</code> is kept alive.</p> <pre><code>mHandle = scene-&gt;AddEventListener&lt;CollisionEvent&gt;(\n    [](const CollisionEvent&amp; ev) { /* ... */ });\n</code></pre>"},{"location":"api/scene/#sendeventt","title":"<code>SendEvent&lt;T&gt;</code>","text":"<pre><code>void SendEvent&lt;T&gt;(T event);\n</code></pre> <p>Publishes an event immediately to all active subscribers.</p> <pre><code>scene-&gt;SendEvent(CollisionEvent { .entityA = a, .entityB = b });\n</code></pre>"},{"location":"api/scene/#update-loop","title":"Update loop","text":""},{"location":"api/scene/#updatedeltatime","title":"<code>Update(deltaTime)</code>","text":"<pre><code>void Update(float deltaTime);\n</code></pre> <p>Drives the full update cycle:</p> <ol> <li><code>PreUpdate</code> \u2192 all systems across all pipelines</li> <li><code>ExecuteTasks()</code> \u2014 flush pending async tasks</li> <li><code>DestroyEntities()</code> \u2014 process deferred destruction queue</li> <li><code>Update</code> \u2192 all systems across all pipelines (ForEach / ForEachAsync live here)</li> <li><code>ExecuteTasks()</code> \u2014 flush pending async tasks</li> <li><code>DestroyEntities()</code> \u2014 process deferred destruction queue</li> <li><code>PostUpdate</code> \u2192 all systems across all pipelines</li> <li><code>ExecuteTasks()</code> \u2014 flush pending async tasks</li> <li><code>DestroyEntities()</code> \u2014 process deferred destruction queue</li> </ol>"},{"location":"api/scene/#executetasks","title":"<code>ExecuteTasks()</code>","text":"<pre><code>void ExecuteTasks();\n</code></pre> <p>Blocks until all tasks dispatched via <code>ForEachAsync</code> have completed. Called automatically at the end of <code>Update</code>, but can be called manually to create explicit sync points.</p>"},{"location":"api/scene/#archetype-builder","title":"Archetype builder","text":"<pre><code>ArchetypeBuilder CreateArchetypeBuilder() const;\n</code></pre> <p>Returns an <code>ArchetypeBuilder</code> for efficient bulk entity creation. See the ArchetypeBuilder reference.</p>"},{"location":"api/scene/#profiling","title":"Profiling","text":"<pre><code>void BeginProfiling();\nvoid EndProfiling() const;   // flushes trace to disk\nvoid BeginTrace(const char* label);\nvoid EndTrace();\n</code></pre> <p>See the Profiling guide.</p>"},{"location":"api/systems/","title":"Systems","text":"<p>Systems contain the logic of the simulation. Each system declares which components it needs and processes all matching entities during the update loop.</p>"},{"location":"api/systems/#defining-a-system","title":"Defining a system","text":"<p>Inherit from <code>fr::System</code> and override the lifecycle hooks you need:</p> <pre><code>#include &lt;Freyr/Freyr.hpp&gt;\n\nclass MovementSystem : public fr::System {\npublic:\n    explicit MovementSystem(const Ref&lt;fr::Scene&gt;&amp; scene) : System(scene) {}\n\n    void Update(float deltaTime) override {\n        mScene-&gt;ForEachAsync&lt;Position, Velocity&gt;(\n            [deltaTime](fr::Entity, Position&amp; pos, const Velocity&amp; vel) {\n                pos.x += vel.dx * deltaTime;\n                pos.y += vel.dy * deltaTime;\n                pos.z += vel.dz * deltaTime;\n            });\n    }\n};\n</code></pre> <p>The constructor must accept <code>const Ref&lt;fr::Scene&gt;&amp;</code> as its first argument. Additional dependencies are resolved by Skirnir's DI container.</p>"},{"location":"api/systems/#lifecycle-hooks","title":"Lifecycle hooks","text":"<p>Systems are called in this order every frame by <code>Scene::Update(dt)</code>:</p> <pre><code>PreUpdate(dt)\nUpdate(dt)\nPostUpdate(dt)\n</code></pre> Hook Typical use <code>PreUpdate(dt)</code> Input gathering, reset accumulators <code>Update(dt)</code> Main simulation logic <code>PostUpdate(dt)</code> Post-processing, late reads <p>All hooks have a default empty implementation \u2014 override only those you need.</p>"},{"location":"api/systems/#dependency-injection","title":"Dependency injection","text":"<p>Systems are singletons registered in Skirnir's DI container. Any service registered in the container can be injected via the constructor:</p> <pre><code>class CollisionSystem : public fr::System {\npublic:\n    CollisionSystem(const Ref&lt;fr::Scene&gt;&amp; scene, Ref&lt;fr::EventManager&gt; events)\n        : System(scene), mEvents(events) {}\n\n    void Update(float dt) override {\n        mScene-&gt;ForEachAsync&lt;Position, Collider&gt;(\n            [this](fr::Entity a, Position&amp; posA, Collider&amp; colA) {\n                // detect collisions and publish events\n                mScene-&gt;SendEvent(CollisionEvent { .entityA = a });\n            });\n    }\n\nprivate:\n    Ref&lt;fr::EventManager&gt; mEvents;\n};\n</code></pre>"},{"location":"api/systems/#registration","title":"Registration","text":"<p>Register systems within a pipeline using <code>FreyrExtension::WithPipeline()</code>:</p> <pre><code>freyr\n    .WithPipeline([](fr::PipelineBuilder&amp; pipeline) {\n        pipeline.WithName(\"Main\")\n            .WithRate(60.0f)\n            .WithSystem&lt;InputSystem&gt;()      // registered first, runs first\n            .WithSystem&lt;MovementSystem&gt;()\n            .WithSystem&lt;CollisionSystem&gt;()\n            .WithSystem&lt;RenderSystem&gt;();    // registered last, runs last\n    });\n</code></pre> <p>Systems are instantiated and wired in registration order. Multiple pipelines can be defined with different rates.</p>"},{"location":"api/systems/#system-id","title":"System ID","text":"<p>Each system type has a unique runtime ID:</p> <pre><code>fr::SystemId id = fr::GetSystemId&lt;MovementSystem&gt;();\n</code></pre>"},{"location":"api/systems/#accessing-the-scene","title":"Accessing the scene","text":"<p>The protected <code>mScene</code> member provides access to all <code>Scene</code> operations:</p> <pre><code>void Update(float dt) override {\n    // iterate\n    mScene-&gt;ForEach&lt;Health&gt;([](fr::Entity e, Health&amp; hp) { ... });\n\n    // create/destroy entities\n    mScene-&gt;DestroyEntity(deadEnemy);\n\n    // add/remove components\n    mScene-&gt;AddComponent&lt;DeadTag&gt;(entity);\n\n    // send events\n    mScene-&gt;SendEvent(MyEvent {});\n}\n</code></pre>"},{"location":"api/systems/#ordering-and-dependencies","title":"Ordering and dependencies","text":"<p>Systems run in registration order, sequentially. If <code>SystemB</code> depends on results produced by <code>SystemA</code>, register <code>SystemA</code> first.</p> <p>For data dependencies within a system, use <code>ForEach</code> (sequential) rather than <code>ForEachAsync</code> when the callback reads from other entities.</p>"},{"location":"concepts/architecture/","title":"Architecture","text":""},{"location":"concepts/architecture/#high-level-overview","title":"High-level overview","text":"<pre><code>graph TD\n    FB[\"skr::ApplicationBuilder\"] --&gt;|AddExtension| FE[\"FreyrExtension\"]\n    FE --&gt;|registers| CM[\"ComponentManager\"]\n    FE --&gt;|registers| SM[\"SystemManager\"]\n    FE --&gt;|configures| OPT[\"FreyrOptions\"]\n    FB --&gt;|Build| APP[\"IApplication\"]\n    APP --&gt;|GetService| SC[\"Scene\"]\n\n    SC --&gt; CM\n    SC --&gt; EM[\"EntityManager\"]\n    SC --&gt; EVM[\"EventManager\"]\n    SC --&gt; SM\n    SC --&gt; TM[\"ThreadPool\"]\n\n    CM --&gt; ARCH[\"Archetype[]\"]\n    ARCH --&gt; CHUNK[\"ArchetypeChunk[]\"]\n    TM --&gt; WORKERS[\"Worker Threads\"]\n    WORKERS --&gt;|process| CHUNK</code></pre>"},{"location":"concepts/architecture/#core-components","title":"Core components","text":""},{"location":"concepts/architecture/#scene","title":"Scene","text":"<p>The central orchestrator. All entity and component operations flow through <code>Scene</code>, which delegates to the appropriate manager.</p> <pre><code>Scene::Update(dt)\n\u251c\u2500 PreUpdate(dt) \u2192 all systems across all pipelines\n\u251c \u251c\u2500 ExecuteTasks() \u2192 flush async tasks\n\u251c \u2514\u2500 DestroyEntities() \u2192 process deferred destruction queue\n\u251c\u2500 Update(dt)   \u2192 all systems across all pipelines  (ForEach / ForEachAsync live here)\n\u251c \u251c\u2500 ExecuteTasks() \u2192 flush async tasks\n\u251c \u2514\u2500 DestroyEntities() \u2192 process deferred destruction queue\n\u2514\u2500 PostUpdate(dt) \u2192 all systems across all pipelines\n  \u251c\u2500 ExecuteTasks() \u2192 flush async tasks\n  \u2514\u2500 DestroyEntities() \u2192 process deferred destruction queue\n</code></pre>"},{"location":"concepts/architecture/#componentmanager","title":"ComponentManager","text":"<p>Manages archetype discovery and entity placement. When an entity's component set changes, <code>ComponentManager</code> moves it to the correct archetype, migrating its data in place.</p>"},{"location":"concepts/architecture/#entitymanager","title":"EntityManager","text":"<p>Allocates and recycles entity IDs. Uses a free-list internally so destroyed entity slots are reused immediately.</p>"},{"location":"concepts/architecture/#systemmanager","title":"SystemManager","text":"<p>Holds all registered system instances. Calls lifecycle hooks in registration order.</p>"},{"location":"concepts/architecture/#eventmanager","title":"EventManager","text":"<p>Thread-safe publish/subscribe bus. Each event type gets its own <code>Publisher&lt;T&gt;</code>, backed by a shared-read/exclusive-write mutex and a pending-listener queue for safe concurrent subscription.</p>"},{"location":"concepts/architecture/#threadpool","title":"ThreadPool","text":"<p>A fixed-size thread pool with one MPMC (multi-producer, multi-consumer) lock-free queue per worker. Tasks (chunk iterations) are distributed either in dispatch order or by chunk affinity, depending on the configured <code>FreyrExecutionStategy</code>.</p>"},{"location":"concepts/architecture/#memory-layout","title":"Memory layout","text":"<pre><code>Archetype [Position, Velocity]\n\u2502\n\u251c\u2500\u2500 ArchetypeChunk (capacity = 512 entities)\n\u2502   \u251c\u2500\u2500 ComponentArray&lt;Position&gt;   [p0, p1, p2, ..., p511]  \u2190 contiguous\n\u2502   \u2514\u2500\u2500 ComponentArray&lt;Velocity&gt;   [v0, v1, v2, ..., v511]  \u2190 contiguous\n\u2502\n\u2514\u2500\u2500 ArchetypeChunk (capacity = 512 entities)\n    \u251c\u2500\u2500 ComponentArray&lt;Position&gt;   [p512, ..., p1023]\n    \u2514\u2500\u2500 ComponentArray&lt;Velocity&gt;   [v512, ..., v1023]\n</code></pre> <p>Each <code>ComponentArray&lt;T&gt;</code> is a raw contiguous buffer of <code>T</code> values. A chunk owns one array per registered component type. When a system iterates <code>ForEach&lt;Position, Velocity&gt;</code>, it strides through both arrays simultaneously \u2014 all data remains in cache.</p>"},{"location":"concepts/architecture/#execution-flow-for-foreachasync","title":"Execution flow for <code>ForEachAsync</code>","text":"<pre><code>sequenceDiagram\n    participant S as System\n    participant SC as Scene\n    participant CM as ComponentManager\n    participant TM as ThreadPool\n    participant W1 as Worker 1\n    participant W2 as Worker 2\n\n    S-&gt;&gt;SC: ForEachAsync&lt;Position, Velocity&gt;(fn)\n    SC-&gt;&gt;CM: find matching archetypes\n    CM--&gt;&gt;SC: [Archetype A, Archetype B]\n    SC-&gt;&gt;TM: enqueue chunk tasks\n    TM-&gt;&gt;W1: Chunk 0 task\n    TM-&gt;&gt;W2: Chunk 1 task\n    W1--&gt;&gt;W1: process entities 0\u2013511\n    W2--&gt;&gt;W2: process entities 512\u20131023\n    TM--&gt;&gt;SC: all tasks done</code></pre>"},{"location":"concepts/architecture/#execution-strategies","title":"Execution strategies","text":"Strategy Behaviour Best for <code>ChunkAffinity</code> Each chunk is preferentially assigned to the same worker thread across frames Systems that access the same chunks repeatedly \u2014 maximises L1/L2 reuse <code>DispatchOrder</code> Chunks are enqueued to workers in creation order Predictable ordering, simpler debugging <p>Configure via <code>FreyrOptionsBuilder::WithExecutionStrategy</code>.</p>"},{"location":"concepts/ecs-overview/","title":"ECS Overview","text":""},{"location":"concepts/ecs-overview/#what-is-ecs","title":"What is ECS?","text":"<p>Entity-Component-System (ECS) is a software architectural pattern popular in real-time simulations and games. It follows the principle of composition over inheritance: objects in the world are not defined by class hierarchies, but by the data attached to them.</p> Concept Role Entity A lightweight identifier (integer ID) representing a world object Component A plain data structure holding one aspect of an entity's state System Logic that processes all entities matching a specific set of components"},{"location":"concepts/ecs-overview/#why-ecs","title":"Why ECS?","text":""},{"location":"concepts/ecs-overview/#traditional-oop-problems","title":"Traditional OOP problems","text":"<p>In a typical game, a <code>Character</code> class might inherit from <code>PhysicsObject</code>, which inherits from <code>GameObject</code>. Adding a new behaviour (e.g. swimming) requires modifying the hierarchy or adding awkward mixins. Data and behaviour are tightly coupled, and related data is scattered across unrelated memory addresses.</p>"},{"location":"concepts/ecs-overview/#ecs-advantages","title":"ECS advantages","text":"<p>Data-Oriented Design : Components are stored contiguously in memory. When a system processes 1 000 000 positions, it reads a packed array \u2014 no pointer chasing, maximum cache efficiency.</p> <p>Parallel-friendly : Systems that operate on different component sets have no shared state by default. They can run in parallel with no synchronisation. Within a single system, individual entities have no dependencies, so chunk-level parallelism is safe.</p> <p>Dynamic behaviour : Adding or removing a component at runtime changes which systems process that entity \u2014 without recompilation or virtual dispatch.</p>"},{"location":"concepts/ecs-overview/#pros-and-cons","title":"Pros and cons","text":""},{"location":"concepts/ecs-overview/#pros","title":"Pros","text":"<ul> <li>Contiguous data layout \u2192 fewer cache misses \u2192 higher throughput</li> <li>Natural fit for multi-core processing</li> <li>Entities change behaviour by gaining/losing components, not by subclassing</li> <li>Scales to millions of entities</li> </ul>"},{"location":"concepts/ecs-overview/#cons","title":"Cons","text":"<ul> <li>Higher initial complexity than OOP</li> <li>Overkill for small numbers of unique, one-off objects</li> <li>Requires careful component design to avoid data duplication</li> </ul>"},{"location":"concepts/ecs-overview/#ecs-in-freyr","title":"ECS in Freyr","text":"<p>Freyr extends the basic ECS model with archetypes \u2014 groups of entities that share the same set of component types. All entities in an archetype are stored together in fixed-size chunks.</p> <pre><code>Archetype [Position, Velocity]\n\u251c\u2500\u2500 Chunk 0   \u2192 entities 0\u2013511\n\u251c\u2500\u2500 Chunk 1   \u2192 entities 512\u20131023\n\u2514\u2500\u2500 Chunk 2   \u2192 entities 1024\u20131535\n\nArchetype [Position, Health]\n\u2514\u2500\u2500 Chunk 0   \u2192 entities 1536\u20132047\n</code></pre> <p>When you call <code>ForEachAsync&lt;Position, Velocity&gt;</code>, Freyr:</p> <ol> <li>Finds all archetypes whose signature contains <code>Position</code> and <code>Velocity</code></li> <li>Enqueues each matching chunk as an independent task</li> <li>Worker threads pull and process chunks concurrently</li> </ol> <p>This design means: - Cache locality \u2014 all positions in a chunk are contiguous - Zero false sharing \u2014 each chunk belongs to exactly one task at a time - Scalable \u2014 more threads = more chunks processed in parallel</p>"},{"location":"concepts/ecs-overview/#entity-identity","title":"Entity identity","text":"<p>An entity is an integer (<code>fr::Entity</code>, <code>uint64_t</code>). It has no data of its own \u2014 its identity is the union of its components.</p> <p>ID stability</p> <p>An entity's internal ID may change when components are added or removed, as the entity migrates to a different archetype. Do not persist raw entity IDs across frames if the entity's component set changes.</p>"},{"location":"getting-started/installation/","title":"Installation","text":""},{"location":"getting-started/installation/#cmake-fetchcontent-recommended","title":"CMake FetchContent (recommended)","text":"<p>Add the following to your <code>CMakeLists.txt</code>:</p> <pre><code>cmake_minimum_required(VERSION 3.29)\nproject(my_project CXX)\n\nset(CMAKE_CXX_STANDARD 23)\nset(CMAKE_CXX_STANDARD_REQUIRED ON)\n\ninclude(FetchContent)\n\nFetchContent_Declare(\n    freyr\n    GIT_REPOSITORY https://github.com/gilmar-sales/Freyr.git\n    GIT_TAG        main  # pin to a tag or commit SHA for reproducibility\n)\n\nFetchContent_MakeAvailable(freyr)\n\nadd_executable(my_app main.cpp)\ntarget_link_libraries(my_app PRIVATE Freyr)\n</code></pre> <p>Then include the single umbrella header in your source:</p> <pre><code>#include &lt;Freyr/Freyr.hpp&gt;\n</code></pre>"},{"location":"getting-started/installation/#optional-features","title":"Optional features","text":""},{"location":"getting-started/installation/#perfetto-profiling","title":"Perfetto profiling","text":"<p>Enable trace-based profiling with Perfetto:</p> <pre><code>target_compile_definitions(my_app PRIVATE FREYR_PROFILING)\n</code></pre> <p>See the Profiling guide for usage.</p>"},{"location":"getting-started/installation/#building-from-source","title":"Building from source","text":"<pre><code>git clone --recurse-submodules https://github.com/gilmar-sales/Freyr.git\ncd Freyr\ncmake -B build -DCMAKE_BUILD_TYPE=Release\ncmake --build build --parallel\n</code></pre>"},{"location":"getting-started/installation/#running-tests","title":"Running tests","text":"<pre><code>cd build\nctest --output-on-failure\n</code></pre>"},{"location":"getting-started/installation/#running-benchmarks","title":"Running benchmarks","text":"<pre><code>./build/benchmarks/ComponentArray/freyr_component_array_benchmark\n./build/benchmarks/ExecutionStrategy/freyr_execution_strategy_benchmark\n</code></pre>"},{"location":"getting-started/quick-start/","title":"Quick Start","text":"<p>This guide walks through building a minimal simulation: entities with <code>Position</code> and <code>Velocity</code> components updated by a <code>MovementSystem</code>.</p>"},{"location":"getting-started/quick-start/#1-define-components","title":"1. Define components","text":"<p>Components are plain data structs that inherit from <code>fr::Component</code>. No logic, no methods \u2014 just fields.</p> <pre><code>// components.hpp\n#include &lt;Freyr/Freyr.hpp&gt;\n\nstruct Position : fr::Component {\n    float x = 0.f;\n    float y = 0.f;\n    float z = 0.f;\n};\n\nstruct Velocity : fr::Component {\n    float dx = 0.f;\n    float dy = 0.f;\n    float dz = 0.f;\n};\n</code></pre>"},{"location":"getting-started/quick-start/#2-define-a-system","title":"2. Define a system","text":"<p>Systems inherit from <code>fr::System</code> and override lifecycle hooks. The constructor must accept a <code>Ref&lt;fr::Scene&gt;</code>.</p> <pre><code>// movement_system.hpp\n#include &lt;Freyr/Freyr.hpp&gt;\n#include \"components.hpp\"\n\nclass MovementSystem : public fr::System {\npublic:\n    explicit MovementSystem(const Ref&lt;fr::Scene&gt;&amp; scene) : System(scene) {}\n\n    void Update(float deltaTime) override {\n        mScene-&gt;ForEachAsync&lt;Position, Velocity&gt;(\n            [deltaTime](fr::Entity, Position&amp; pos, const Velocity&amp; vel) {\n                pos.x += vel.dx * deltaTime;\n                pos.y += vel.dy * deltaTime;\n                pos.z += vel.dz * deltaTime;\n            });\n    }\n};\n</code></pre>"},{"location":"getting-started/quick-start/#3-define-the-application","title":"3. Define the application","text":"<pre><code>// app.hpp\n#include &lt;Freyr/Freyr.hpp&gt;\n#include \"components.hpp\"\n\nclass MyApp : public skr::IApplication {\npublic:\n    explicit MyApp(const Ref&lt;skr::ServiceProvider&gt;&amp; sp) : IApplication(sp) {\n        mScene = sp-&gt;GetService&lt;fr::Scene&gt;();\n\n        // Bulk-create 1 million entities with default values\n        mScene-&gt;CreateArchetypeBuilder()\n            .WithComponent(Position {})\n            .WithComponent(Velocity { .dx = 1.f, .dy = 0.5f })\n            .WithEntities(1'000'000)\n            .Build();\n    }\n\n    void Run() override {\n        constexpr float dt = 1.0f / 60.0f;\n        while (true)\n            mScene-&gt;Update(dt);\n    }\n\nprivate:\n    Ref&lt;fr::Scene&gt; mScene;\n};\n</code></pre>"},{"location":"getting-started/quick-start/#4-bootstrap-with-freyrextension","title":"4. Bootstrap with FreyrExtension","text":"<pre><code>// main.cpp\n#include &lt;Freyr/Freyr.hpp&gt;\n#include \"app.hpp\"\n#include \"components.hpp\"\n#include \"movement_system.hpp\"\n\nint main() {\n    auto app = skr::ApplicationBuilder()\n        .AddExtension&lt;fr::FreyrExtension&gt;([](fr::FreyrExtension&amp; freyr) {\n            freyr\n                .WithOptions([](fr::FreyrOptionsBuilder&amp; opts) {\n                    opts\n                        .WithMaxEntities(1'500'000)        // max live entities\n                        .WithArchetypeChunkCapacity(512)   // entities per chunk\n                        .WithThreadCount(8);               // worker threads\n                })\n                .WithComponent&lt;Position&gt;()\n                .WithComponent&lt;Velocity&gt;()\n                .WithPipeline([](fr::PipelineBuilder&amp; pipeline) {\n                    pipeline.WithName(\"Main\")\n                        .WithRate(60.0f)\n                        .WithSystem&lt;MovementSystem&gt;();\n                });\n        })\n        .Build&lt;MyApp&gt;();\n\n    app-&gt;Run();\n    return 0;\n}\n</code></pre>"},{"location":"getting-started/quick-start/#5-what-happens-at-runtime","title":"5. What happens at runtime","text":"<pre><code>main()\n \u2514\u2500 ApplicationBuilder::Build&lt;MyApp&gt;()\n     \u251c\u2500 FreyrExtension registers Position, Velocity, MovementSystem\n     \u251c\u2500 Scene is created and added to the DI container\n     \u2514\u2500 MyApp constructor runs\n         \u2514\u2500 ArchetypeBuilder creates 1 000 000 entities in chunks of 512\n\nMyApp::Run() loop\n \u2514\u2500 Scene::Update(dt) each frame\n     \u251c\u2500 MovementSystem::PreUpdate(dt)\n     \u251c\u2500 MovementSystem::Update(dt)   \u2190 ForEachAsync distributes chunks across 8 threads\n     \u251c\u2500 MovementSystem::PostUpdate(dt)\n     \u2514\u2500 deferred entity destruction (none here)\n</code></pre>"},{"location":"getting-started/quick-start/#next-steps","title":"Next steps","text":"<ul> <li>ECS Overview \u2014 understand the model behind the API</li> <li>Scene API \u2014 full reference for entity and component operations</li> <li>ArchetypeBuilder \u2014 efficient bulk entity creation</li> <li>PipelineBuilder \u2014 organize systems into pipelines</li> <li>Parallel Processing guide \u2014 tune parallelism for your workload</li> </ul>"},{"location":"guides/parallel-processing/","title":"Parallel Processing","text":"<p>Freyr provides three iteration modes. Choosing the right one for each situation is the most impactful performance decision you can make.</p>"},{"location":"guides/parallel-processing/#iteration-modes","title":"Iteration modes","text":"Method Blocking Thread pool Use when <code>ForEach</code> Yes No Ordered logic, cross-entity writes, debugging <code>ForEachAsync</code> No Yes Fire-and-forget; sync later with <code>ExecuteTasks()</code>"},{"location":"guides/parallel-processing/#foreach-sequential","title":"<code>ForEach</code> \u2014 sequential","text":"<pre><code>mScene-&gt;ForEach&lt;Position, Velocity&gt;([dt](fr::Entity, Position&amp; pos, Velocity&amp; vel) {\n    pos.x += vel.dx * dt;\n});\n</code></pre> <p>Processes entities one at a time, in chunk order. Safe for any operation, including reading/writing other entities.</p>"},{"location":"guides/parallel-processing/#foreachasync-fire-and-forget","title":"<code>ForEachAsync</code> \u2014 fire-and-forget","text":"<pre><code>// Dispatch tasks without waiting\nmScene-&gt;ForEachAsync&lt;Velocity&gt;([](fr::Entity, Velocity&amp; vel) {\n    vel.dx *= 0.99f;\n});\n\n// Do other sequential work here while tasks run...\nmScene-&gt;ForEach&lt;Health&gt;([](fr::Entity, Health&amp; hp) { ... });\n\n// Wait for async tasks before reading Velocity results\nmScene-&gt;ExecuteTasks();\n</code></pre> <p>Use <code>ForEachAsync</code> to overlap CPU work: start a long parallel computation, do unrelated sequential work, then sync.</p>"},{"location":"guides/parallel-processing/#execution-strategies","title":"Execution strategies","text":"<p>The strategy controls how chunks are assigned to worker threads. Configure it in <code>FreyrOptionsBuilder</code>:</p> <pre><code>opts.WithExecutionStrategy(fr::FreyrExecutionStategy::ChunkAffinity);\n</code></pre>"},{"location":"guides/parallel-processing/#chunkaffinity-default","title":"<code>ChunkAffinity</code> (default)","text":"<p>Each chunk is \"pinned\" to the last worker thread that processed it. On subsequent frames, that same thread handles the chunk again.</p> <p>Effect: the chunk's component arrays stay warm in the thread's private L1/L2 caches across frames.</p> <p>Best for: simulations where the same systems run every frame on the same entities (the common case for games).</p>"},{"location":"guides/parallel-processing/#dispatchorder","title":"<code>DispatchOrder</code>","text":"<p>Chunks are dispatched to workers in creation order, round-robin style.</p> <p>Effect: simpler scheduling, more even distribution if entity populations change frequently.</p> <p>Best for: workloads with high entity churn or one-shot batch operations.</p>"},{"location":"guides/parallel-processing/#tuning-chunk-capacity","title":"Tuning chunk capacity","text":"<p><code>ArchetypeChunkCapacity</code> controls the number of entities per chunk, which directly determines task granularity:</p> <pre><code>Total entities: 1 000 000\nChunk capacity: 512  \u2192 ~1 953 tasks\nChunk capacity: 4096 \u2192 ~244 tasks\n</code></pre> Capacity Task count Overhead Load balance 128 High High Excellent 512 Medium Low Good 1024 Low Very low Fair 4096 Very low Minimal Poor for small N <p>Guideline: start with 512. If you have many short-running callbacks and high thread counts, try 256. If each callback does substantial work (e.g. physics), try 1024\u20134096.</p>"},{"location":"guides/parallel-processing/#labelled-overloads","title":"Labelled overloads","text":"<p>All iteration methods accept an optional label string used in Perfetto traces:</p> <pre><code>mScene-&gt;ForEachAsync&lt;Position, Velocity&gt;(\"Physics::Integrate\", fn);\n</code></pre> <p>This makes it easy to identify hotspots in profiling output. See the Profiling guide.</p>"},{"location":"guides/parallel-processing/#example-overlapping-parallel-work","title":"Example: overlapping parallel work","text":"<pre><code>void Update(float dt) override {\n    // Start async physics integration\n    mScene-&gt;ForEachAsync&lt;Position, Velocity&gt;(\"Integrate\", [dt](fr::Entity, Position&amp; pos, Velocity&amp; vel) {\n        pos.x += vel.dx * dt;\n        pos.y += vel.dy * dt;\n    });\n\n    // Do sequential AI work while integration runs\n    mScene-&gt;ForEach&lt;AIState&gt;(\"AI::Think\", [dt](fr::Entity, AIState&amp; ai) {\n        ai.thinkTimer -= dt;\n        if (ai.thinkTimer &lt;= 0.f)\n            ai.nextAction = computeNextAction(ai);\n    });\n\n    // Sync before anything reads Position\n    mScene-&gt;ExecuteTasks();\n}\n</code></pre>"},{"location":"guides/profiling/","title":"Profiling","text":"<p>Freyr integrates with Perfetto to produce detailed execution traces. Traces can be opened in the Perfetto UI to visualise system timings, chunk iteration durations, and thread utilisation.</p>"},{"location":"guides/profiling/#enable-at-compile-time","title":"Enable at compile time","text":"<p>Define <code>FREYR_PROFILING</code> before building:</p> CMakeCompiler flag <pre><code>target_compile_definitions(my_app PRIVATE FREYR_PROFILING)\n</code></pre> <pre><code>-DFREYR_PROFILING\n</code></pre> <p>When the flag is absent, all profiling calls are compiled out as no-ops with zero overhead.</p>"},{"location":"guides/profiling/#basic-usage","title":"Basic usage","text":"<p>Wrap the section you want to trace with <code>BeginProfiling</code> / <code>EndProfiling</code>:</p> <pre><code>void Run() override {\n    mScene-&gt;BeginProfiling();\n\n    for (int i = 0; i &lt; 200; i++)\n        mScene-&gt;Update(1.0f / 60.0f);\n\n    mScene-&gt;EndProfiling(); // flushes the trace file to disk\n}\n</code></pre> <p><code>EndProfiling</code> writes a <code>.perfetto-trace</code> file to the working directory.</p>"},{"location":"guides/profiling/#viewing-the-trace","title":"Viewing the trace","text":"<ol> <li>Open ui.perfetto.dev in Chrome or Edge</li> <li>Click Open trace file and select the <code>.perfetto-trace</code> file</li> <li>Navigate the timeline to inspect each system and chunk</li> </ol> <p>You will see:</p> <ul> <li>One track per worker thread showing which chunks they processed</li> <li>Named spans for each labelled <code>ForEach</code> / <code>ForEachAsync</code> call</li> <li>System <code>PreUpdate</code> / <code>Update</code> / <code>PostUpdate</code> boundaries</li> </ul>"},{"location":"guides/profiling/#custom-spans","title":"Custom spans","text":"<p>Add your own named spans around any code section:</p> <pre><code>void Update(float dt) override {\n    mScene-&gt;BeginTrace(\"CollisionBroadphase\");\n    runBroadphase();\n    mScene-&gt;EndTrace();\n\n    mScene-&gt;BeginTrace(\"CollisionNarrowphase\");\n    runNarrowphase();\n    mScene-&gt;EndTrace();\n}\n</code></pre> <p>Spans are nested under the calling thread's track in the Perfetto UI.</p>"},{"location":"guides/profiling/#labelling-iterations","title":"Labelling iterations","text":"<p>All iteration methods accept an optional label that appears in the trace:</p> <pre><code>mScene-&gt;ForEach&lt;Position, Velocity&gt;(\"Physics::Integrate\", fn);\nmScene-&gt;ForEachAsync&lt;Position&gt;(\"Render::CullFrustum\", fn);\n</code></pre> <p>Without a label, the lambda's type name is used (often unreadable). Prefer explicit labels in any code you want to profile.</p>"},{"location":"guides/profiling/#profiling-example","title":"Profiling example","text":"<p>The <code>examples/Profiling</code> directory contains a ready-to-run profiling scenario:</p> <pre><code>mScene-&gt;BeginProfiling();\n\n// 2M entities with Position only\nmScene-&gt;CreateArchetypeBuilder()\n    .WithComponent(Position {})\n    .WithEntities(2'000'000)\n    .Build();\n\n// 2M entities with Position + Velocity\nmScene-&gt;CreateArchetypeBuilder()\n    .WithComponent(Position {})\n    .WithComponent(Velocity {})\n    .WithEntities(2'000'000)\n    .Build();\n\nfor (auto i = 0; i &lt; 100; i++)\n    mScene-&gt;Update(1.0f);\n\nmScene-&gt;EndProfiling();\n</code></pre> <p>Build and run with:</p> <pre><code>cmake -B build -DCMAKE_BUILD_TYPE=Release -DFREYR_PROFILING=ON\ncmake --build build --target freyr_profiling_example\n./build/examples/Profiling/freyr_profiling_example\n</code></pre> <p>Then open the resulting trace in ui.perfetto.dev.</p>"},{"location":"guides/profiling/#tips","title":"Tips","text":"<ul> <li>Profile Release builds \u2014 Debug builds have much higher per-entity overhead that distorts results</li> <li>Run multiple warm-up frames before the profiled section to avoid cold-cache skew</li> <li>Compare <code>ChunkAffinity</code> vs <code>DispatchOrder</code> for your specific workload</li> <li>Use the Slice details panel in Perfetto to see exact durations and thread assignments per chunk</li> </ul>"}]}